# 上游 Pixelle-Video 功能更新分析与参考图 / Vision 最佳实践实施方案（覆盖版）

> 文档日期：2026-06-17  
> 适用仓库：`beyondchenlin/Pixelle-Video`  
> 适用分支：`dev`  
> 文件名：`上游功能更新分析.md`  
> 文档用途：本文件可直接覆盖原《上游功能更新分析.md》。  
> 核心结论：原文档对上游能力的优先级判断基本正确，但参考图 / Vision 不能按“轻量同步上游代码”实现；在当前 `dev` 分支中，应升级为“参考图资产化 + Vision 结构化分析 + 接入现有 IP/视觉签名链路 + workflow 能力检测 + trace 脱敏”的生产级方案。

---

## 0. 一页结论

原文档识别的四类上游能力可以保留为分析框架：

| 功能 | 原文档判断 | 覆盖版结论 |
|---|---|---|
| 生图提示词生成优化 / Vision | 强烈推荐借鉴 | 推荐，但不能只新增 `vision_util.py`；必须纳入 LLM trace、Vision 模型配置、结构化分析和脱敏机制 |
| 分镜垫图 / `ref_image` | 推荐借鉴 | 推荐，但不能只加 `StoryboardConfig.ref_image`；必须做任务资产化、workflow 能力检测和注入状态记录 |
| 模板功能增强 | 低优先级 | 保持低优先级；当前分支已有更复杂模板体系 |
| 视频音频智能时长调整 | 已实现，无需开发 | 保持结论；只做回归测试，不重复投入 |

最终推荐路线：

```text
MVP-A：参考图资产化 + Vision 分析 + prompt-only 视觉一致性
  ↓
MVP-B：白名单 workflow 物理垫图
  ↓
生产版：通用 workflow capability 检测 + UI/API + 完整 trace / 测试 / 回滚
```

不建议直接照搬上游轻量方案，也不建议承诺 1–1.5 人天完成生产级能力。当前分支已经具备较复杂的 LLM trace、IPProfile、SeriesVisualSignature、VisualStoryEngine、ImagePromptComposer、MediaService 和 workflow capability 体系，参考图能力应该扩展这些体系，而不是绕开它们。

---

## 1. 当前分支代码审计摘要

### 1.1 `LLMService` 当前仍以 `prompt: str` 为主路径

当前 `LLMService.__call__` 的主入口仍是：

```python
prompt: str
```

没有正式的 `messages` 参数。普通文本调用会把 prompt 包装为单条 user message。结构化输出和 dict JSON 输出也都是基于 prompt 拼装请求。

更重要的是，当前分支的 LLM 调用已经强制要求：

```python
trace_context is not None
trace_recorder is not None
```

这意味着任何新加的 Vision 调用都不能绕开 trace，也不能简单调用：

```python
await llm_client(messages=[...])
```

否则要么直接失败，要么留下不可审计的调用路径。

### 1.2 `StoryboardConfig` 已有 `ref_audio`，但没有 `ref_image`

当前 `StoryboardConfig` 已有：

```python
ref_audio: Optional[str] = None
ref_audio_text: Optional[str] = None
```

但尚无：

```python
ref_image: Optional[str] = None
```

因此原文档中“补一个字段”的判断是成立的，但这只是最低层数据占位，不等于功能可用。

### 1.3 Pipeline 参数是通过 `PipelineContext.params` 传递

当前标准 pipeline 已经基于 `LinearVideoPipeline.__call__(text, **kwargs)`，所有业务参数先进入：

```python
ctx = PipelineContext(
    input_text=text,
    params=pipeline_params,
    ...
)
```

这和原文档里描述的上游旧式 `StandardPipeline.create(..., ref_image=...)` 不完全一致。因此，参考图接入点不应只看函数签名，而要围绕 `PipelineContext` 的生命周期做设计：

1. `setup_environment`：任务目录和输入资产准备。
2. `plan_visuals`：Vision 分析结果接入 IP / 视觉签名 / prompt 规划。
3. `initialize_storyboard`：把可复现配置写入 `StoryboardConfig`。
4. `produce_assets` / `FrameProcessor`：按 workflow binding 决定是否物理传图。
5. `finalize`：写入 metadata / artifacts。

### 1.4 当前已有视觉一致性体系，不能绕开

当前分支已经有：

- `IPProfile`
- `SeriesVisualSignatureRequest`
- `VisualStoryEngineService`
- `VisualStoryBatchOrchestrator`
- `ImagePromptComposer`
- `PromptPlanBundle`
- `final_visual_prompt_artifact`

因此参考图功能不应只是：

```python
reference_image_description + prompt_prefix
```

更合理的路径是：

```text
参考图 → Vision 结构化分析 → ReferenceImageVisualContext
      → runtime IPProfile / supplemental visual_story_context
      → ImagePromptComposer
      → 最终逐帧 prompt
```

### 1.5 workflow capability 目前没有参考图能力字段

当前 workflow 能力主要包括：

```python
supports_negative_prompt
uses_gguf_loaders
local_memory_profile
prefers_isolated_local_execution
```

但没有：

```python
supports_reference_image
reference_image_param_names
```

所以不能假设传入 `ref_image` 后 ComfyUI / RunningHub workflow 一定会消费它。必须做 capability 检测或白名单绑定。

### 1.6 `MediaService` 可以接收额外参数，但缺少安全绑定

`MediaService.__call__(..., **params)` 理论上可以把额外参数传给 workflow：

```python
workflow_params.update(params)
```

这给 `ref_image` 传参提供了低层基础，但也带来风险：

- 参数名不明确。
- `image` 可能是输入图、参考图、初始图、mask 或其他含义。
- trace 可能保存本地绝对路径。
- API 模式可能被滥用为任意服务器路径读取。

所以必须引入 `ReferenceImageWorkflowBinding`，不要靠猜。

### 1.7 音视频智能时长调整已实现

当前 `VideoService.merge_audio_video(...)` 已有：

```python
auto_adjust_duration: bool = True
duration_tolerance: float = 0.3
pad_strategy: str = "freeze"
```

并且已有视频短于音频时 padding、视频长于音频时 trim 的逻辑。该部分只需要回归测试，不建议重复投入。

---

## 2. 原文档合理点与必须修正点

### 2.1 合理点

原文档以下判断应保留：

1. Vision / 参考图值得借鉴，不应只做纯文本 prompt 优化。
2. 模板增强优先级低于参考图和 Vision。
3. 音视频智能时长调整当前分支已有实现，不应重复开发。
4. 参考图不应只作为 prompt_prefix，而应接入视觉一致性链路。
5. trace 脱敏和 workflow 能力检测是生产级实现的必要条件。

### 2.2 必须修正点

原文档或草案中存在几处需要修正的问题：

| 问题 | 修正 |
|---|---|
| 默认开关有矛盾，一处写 `enabled=true`，发布策略又写默认关闭 | 生产默认必须关闭；内部灰度再打开 |
| MVP-A 估算偏乐观 | 生产级 MVP-A 建议 5–8 人天；2.5–4 人天只适合精简 demo |
| `ReferenceImageVisualContext` 中 `runtime_ip_profile` 与伪代码 `merged_ip_profile` 不一致 | 统一使用 `merged_ip_profile` |
| API 输入边界被放到后置 PR | API 任意服务器路径读取风险必须在 PR1 阻断 |
| trace 脱敏只强调 LLM trace | media workflow param trace 也必须摘要化，不能保存绝对路径或图片二进制 |

---

## 3. 目标与非目标

### 3.1 目标

本方案的目标是建立一套可生产使用的参考图与 Vision 能力：

1. 用户可以上传 / 指定参考图。
2. 系统将参考图复制、规范化并记录为任务内资产。
3. 系统可使用 Vision 模型分析参考图，生成结构化视觉身份与风格描述。
4. 分析结果进入现有 IPProfile / SeriesVisualSignature / VisualStoryEngine / ImagePromptComposer 链路。
5. 如果当前 media workflow 明确支持参考图输入，系统再把任务内参考图注入 workflow。
6. 全链路可审计、可复现、可回滚。
7. trace 不保存 base64 图片、不保存用户本机绝对路径、不保存不必要的敏感 metadata。

### 3.2 非目标

第一版不建议做以下事情：

1. 不支持远程 URL 直接下载参考图。
2. 不自动猜测所有 workflow 的参考图参数。
3. 不把 multimodal messages 和 Pydantic structured output 一次性混改。
4. 不把参考图分析结果粗暴拼到 `prompt_prefix`。
5. 不要求所有 workflow 都支持物理垫图。
6. 不重复实现音视频时长调整。
7. 不优先迁移上游单个 HTML 模板。

---

## 4. 生产级原则

### 4.1 参考图不是 prompt_prefix

不推荐：

```python
description = await analyze_ref_image(ref_image)
prompt_prefix = description + "\n" + prompt_prefix
```

原因：

- 绕过已有 IP / 视觉签名系统。
- 无法管理用户已有角色设定与参考图的冲突。
- 难以解释每一帧为什么生成了某种形象。
- 很难沉淀成可复用 profile。
- 容易让 prompt 越来越长，且不可控。

推荐：

```text
ReferenceImageAnalysis
  → ReferenceImageVisualContext
  → runtime IPProfile / supplemental visual_story_context
  → ImagePromptComposer
```

### 4.2 拆分三种能力

参考图功能应拆成三层，而不是混在一个 `ref_image` 字段里：

| 层级 | 说明 | 是否依赖 Vision | 是否依赖 workflow 支持 |
|---|---|---:|---:|
| 参考图资产化 | 上传、校验、复制、压缩、hash、记录 | 否 | 否 |
| Vision 分析 / prompt-only 一致性 | 将参考图转成结构化视觉约束 | 是 | 否 |
| 物理 workflow 垫图 | 把参考图作为 workflow 输入 | 否 | 是 |

这样可以先稳定落地 MVP-A，即使 workflow 暂时不支持垫图，也能通过 prompt-only 提升角色 / 风格一致性。

### 4.3 trace 不能保存 base64

Vision 请求里真实发给模型的 payload 可能包含：

```text
data:image/png;base64,...
```

但 trace 中必须保存脱敏版本：

```json
{
  "type": "image_url",
  "image_url": {
    "url": "<redacted:data-url>",
    "mime_type": "image/png",
    "sha256": "...",
    "byte_size": 123456,
    "width": 1024,
    "height": 768
  }
}
```

必须区分：

```python
wire_messages = messages              # 真正发给模型
trace_messages = redact(messages)      # 写入 trace
```

### 4.4 公共 API 不接受任意服务器路径

本地 CLI / Python SDK 可以接受：

```python
ref_image="/local/path/to/image.png"
```

公共 API / Web UI 不应接受：

```json
{"ref_image": "/etc/passwd"}
```

公共 API 推荐使用：

```json
{
  "reference_image": {
    "upload_id": "...",
    "analysis_mode": "auto",
    "workflow_injection_mode": "auto"
  }
}
```

或：

```json
{
  "reference_image": {
    "artifact_id": "...",
    "analysis_mode": "auto"
  }
}
```

### 4.5 workflow 参数必须显式绑定

不要默认认为这些参数都等于参考图：

```text
image
media
input
file
```

推荐优先级：

```python
STRICT_REFERENCE_IMAGE_PARAM_NAMES = (
    "ref_image",
    "reference_image",
    "reference_image_path",
    "source_image",
    "input_image",
)

AMBIGUOUS_REFERENCE_IMAGE_PARAM_NAMES = (
    "image",
)
```

`image` 只能在 workflow metadata 明确声明它是参考图 / 控制图时使用，或通过白名单 override 使用。

### 4.6 一切功能必须可关闭

必须支持：

```yaml
reference_image:
  enabled: false
```

关闭后：

- 不执行 Vision 分析。
- 不修改 prompt 规划。
- 不向 workflow 传任何参考图参数。
- 不影响旧视频生成。

---

## 5. 推荐总体架构

```mermaid
flowchart TD
    A[API/UI/CLI 输入参考图] --> B[ReferenceImageInputResolver]
    B --> C[ReferenceImageAssetService]
    C --> D[task_dir/reference_image/asset.json]
    C --> E[vision image variant]
    C --> F[workflow image variant]

    E --> G{analysis_mode}
    G -->|off| H[跳过分析]
    G -->|auto|required| I[ReferenceImageAnalysisService]
    I --> J[LLMService multimodal messages]
    J --> K[ReferenceImageAnalysis JSON]

    K --> L[ReferenceImageVisualContextAdapter]
    H --> L
    L --> M[合并到 IPProfile / visual_story_context / prompt fallback]
    M --> N[VisualStoryEngine / ImagePromptComposer]
    N --> O[最终逐帧 media prompt]

    F --> P{workflow_injection_mode}
    P -->|off| Q[prompt-only]
    P -->|auto|required| R[WorkflowReferenceImageCapabilityResolver]
    R -->|supported| S[FrameProcessor 注入参数]
    R -->|unsupported auto| Q
    R -->|unsupported required| T[fail-fast]
    S --> U[MediaService workflow execution]
    Q --> U
```

---

## 6. 配置设计

### 6.1 生产默认配置

生产默认必须关闭，避免无意中改变旧流程：

```yaml
reference_image:
  enabled: false
  analysis_mode: off              # off | auto | required
  workflow_injection_mode: off    # off | auto | required
  profile_merge_mode: supplement  # supplement | override | strict

  allowed_extensions: [".jpg", ".jpeg", ".png", ".webp"]
  max_upload_size_mb: 20
  max_vision_edge_px: 1024
  max_workflow_edge_px: 2048
  strip_exif: true
  convert_to_png_for_workflow: false

  allow_ambiguous_image_param: false
  workflow_param_overrides: {}

vision_llm:
  enabled: false
  api_key: ""
  base_url: ""
  model: ""
  temperature: 0.2
  max_tokens: 1200
  max_image_size_mb: 5
  max_vision_edge_px: 1024
  unavailable_policy: skip   # skip | fail
  force_supports_vision: null
```

### 6.2 内部灰度配置

内部灰度可以打开资产化和 prompt-only：

```yaml
reference_image:
  enabled: true
  analysis_mode: auto
  workflow_injection_mode: off

vision_llm:
  enabled: true
  model: qwen-vl-max
```

### 6.3 白名单物理垫图配置

MVP-B 阶段只对明确验证过的 workflow 开启：

```yaml
reference_image:
  enabled: true
  analysis_mode: auto
  workflow_injection_mode: auto
  workflow_param_overrides:
    "selfhost/image_ipadapter_example.json": "reference_image"
    "runninghub/some_reference_workflow.json": "ref_image"
```

### 6.4 配置落点

当前 config schema 尚无 `reference_image` 或 `vision_llm` 顶层字段，所以必须修改配置 schema，而不是只改 `config.example.yaml`。建议新增：

```python
class ReferenceImageConfig(BaseModel):
    enabled: bool = False
    analysis_mode: Literal["off", "auto", "required"] = "off"
    workflow_injection_mode: Literal["off", "auto", "required"] = "off"
    profile_merge_mode: Literal["supplement", "override", "strict"] = "supplement"
    allowed_extensions: list[str] = [".jpg", ".jpeg", ".png", ".webp"]
    max_upload_size_mb: int = 20
    max_vision_edge_px: int = 1024
    max_workflow_edge_px: int = 2048
    strip_exif: bool = True
    convert_to_png_for_workflow: bool = False
    allow_ambiguous_image_param: bool = False
    workflow_param_overrides: dict[str, str] = Field(default_factory=dict)


class VisionLLMConfig(BaseModel):
    enabled: bool = False
    api_key: str = ""
    base_url: str = ""
    model: str = ""
    temperature: float = 0.2
    max_tokens: int = 1200
    max_image_size_mb: int = 5
    max_vision_edge_px: int = 1024
    unavailable_policy: Literal["skip", "fail"] = "skip"
    force_supports_vision: bool | None = None
```

并挂到：

```python
class PixelleVideoConfig(BaseModel):
    ...
    reference_image: ReferenceImageConfig = Field(default_factory=ReferenceImageConfig)
    vision_llm: VisionLLMConfig = Field(default_factory=VisionLLMConfig)
```

---

## 7. 数据模型设计

### 7.1 `ReferenceImageAsset`

新增文件：`pixelle_video/models/reference_image.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any, Mapping


@dataclass(frozen=True)
class ReferenceImageAsset:
    source_kind: str                 # local_path | uploaded_file | artifact_id
    original_display_name: str       # sanitized, never trusted as path
    task_asset_path: str             # absolute task-local path, internal use only
    task_asset_relative_path: str    # stored in trace / metadata
    vision_asset_path: str | None
    vision_asset_relative_path: str | None
    workflow_asset_path: str
    workflow_asset_relative_path: str
    sha256: str
    mime_type: str
    width: int
    height: int
    byte_size: int
    normalized_width: int | None = None
    normalized_height: int | None = None
    metadata: Mapping[str, Any] = field(default_factory=dict)

    def to_trace_dict(self) -> dict[str, Any]:
        return {
            "sha256": self.sha256,
            "mime_type": self.mime_type,
            "width": self.width,
            "height": self.height,
            "byte_size": self.byte_size,
            "task_asset_relative_path": self.task_asset_relative_path,
            "vision_asset_relative_path": self.vision_asset_relative_path,
            "workflow_asset_relative_path": self.workflow_asset_relative_path,
            "source_kind": self.source_kind,
        }
```

要求：

- `task_asset_path` 仅供内部运行使用。
- metadata / trace / final result 中只保存相对路径和摘要。
- 不保存用户原始绝对路径。
- 不直接使用用户上传文件名。

### 7.2 `ReferenceImageAnalysis`

新增文件：`pixelle_video/models/reference_image_analysis.py`

```python
from pydantic import BaseModel, Field


class ReferenceImageAnalysis(BaseModel):
    subject_summary: str = Field(default="")
    style_summary: str = Field(default="")
    color_atmosphere: str = Field(default="")
    composition_summary: str = Field(default="")
    identity_anchors: list[str] = Field(default_factory=list)
    style_anchors: list[str] = Field(default_factory=list)
    negative_constraints: list[str] = Field(default_factory=list)
    prompt_hint_en: str = ""
    prompt_hint_zh: str = ""
    confidence: float = 0.0
    limitations: list[str] = Field(default_factory=list)
```

### 7.3 `ReferenceImageVisualContext`

新增模型：

```python
from pydantic import BaseModel, Field


class ReferenceImageVisualContext(BaseModel):
    enabled: bool = False
    asset: dict = Field(default_factory=dict)
    analysis: dict = Field(default_factory=dict)
    merged_ip_profile: dict | None = None
    supplemental_visual_story_context: dict = Field(default_factory=dict)
    prompt_fallback_hint: str = ""
    merge_mode: str = "supplement"
    merge_warnings: list[str] = Field(default_factory=list)
```

注意：统一使用 `merged_ip_profile`，不要再同时出现 `runtime_ip_profile` 和 `merged_ip_profile` 两套命名。

推荐 adapter 返回：

```python
@dataclass(frozen=True)
class ReferenceImageContextBuildResult:
    visual_context: ReferenceImageVisualContext
    ip_profile: IPProfile | None
    visual_story_context_patch: dict[str, Any]
```

### 7.4 `ReferenceImageWorkflowBinding`

新增模型：

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class ReferenceImageWorkflowBinding:
    supported: bool
    param_name: str | None = None
    workflow_key: str = ""
    media_type: str = ""
    source: str = "none"    # capability | whitelist | none
    reason: str = ""

    def to_dict(self) -> dict:
        return {
            "supported": self.supported,
            "param_name": self.param_name,
            "workflow_key": self.workflow_key,
            "media_type": self.media_type,
            "source": self.source,
            "reason": self.reason,
        }
```

### 7.5 `StoryboardConfig` 扩展

建议新增字段：

```python
ref_image: Optional[str] = None
ref_image_asset: Optional[Dict[str, Any]] = None
reference_image_analysis: Optional[Dict[str, Any]] = None
reference_image_visual_context: Optional[Dict[str, Any]] = None
reference_image_workflow_binding: Optional[Dict[str, Any]] = None
reference_image_workflow_injection_mode: str = "auto"
```

说明：

- `ref_image` 是低层 workflow 使用的任务内图片路径。
- `ref_image_asset` 用于复现和 metadata。
- `reference_image_analysis` 用于调试 Vision 输出。
- `reference_image_visual_context` 用于解释 prompt planning。
- `reference_image_workflow_binding` 用于说明是否真的物理垫图。

---

## 8. 参考图资产化服务

新增文件：`pixelle_video/services/reference_image_asset_service.py`

职责：

1. 解析输入来源：local path / upload id / artifact id。
2. 校验源文件存在且在允许范围内。
3. 使用 Pillow 验证真实图片格式。
4. 防止解压炸弹与超大尺寸图片。
5. 去除 EXIF 和敏感 metadata。
6. 生成 vision 版本和 workflow 版本。
7. 计算 sha256、mime type、尺寸、字节数。
8. 写入 `reference_image/asset.json`。
9. 返回 `ReferenceImageAsset`。

推荐任务目录：

```text
{task_dir}/reference_image/
  original_{sha8}.jpg
  vision_{sha8}.jpg
  workflow_{sha8}.png
  preview_{sha8}.jpg
  asset.json
```

图片规格：

| 用途 | 推荐规格 |
|---|---|
| Vision | 长边 ≤ 1024px，JPEG/WebP，质量 85 |
| Workflow | 长边 ≤ 2048px，PNG 或原格式 |
| UI Preview | 长边 ≤ 256px |
| Trace | 只保存摘要，不保存二进制 |

安全要求：

- 禁止 SVG、HEIC、TIFF 等未明确支持格式。
- 禁止直接使用用户原始文件名。
- 禁止跟随不可信 symlink。
- 禁止写出 task_dir。
- 可选保存原图；如保存，必须在 task_dir 内。
- EXIF 默认剥离。

---

## 9. LLMService messages 改造

### 9.1 第一阶段边界

第一阶段只建议支持：

| 路径 | `messages` 支持 | 说明 |
|---|---:|---|
| `response_type=None` | 是 | Vision 分析需要 |
| `response_type=dict` | 否 | 当前 dict JSON 会改写 prompt，暂不混改 |
| Pydantic structured output | 否 | 当前 structured output 会改写 prompt，暂不混改 |

Vision 结构化分析由 `ReferenceImageAnalysisService` 负责：

1. 用 `messages` 调普通 LLM 输出字符串。
2. 用 `parse_llm_json_response(...)` 解析。
3. 用 Pydantic 校验。
4. 按 `auto|required` 策略处理失败。

### 9.2 推荐签名

```python
async def __call__(
    self,
    prompt: Optional[str] = None,
    *,
    messages: Optional[list[dict[str, Any]]] = None,
    api_key: Optional[str] = None,
    base_url: Optional[str] = None,
    model: Optional[str] = None,
    temperature: float = 0.7,
    max_tokens: int = 2000,
    response_type: Optional[Type[T] | Type[dict]] = None,
    trace_context: Optional[LLMTraceContext] = None,
    trace_recorder: Optional[LLMInteractionRecorder] = None,
    **kwargs,
) -> Union[str, T, dict[str, Any]]:
    ...
```

校验：

```python
if prompt is None and messages is None:
    raise ValueError("Either prompt or messages must be provided")
if prompt is not None and messages is not None:
    raise ValueError("Cannot provide both prompt and messages")
if messages is not None and response_type is not None:
    raise ValueError("response_type is not supported with multimodal messages in phase 1")
```

### 9.3 token 预检策略

当前 `prompt` 路径有 token 预估。`messages` 路径不能直接把图片 data URL 计入普通文本 token 估算，否则会严重误判。建议第一阶段：

```python
estimated_text_tokens = estimate_messages_text_tokens(redacted_or_text_parts)
if estimated_text_tokens > capabilities.max_input_tokens:
    raise ValueError(...)
```

图片限制由 `vision_llm.max_image_size_mb` 和 `max_vision_edge_px` 控制，而不是用 base64 字符串长度粗暴估算。

### 9.4 trace 脱敏

不要这样：

```python
request_payload = self._build_request_payload(messages=messages)
await client.chat.completions.create(messages=messages)
await self._record_llm_trace(request_payload=request_payload)
```

推荐：

```python
wire_messages = messages
trace_messages = redact_multimodal_messages_for_trace(messages)

response = await client.chat.completions.create(
    model=final_model,
    messages=wire_messages,
    temperature=temperature,
    max_tokens=max_tokens,
    **kwargs,
)

request_payload_for_trace = self._build_request_payload(
    model=final_model,
    messages=trace_messages,
    temperature=temperature,
    max_tokens=max_tokens,
    extra_parameters=kwargs,
)
```

脱敏规则：

| 输入 | trace 保存 |
|---|---|
| `data:image/png;base64,...` | `<redacted:data-url>` + mime + sha256 + byte_size |
| 任务内图片路径 | 任务相对路径 + sha256 |
| 远程 URL | 第一版不支持；如后续支持，只保存 domain/hash，不保存签名 URL |
| 文本 | 原文可保存，但遵循现有 preview 限制 |

必须测试：

```text
"base64," not in request_payload_json
"data:image" not in request_payload_json，或仅出现 redacted marker
用户本机绝对路径 not in request_payload_json
```

---

## 10. media workflow trace 摘要化

这是必须补充的阻断项，不应只做 LLM trace 脱敏。

当前 media prompt trace 链会记录 workflow 参数，且 `image`、`reference_image`、`source_image`、`target_image`、`video`、`media`、`ref_audio` 等都属于 traceable 参数。参考图传入 workflow 后，如果不做处理，trace 可能保存绝对路径或二进制 data URL。

推荐将参考图 workflow 参数统一摘要化：

```json
{
  "workflow_param_inputs": {
    "reference_image": {
      "kind": "task_reference_image",
      "relative_path": "reference_image/workflow_ab12cd34.png",
      "sha256": "...",
      "mime_type": "image/png",
      "width": 1024,
      "height": 768,
      "byte_size": 123456
    }
  }
}
```

不得保存：

```text
C:\Users\...
/Users/...
/home/...
data:image/png;base64,...
```

由于 media trace 存在 artifact/context/actual trace 三方校验，必须保证这三处使用同一套摘要化结构，否则会触发 trace 校验不一致。

建议新增 helper：

```python
def normalize_reference_image_workflow_param_for_trace(
    *,
    param_name: str,
    value: Any,
    asset: ReferenceImageAsset | None,
) -> dict[str, Any]:
    ...
```

并在构建 `workflow_param_inputs` 前调用。

---

## 11. Vision 模型配置与能力检测

### 11.1 不默认复用主文本 LLM

不要默认认为：

```yaml
llm:
  model: qwen-max
```

就可以处理图片。应该增加独立配置：

```yaml
vision_llm:
  enabled: true
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-vl-max
```

### 11.2 能力模型

新增：

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class VisionCapabilities:
    supports_vision_messages: bool = False
    supports_data_url_images: bool = True
    max_image_size_mb: int | None = None
    reason: str = ""
```

检测策略：

1. 配置显式声明优先。
2. 官方 known model whitelist 次之。
3. 字符串猜测只作为提示，不作为强依据。
4. 不确定时默认不支持。

默认建议：

| Provider / model | 默认判断 |
|---|---|
| OpenAI `gpt-4o` / 明确 vision 模型 | 支持 |
| DashScope `qwen-vl-*` | 支持 |
| DashScope `qwen-max` | 不默认支持 |
| DeepSeek chat | 不默认支持 |
| Ollama | 只有配置声明时支持 |

---

## 12. ReferenceImageAnalysisService

新增：`pixelle_video/services/reference_image_analysis.py`

接口：

```python
class ReferenceImageAnalysisService:
    async def analyze(
        self,
        *,
        llm_service: LLMService,
        asset: ReferenceImageAsset,
        prompt_language: str,
        trace_context: LLMTraceContext,
        trace_recorder: LLMInteractionRecorder,
        vision_config: Mapping[str, Any],
    ) -> ReferenceImageAnalysis:
        ...
```

### 12.1 Prompt 原则

要求模型输出 JSON，而不是自由散文。

建议系统规则：

```text
You are analyzing a reference image for consistent AI video storyboard generation.
Return ONLY a valid JSON object.
Do not identify real people.
Do not infer sensitive attributes.
Do not claim copyrighted character identity unless explicitly supplied by the user.
Focus on visible visual traits, style, composition, palette, clothing, pose, and reusable anchors.
```

输出示例：

```json
{
  "subject_summary": "...",
  "style_summary": "...",
  "color_atmosphere": "...",
  "composition_summary": "...",
  "identity_anchors": ["..."],
  "style_anchors": ["..."],
  "negative_constraints": ["..."],
  "prompt_hint_en": "...",
  "prompt_hint_zh": "...",
  "confidence": 0.82,
  "limitations": []
}
```

### 12.2 失败策略

| 场景 | `auto` | `required` |
|---|---|---|
| `vision_llm` 未配置 | 跳过分析 | 报错 |
| 模型不支持 Vision | 跳过分析 | 报错 |
| JSON 解析失败 | retry 一次，失败后跳过 | retry 一次，失败后报错 |
| confidence 低 | style-only 降级 | 可报错或 warning，由配置决定 |

### 12.3 缓存

推荐缓存 key：

```text
sha256 + vision_model + prompt_version + language
```

MVP 可先不做跨任务缓存，但 `analysis.json` 应记录：

- image sha256
- vision model
- prompt version
- analysis language
- created_at

---

## 13. 真实接入 ImagePromptComposer

这是本方案最关键的部分。

不要只做：

```python
ctx.params["reference_image_analysis"] = analysis.model_dump()
```

这样不能保证 `ImagePromptComposer.compose(...)` 实际消费它。

推荐在 `StandardPipeline.plan_visuals` 中，完成 `ip_profile` 获取之后、调用 `VisualStoryEngineService.prepare(...)` 和 `ImagePromptComposer.compose(...)` 之前插入：

```python
build_result = ReferenceImageVisualContextAdapter().build(
    asset=ctx.reference_image_asset,
    analysis=ctx.reference_image_analysis,
    existing_ip_profile=ip_profile,
    storyboard_contract=storyboard_contract,
    params=ctx.params,
)

ctx.reference_image_visual_context = build_result.visual_context
ip_profile = build_result.ip_profile or ip_profile
visual_story_context = merge_visual_story_context(
    visual_story_context,
    build_result.visual_story_context_patch,
)
ctx.params["reference_image_visual_context"] = build_result.visual_context.model_dump()
```

随后现有调用自然消费：

```python
visual_story_plan = await VisualStoryEngineService().prepare(
    ...,
    ip_profile=ip_profile,
    ...,
)

styled_batch = await ImagePromptComposer().compose(
    ...,
    ip_profile=ip_profile,
    visual_story_context=visual_story_context,
    ...,
)
```

### 13.1 映射规则

| `ReferenceImageAnalysis` | 建议进入位置 |
|---|---|
| `subject_summary` | `IPProfile.visual_summary` 或 supplemental identity summary |
| `style_summary` | `IPProfile.style_hint` |
| `identity_anchors` | `IPProfile.identity_anchors` |
| `style_anchors` | `IPProfile.style_boundary_rules` |
| `negative_constraints` | `IPProfile.negative_constraints` |
| `color_atmosphere` | `metadata.reference_image_color_atmosphere` |
| `prompt_hint_zh/en` | prompt-only fallback hint |

### 13.2 用户已有 IPProfile 时

合并规则：

1. 用户显式 IPProfile 优先。
2. 参考图只能补充，不默认覆盖。
3. 合并后记录 `merge_warnings`。
4. 如果参考图和已有 profile 明显冲突，默认 warning，不报错。
5. 可新增 `reference_image_profile_merge_mode`: `supplement|override|strict`，默认 `supplement`。

### 13.3 没有已有 IPProfile 时

不要强依赖 asset bible。MVP-A 默认策略：

1. 不强造持久化 `IPProfile`。
2. 优先生成 `supplemental_visual_story_context`。
3. 同时生成 `prompt_fallback_hint`，供 prompt-only 一致性使用。
4. 如确实需要 runtime-only IPProfile，必须补齐 `workspace_id`、`project_id`、`series_visual_signature_profile_id`、`name` 等必填字段，并明确不写入 asset bible。

推荐 MVP-A 默认：

```text
无用户 IPProfile：走 supplemental_visual_story_context + prompt_fallback_hint
有用户 IPProfile：走 supplement 合并，不覆盖用户显式字段
```

---

## 14. workflow 参考图能力检测

### 14.1 扩展 `WorkflowCapabilities`

```python
@dataclass(frozen=True)
class WorkflowCapabilities:
    supports_negative_prompt: bool = False
    uses_gguf_loaders: bool = False
    local_memory_profile: LocalMemoryProfile = "standard"
    prefers_isolated_local_execution: bool = False
    supports_reference_image: bool = False
    reference_image_param_names: tuple[str, ...] = ()
```

### 14.2 检测来源

| workflow 类型 | 检测方式 |
|---|---|
| selfhost | 解析 workflow metadata / params / node inputs |
| runninghub | 解析 wrapper declared params |
| 不确定 | 默认不支持，除非 override |
| 白名单 | `reference_image.workflow_param_overrides` 明确指定 |

### 14.3 参数选择规则

```python
STRICT_REFERENCE_IMAGE_PARAM_NAMES = (
    "ref_image",
    "reference_image",
    "reference_image_path",
    "source_image",
    "input_image",
)

AMBIGUOUS_REFERENCE_IMAGE_PARAM_NAMES = (
    "image",
)
```

规则：

- strict 参数命中即可认为支持。
- `image` 只有在 metadata 明确标注它是 reference/control/input image 时才使用。
- 如果同时有多个候选参数，必须记录选择依据。
- 如果参数冲突，`required` 模式报错，`auto` 模式降级 prompt-only。

### 14.4 白名单 MVP

在通用检测完成前，推荐先做白名单：

```yaml
reference_image:
  workflow_param_overrides:
    "selfhost/image_ipadapter_example.json": "reference_image"
    "runninghub/some_reference_workflow.json": "ref_image"
```

这比猜参数名更安全，也更适合快速验证。

---

## 15. FrameProcessor 与 MediaService 接入

### 15.1 FrameProcessor

在 `_step_generate_media(...)` 中增加：

```python
binding = getattr(config, "reference_image_workflow_binding", None) or {}
ref_image = getattr(config, "ref_image", None)

if ref_image and binding.get("supported"):
    param_name = binding["param_name"]
    media_params[param_name] = ref_image
elif ref_image and config.reference_image_workflow_injection_mode == "required":
    raise ValueError(
        f"media_workflow {config.media_workflow!r} does not support reference image input"
    )
```

同时记录注入状态：

```json
{
  "reference_image_injection": {
    "status": "used|skipped|unsupported|required_failed",
    "param_name": "reference_image",
    "asset_sha256": "...",
    "relative_path": "reference_image/workflow_ab12cd34.png"
  }
}
```

### 15.2 MediaService

`MediaService` 可以继续通过 `**params` 接收参数，但必须补充 trace 摘要化：

- 如果 workflow params 中出现 reference image 参数，只保存摘要。
- 不保存图片二进制。
- 不保存用户绝对路径。
- 不把任意 `image` 参数都当成参考图。

建议 helper：

```python
def summarize_reference_image_workflow_param(
    value: Any,
    asset: ReferenceImageAsset | None,
) -> dict[str, Any]:
    ...
```

---

## 16. PipelineContext 与 StandardPipeline 接入点

### 16.1 `PipelineContext` 扩展

```python
reference_image_asset: Optional[ReferenceImageAsset] = None
reference_image_analysis: Optional[ReferenceImageAnalysis] = None
reference_image_visual_context: Optional[ReferenceImageVisualContext] = None
reference_image_workflow_binding: Optional[ReferenceImageWorkflowBinding] = None
```

### 16.2 `setup_environment`

职责：资产化。

```python
raw_reference_image = resolve_reference_image_input(ctx.params)
if raw_reference_image:
    ctx.reference_image_asset = ReferenceImageAssetService(...).prepare(
        raw_reference_image,
        task_dir=ctx.task_dir,
    )
    ctx.params["ref_image"] = ctx.reference_image_asset.workflow_asset_path
    ctx.params["ref_image_asset"] = ctx.reference_image_asset.to_trace_dict()
```

注意：API 模式必须在这里之前已经把 `upload_id` / `artifact_id` 解析为受控临时文件；不能让用户传任意服务器路径。

### 16.3 `plan_visuals`

职责：分析 + 进入视觉规划。

推荐顺序：

1. 解析 storyboard contract。
2. 如有参考图且 `analysis_mode != off`，执行 Vision 分析。
3. 获取或创建 `ip_profile`。
4. 用 `ReferenceImageVisualContextAdapter` 合并参考图分析。
5. 调用 VisualStoryEngine。
6. 调用 ImagePromptComposer。

关键点：必须在 `ImagePromptComposer.compose(...)` 前完成合并。

### 16.4 `initialize_storyboard`

写入 config：

```python
ref_image=ctx.params.get("ref_image"),
ref_image_asset=ctx.params.get("ref_image_asset"),
reference_image_analysis=(
    ctx.reference_image_analysis.model_dump()
    if ctx.reference_image_analysis else None
),
reference_image_visual_context=(
    ctx.reference_image_visual_context.model_dump()
    if ctx.reference_image_visual_context else None
),
reference_image_workflow_binding=(
    ctx.reference_image_workflow_binding.to_dict()
    if ctx.reference_image_workflow_binding else None
),
```

---

## 17. 可观测性 artifacts

建议在任务目录生成：

```text
reference_image/
  asset.json
  analysis.json
  visual_context.json
  workflow_support.json
  injection_summary.json
```

`injection_summary.json` 示例：

```json
{
  "analysis_status": "success|skipped|failed",
  "visual_context_status": "merged|skipped",
  "workflow_injection_status": "used|prompt_only|unsupported|required_failed|off",
  "workflow_key": "selfhost/image_xxx.json",
  "param_name": "reference_image",
  "asset_sha256": "...",
  "warnings": []
}
```

这些 artifact 应进入 task metadata，方便 UI 展示和问题排查。

---

## 18. API / UI 建议

### 18.1 API 输入

推荐：

```json
{
  "text": "...",
  "reference_image": {
    "upload_id": "...",
    "analysis_mode": "auto",
    "workflow_injection_mode": "auto",
    "profile_merge_mode": "supplement"
  }
}
```

或：

```json
{
  "text": "...",
  "reference_image": {
    "artifact_id": "...",
    "analysis_mode": "required",
    "workflow_injection_mode": "off"
  }
}
```

短期兼容：

```json
{
  "text": "...",
  "ref_image": "<server-controlled-temp-file>"
}
```

但 API 层必须保证该路径来自受控上传目录，而不是用户任意传入。

### 18.2 UI 显示

UI 应显示：

- 参考图是否上传成功。
- 是否执行 Vision 分析。
- Vision 分析摘要。
- 本次是 prompt-only 还是 workflow 真的使用了参考图。
- workflow 不支持参考图时的降级原因。

### 18.3 用户文案

示例：

| 状态 | 用户提示 |
|---|---|
| Vision 未配置 | 已保存参考图，但未配置视觉模型，本次将使用普通提示词生成 |
| Vision 成功 | 已分析参考图，并用于保持角色 / 风格一致性 |
| workflow 不支持 | 当前工作流不支持物理参考图输入，已采用 prompt-only 一致性 |
| required 失败 | 当前工作流不支持参考图输入，请切换支持 IPAdapter / ControlNet / reference image 的工作流 |

---

## 19. 错误处理策略

| 场景 | `auto` 策略 | `required` 策略 | 用户提示 |
|---|---|---|---|
| 没有配置 Vision 模型 | 跳过分析 | 报错 | 请配置支持图片的 Vision 模型，或关闭必须分析 |
| 主 LLM 是文本模型 | 不使用主 LLM 做 Vision | 报错 | 当前 `vision_llm.model` 不支持图片 |
| 图片无效 | 报错 | 报错 | 参考图不是有效图片或格式不支持 |
| 图片过大 | 尝试压缩，硬限制报错 | 同左 | 图片过大，请上传较小图片 |
| workflow 不支持垫图 | prompt-only | 报错 | 当前 workflow 不支持参考图输入 |
| workflow 参数冲突 | prompt-only + warning | 报错 | workflow 参考图参数不明确 |
| Vision JSON 解析失败 | retry 后跳过 | retry 后报错 | 参考图分析失败 |
| trace 写入失败 | 现有策略可继续 | 记录 warning | 生成继续，但需运维排查 trace 存储 |

---

## 20. 测试计划

### 20.1 单元测试

建议新增：

```text
tests/models/test_reference_image_asset.py
tests/models/test_reference_image_analysis.py
tests/services/test_reference_image_asset_service.py
tests/services/test_reference_image_analysis_service.py
tests/services/test_llm_service_multimodal_trace_redaction.py
tests/services/test_vision_capabilities.py
tests/services/test_workflow_reference_image_capabilities.py
tests/services/test_reference_image_visual_context_adapter.py
tests/services/test_frame_processor_reference_image.py
tests/pipelines/test_standard_reference_image_flow.py
```

### 20.2 关键断言

LLM：

- `prompt` 与 `messages` 二选一。
- `messages + response_type` 报错。
- wire payload 可包含 data URL。
- trace payload 不包含 base64。
- trace payload 不包含用户绝对路径。

资产：

- 拒绝伪图片。
- 拒绝超大维度图片。
- 去除 EXIF。
- 文件名不含用户原始路径。
- 输出 `asset.json`。

视觉上下文：

- 没有已有 IPProfile 时走 `supplemental_visual_story_context`。
- 有已有 IPProfile 时 supplement 合并，不覆盖用户显式字段。
- 最终传给 `ImagePromptComposer` 的 `ip_profile` 或 `visual_story_context` 包含参考图分析。

workflow：

- strict 参数命中时传图。
- ambiguous `image` 默认不传。
- whitelist override 生效。
- required 模式下不支持则报错。
- auto 模式下不支持则 prompt-only。

回归：

- 无参考图时，旧标准 pipeline 行为不变。
- structured output 旧测试不受 multimodal 改造影响。
- media trace 不保存图片内容。
- 音视频智能时长调整原测试继续通过。

---

## 21. 分 PR 实施计划

### PR 1：配置 schema + 输入边界 + 资产化

交付：

- `ReferenceImageConfig`
- `VisionLLMConfig`
- `ReferenceImageAsset`
- `ReferenceImageAssetService`
- `PipelineContext.reference_image_asset`
- `StandardPipeline.setup_environment` 接入
- API 不接受任意服务器路径的校验
- feature flag 默认关闭

验收：

- `ref_image` 被复制为任务资产。
- `asset.json` 存在。
- 无参考图时旧流程不变。
- API 传 `/etc/passwd`、`C:\Windows\...` 等路径直接拒绝。
- metadata / trace 不记录用户原始绝对路径。

估算：1.5–2.5 人天。

### PR 2：LLM messages + trace 脱敏

交付：

- `LLMService.__call__(messages=...)`
- trace 脱敏函数
- Vision capabilities 初版
- 禁止 `messages + response_type`
- messages 路径 token / image size 预检

验收：

- fake multimodal 调用成功。
- trace 不含 base64。
- trace 不含本机绝对路径。
- 旧 structured output 不回归。

估算：1.5–2.5 人天。

### PR 3：Vision 分析服务

交付：

- `ReferenceImageAnalysis`
- `ReferenceImageAnalysisService`
- `analysis.json`
- `analysis_mode=off|auto|required`

验收：

- fake Vision JSON 可解析。
- 失败策略正确。
- 分析 artifact 可用于复现。

估算：1–1.5 人天。

### PR 4：视觉上下文真实接入

交付：

- `ReferenceImageVisualContext`
- `ReferenceImageVisualContextAdapter`
- 在 `plan_visuals` 中合并到 `ip_profile` / `visual_story_context`
- `visual_context.json`
- 无 IPProfile 时 prompt-only / supplemental context 降级策略

验收：

- `ImagePromptComposer.compose(...)` 实际收到合并后的上下文。
- 有 / 无用户 IPProfile 的合并规则通过测试。
- 不覆盖用户显式 IPProfile 字段。

估算：1.5–2.5 人天。

### PR 5：workflow 能力与物理垫图

交付：

- `supports_reference_image`
- `ReferenceImageWorkflowBinding`
- workflow override 白名单
- `workflow_injection_mode=off|auto|required`
- `FrameProcessor` 按 binding 传参
- media trace 摘要化

验收：

- 支持 workflow 时传图。
- 不支持 workflow 时按模式处理。
- ambiguous 参数不误用。
- media trace 不含绝对路径、data URL 或图片二进制。

估算：2–4 人天。

### PR 6：UI/API/文档与灰度

交付：

- API upload / artifact 输入
- UI 开关
- 用户错误提示
- `config.example.yaml`
- 文档与示例
- feature flag

验收：

- 可灰度开启。
- 可一键关闭参考图功能。
- 用户能看到分析 / 垫图状态。

估算：1–2 人天。

---

## 22. 工时修正

建议区分 demo、MVP 和生产版，不要混用估算。

| 范围 | 更可信估算 |
|---|---:|
| 内部 demo：只做 messages + 简单 vision_util + `ref_image` 字段 | 1–1.5 人天 |
| MVP-A：资产化 + Vision 分析 + prompt-only 真实接入 ImagePromptComposer | 5–8 人天 |
| MVP-B：在 MVP-A 基础上白名单 workflow 物理垫图 | 2–4 人天增量 |
| 从零到 MVP-B | 7–12 人天 |
| 生产版：API/UI、capability、trace、测试、灰度完整闭环 | 10–18 人天 |

说明：

- 2.5–4 人天只适合精简 demo，不适合当前 `dev` 分支的生产级合入。
- 只要要求 trace、API 安全、workflow binding 和测试闭环，MVP-A 就不应按 1–2 天承诺。
- PR1–PR4 才构成真正的 MVP-A。

---

## 23. 发布与回滚策略

### 23.1 Feature flags

默认关闭：

```yaml
reference_image:
  enabled: false
  analysis_mode: off
  workflow_injection_mode: off
```

上线顺序：

1. 默认关闭。
2. 内部开启 `enabled=true, analysis_mode=auto, workflow_injection_mode=off`。
3. 验证 prompt-only 效果。
4. 对白名单 workflow 开启 physical injection。
5. 再逐步开放 UI。

### 23.2 回滚

必须能通过配置立即回滚：

```yaml
reference_image:
  enabled: false
```

回滚后：

- 不影响旧视频生成。
- 不影响 LLM structured output。
- 不向 workflow 传任何新增 reference image 参数。
- 已生成的旧任务 artifacts 保留，不参与新任务。

---

## 24. Definition of Done

生产级 DoD：

- [ ] 无参考图路径下所有旧测试通过。
- [ ] 参考图资产化有单测和集成测试。
- [ ] API 不接受任意服务器路径。
- [ ] Vision messages 调用成功，trace 不含 base64。
- [ ] Vision 不可用时 `auto|required` 行为正确。
- [ ] 视觉分析结果真实进入 `ImagePromptComposer` 输入。
- [ ] 无 IPProfile 时有明确 prompt-only / supplemental context 降级策略。
- [ ] workflow 不支持参考图时不会误传参数。
- [ ] required 模式下错误信息明确。
- [ ] media trace 不含图片二进制、data URL 和用户绝对路径。
- [ ] UI/API 能展示参考图分析状态与垫图状态。
- [ ] feature flag 可关闭所有新功能。
- [ ] 音视频智能时长调整无回归。

---

## 25. 原文档四项功能的覆盖式结论

### 25.1 生图提示词生成优化 / Vision

原文档的“强烈推荐借鉴”结论保留，但实现方式修正为：

```text
不是：新增 vision_util.py 自由散文描述 + 拼 prompt_prefix
而是：ReferenceImageAnalysisService + 结构化 JSON + ReferenceImageVisualContext + ImagePromptComposer
```

必须补齐：

- `LLMService messages` 支持。
- Vision 模型独立配置。
- LLM trace 脱敏。
- media trace 摘要化。
- Vision JSON 解析与 retry。
- prompt-only 降级。

### 25.2 分镜垫图逻辑 / `ref_image`

原文档的“推荐借鉴”结论保留，但实现方式修正为：

```text
不是：StoryboardConfig +1 字段 + copy2
而是：ReferenceImageAsset + task_dir 资产化 + workflow binding + injection status
```

必须补齐：

- API / CLI 输入边界。
- 图片安全校验和 EXIF 清理。
- 任务内相对路径和 hash。
- workflow 能力检测。
- 不支持时 auto 降级 / required 报错。

### 25.3 模板功能增强

保持低优先级。

当前分支已经具备更复杂的模板、HyperFrames、Layered Template 和渲染后端体系。除非业务明确需要某个视觉风格，否则不建议把移植单个 HTML 模板作为工程重点。

### 25.4 视频音频智能时长调整

保持“已实现，无需额外开发”的结论。

建议只补回归测试：

- 视频短于音频时 freeze / black padding 正常。
- 视频长于音频时 trim 正常。
- 无音频流视频合并正常。
- `duration_tolerance` 行为符合预期。

---

## 26. 风险清单

| 风险 | 等级 | 说明 | 缓解 |
|---|---:|---|---|
| trace 保存 base64 | 高 | 造成存储膨胀和隐私风险 | wire/trace payload 分离，单测阻断 |
| API 任意路径读取 | 高 | 用户可能读取服务器敏感文件 | API 只接受 upload id / artifact id |
| workflow 参数误用 | 高 | `image` 参数可能不是参考图 | 默认禁用 ambiguous 参数，使用白名单 |
| media trace 泄露绝对路径 | 高 | workflow 参数 trace 会记录路径 | 参考图参数摘要化 |
| Vision 模型不可用 | 中 | 主 LLM 多数不支持图片 | 独立 `vision_llm` 配置和 capability 检测 |
| 参考图与已有 IPProfile 冲突 | 中 | 用户角色设定可能被覆盖 | 默认 supplement，不覆盖显式字段 |
| prompt 变长导致质量波动 | 中 | prompt-only 可能污染风格 | 结构化 anchors + 字段级合并 |
| 大图导致延迟和成本上升 | 中 | Vision 图片 token / 带宽增加 | 1024px vision variant，大小限制 |
| 物理垫图效果不稳定 | 中 | 不同 workflow 支持程度不同 | 从白名单 workflow 开始 |
| 一次性改太多路径 | 中 | 容易破坏旧 structured output | 分 PR，messages 不混 structured output |
| 回滚困难 | 高 | 生成链路复杂 | feature flag 默认可关闭 |

---

## 27. 推荐最终路线

建议采用以下最终路线：

1. **先做 MVP-A：prompt-only 参考图一致性。** 这是风险最低、最不依赖 workflow 的路线。
2. **不要直接 cherry-pick 轻量上游实现。** 当前分支的 trace、IP、workflow 体系更复杂，直接照搬会形成技术债。
3. **物理垫图必须从白名单开始。** 不要一开始就做全自动参数猜测。
4. **把 trace 脱敏作为阻断项。** 只要 LLM trace 里出现 base64，就不应该合并。
5. **把 media trace 摘要化作为阻断项。** 只要 media trace 里出现图片二进制、data URL 或用户绝对路径，就不应该合并。
6. **把“是否实际垫图”暴露给用户和 metadata。** 用户需要知道这次是 prompt-only，还是 workflow 真的用了参考图。
7. **保持音视频智能时长现状。** 只做回归，不重复开发。
8. **模板移植按业务需求处理。** 不进入参考图 / Vision 主线排期。

最终目标不是“支持上传一张图”，而是：

```text
参考图驱动、可审计、可复现、可降级、可回滚的视觉一致性能力。
```

---

## 28. 附录：建议文件变更清单

### 新增文件

```text
pixelle_video/models/reference_image.py
pixelle_video/models/reference_image_analysis.py
pixelle_video/services/reference_image_asset_service.py
pixelle_video/services/reference_image_analysis.py
pixelle_video/services/reference_image_visual_context_adapter.py
pixelle_video/services/vision_capabilities.py
pixelle_video/services/workflow_reference_image_capabilities.py
```

### 修改文件

```text
pixelle_video/config/schema.py
pixelle_video/services/llm_service.py
pixelle_video/services/llm_capabilities.py
pixelle_video/models/storyboard.py
pixelle_video/pipelines/linear.py
pixelle_video/pipelines/standard.py
pixelle_video/services/frame_processor.py
pixelle_video/services/media.py
pixelle_video/services/prompt_trace_artifacts.py
pixelle_video/utils/workflow_capabilities.py
config.example.yaml
```

### 测试文件

```text
tests/models/test_reference_image_asset.py
tests/models/test_reference_image_analysis.py
tests/services/test_reference_image_asset_service.py
tests/services/test_reference_image_analysis_service.py
tests/services/test_llm_service_multimodal_trace_redaction.py
tests/services/test_vision_capabilities.py
tests/services/test_workflow_reference_image_capabilities.py
tests/services/test_reference_image_visual_context_adapter.py
tests/services/test_frame_processor_reference_image.py
tests/pipelines/test_standard_reference_image_flow.py
```

---

## 29. 附录：最小 MVP 验收用例

### 用例 A：无参考图旧流程

输入：

```python
await pixelle_video.generate_video(text="如何提高学习效率")
```

期望：

- 不生成 `reference_image/` 目录。
- 不调用 Vision。
- 不新增 workflow 参数。
- 旧流程成功。

### 用例 B：参考图 + Vision 未配置 + auto

输入：

```python
await pixelle_video.generate_video(
    text="生成一个儿童故事",
    ref_image="/local/role.png",
    reference_image_analysis_mode="auto",
)
```

期望：

- 参考图资产化成功。
- Vision 跳过。
- prompt-only 不注入或仅使用用户已有 prompt。
- 任务成功，并有 warning。

### 用例 C：参考图 + Vision 成功 + workflow 不支持

期望：

- `analysis.json` 存在。
- `visual_context.json` 存在。
- `workflow_injection_status=prompt_only`。
- `ImagePromptComposer` 输入包含参考图视觉上下文。

### 用例 D：参考图 + workflow 白名单支持

期望：

- `workflow_support.json` 显示 supported。
- `FrameProcessor` media params 包含指定参数名。
- `injection_summary.json` 显示 used。
- media trace 不保存图片二进制、data URL 或绝对路径。

### 用例 E：required 模式下 workflow 不支持

期望：

- fail-fast。
- 错误信息包含 workflow key 和“不支持参考图输入”。
- 不进入 media generation。

---

## 30. 附录：覆盖原文档后的摘要说明

本覆盖版保留了原文档对上游四项功能的分析入口，但修正了五个关键偏差：

1. 原文档更像“上游 diff 同步建议”，适合内部 demo；本覆盖版面向当前 `dev` 分支的生产级实现。
2. 原文档把 Vision 和 `ref_image` 看作两个较小改动；本覆盖版将它们纳入现有 trace、IPProfile、SeriesVisualSignature、ImagePromptComposer 和 workflow 能力系统。
3. 原文档对默认开关表达不统一；本覆盖版明确生产默认关闭。
4. 原文档的工时估算偏乐观；本覆盖版区分 demo、MVP-A、MVP-B 与生产版。
5. 原文档主要强调 LLM trace；本覆盖版补充 media workflow param trace 摘要化要求。

因此，覆盖后建议团队按本文件重新排期和拆 PR，不再以原文档的 1–1.5 人天作为生产级排期依据。
