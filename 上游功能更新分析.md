# 上游 Pixelle-Video 功能更新分析

> 分析日期：2026-06-16  
> 上游仓库：https://github.com/AIDC-AI/Pixelle-Video  
> 分析分支：`dev_latest`

---

## 概述

本文档分析上游项目 `AIDC-AI/Pixelle-Video` 的四项重要功能更新，评估其借鉴价值并提供实施建议。

---

## 1. 生图提示词生成优化 ⭐⭐⭐⭐⭐

### 状态
**强烈推荐借鉴**

### 核心改动

#### 1.1 LLMService 支持 Vision API

**文件**: `pixelle_video/services/llm_service.py`

```python
# 新增 messages 参数，支持 OpenAI Vision 格式
async def __call__(
    self,
    prompt: Optional[str] = None,        # 原有：简单文本提示
    messages: Optional[list] = None,     # 新增：支持图片分析
    # ... 其他参数
):
    # 支持两种调用方式：
    # 1. prompt: 简单文本生成
    # 2. messages: 多模态对话（含图片）
    
    # 示例：Vision 调用
    messages=[{
        "role": "user",
        "content": [
            {"type": "text", "text": "描述这张图片的风格"},
            {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,..."}}
        ]
    }]
```

#### 1.2 新增 Vision 工具模块

**文件**: `pixelle_video/utils/vision_util.py` (新增)

```python
async def analyze_ref_image(image_path: str, llm_client) -> Optional[str]:
    """
    使用 Vision API 分析参考图片，提取视觉特征
    
    分析维度：
    1. 主体（人物、物体、角色）及其外观特征
    2. 动作、姿势、表情
    3. 艺术风格（水彩、卡通、写实、水墨等）
    4. 色调和氛围
    5. 其他重要视觉元素
    
    返回：自然语言描述（约100-150字），用于指导 AI 生成相似风格图片
    """
```

### 借鉴价值

| 能力 | 说明 |
|------|------|
| 角色一致性 | 上传角色参考图，生成同风格图片 |
| 风格迁移 | 参考图风格 + 新内容描述 |
| 批量统一 | 批量生图时保持视觉风格统一 |

### 适用场景

- **系列视频制作**：保持主角形象一致
- **风格化内容**：根据参考图生成特定艺术风格
- **IP 延续**：基于已有视觉资产生成新内容

---

## 2. 分镜垫图逻辑支持 ⭐⭐⭐⭐⭐

### 状态
**强烈推荐借鉴**

### 核心改动

#### 2.1 数据模型扩展

**文件**: `pixelle_video/models/storyboard.py`

```python
@dataclass
class StoryboardConfig:
    # ... 原有字段
    
    # 新增：垫图支持
    ref_image: Optional[str] = None  # 参考图片路径，用于角色一致性
```

#### 2.2 Pipeline 支持垫图传递

**文件**: `pixelle_video/pipelines/standard.py`

```python
class StandardPipeline(BasePipeline):
    async def create(
        self,
        # ... 原有参数
        ref_image: Optional[str] = None,  # 新增参数
        # ...
    ):
        # 垫图文件管理：
        # 1. 将临时上传的垫图复制到任务目录
        # 2. 保存到 StoryboardConfig
        # 3. 后续生图时传递给 ComfyUI
        
        if ref_image:
            ref_image_src = Path(ref_image)
            if ref_image_src.exists():
                final_ref_image = Path(task_dir) / f"ref_image{ref_image_src.suffix}"
                shutil.copy2(ref_image, final_ref_image)
                config.ref_image = str(final_ref_image)
```

### 借鉴价值

| 模式 | 说明 |
|------|------|
| 简单垫图 | 直接传给 ComfyUI 的 IPAdapter 节点 |
| 智能垫图 | 先用 Vision 分析参考图，再生成提示词 |

### 适用场景

- **角色一致性**：系列视频保持主角形象一致
- **分镜连贯**：分镜之间视觉连贯
- **风格统一**：根据参考图生成相似风格的分镜图

### 与功能1的配合

```
用户上传参考图 
    ↓
Vision 分析 → 生成风格描述
    ↓
垫图 + 风格描述 → 生成分镜图
    ↓
保持角色/风格一致性
```

---

## 3. 模板功能增强 ⭐⭐⭐

### 状态
**视需求借鉴**

### 核心改动

**文件**: `templates/1080x1920/image_life_insights_light.html` (新增)

- 新增"人生感悟"风格模板
- 包含配套的预览图

### 技术特点

```html
<meta name="template:media-width" content="1024">
<meta name="template:media-height" content="1024">
<!-- 定义模板适配的素材尺寸 -->
```

### 借鉴价值

- 只是新增了一个 HTML 模板文件
- 如果你们有自己的模板体系，可以参考其 CSS 样式和布局思路
- 技术层面没有太多新东西

### 适用场景

- 需要"人生感悟"类内容风格时可直接使用
- 作为模板开发参考

---

## 4. 视频音频合并智能时长调整 ⭐⭐⭐⭐⭐

### 状态
**强烈推荐借鉴**

### 核心改动

**文件**: `pixelle_video/services/video.py`

#### 4.1 视频裁剪

```python
def _trim_video_to_duration(self, video: str, target_duration: float) -> str:
    """
    将视频裁剪到指定时长
    
    实现：
    - 使用 FFmpeg stream copy 快速裁剪
    - 保持音视频编码不变（无损裁剪）
    """
    ffmpeg.input(video, t=target_duration).output(
        output, 
        vcodec='copy', 
        acodec='copy'
    ).run()
```

#### 4.2 视频填充

```python
def _pad_video_to_duration(
    self, 
    video: str, 
    target_duration: float, 
    pad_strategy: str = "freeze"
) -> str:
    """
    将视频填充到指定时长
    
    策略：
    1. freeze (推荐): 冻结最后一帧
       - 使用 tpad filter: stop_mode='clone'
       - 自然过渡，观众无感知
       
    2. black: 黑屏填充
       - 生成黑色视频拼接
       - 适用于需要明确结束感的场景
    """
```

### 借鉴价值

| 问题 | 解决方案 |
|------|----------|
| 视频 > 音频 | 裁剪视频（stream copy 快速处理） |
| 视频 < 音频 | 填充视频（freeze/black） |

### 适用场景

- **TTS 配音**：生成的音频时长与视频素材不匹配
- **分镜对齐**：多个分镜视频时长需要精确对齐
- **音画同步**：避免音画不同步问题

### 智能合并流程

```
音频时长 = TTS 生成结果
视频时长 = 素材实际时长

if 视频时长 > 音频时长:
    裁剪视频到音频时长
elif 视频时长 < 音频时长:
    根据策略填充视频
    - freeze: 冻结最后一帧（推荐）
    - black: 黑屏填充
else:
    直接合并
```

---

## 总结与建议

### 优先级排序

| 功能 | 优先级 | 建议 |
|------|--------|------|
| 生图提示词生成优化 | ⭐⭐⭐⭐⭐ | **优先实现**，Vision API 是核心能力 |
| 分镜垫图逻辑 | ⭐⭐⭐⭐⭐ | **优先实现**，与 Vision 配合效果最佳 |
| 视频音频智能时长调整 | ⭐⭐⭐⭐⭐ | **优先实现**，解决音画同步痛点 |
| 模板功能增强 | ⭐⭐⭐ | 按需添加，只是新增模板文件 |

### 推荐实施顺序

```
第一阶段：视频音频智能时长调整
    ↓ 独立功能，改动小，立竿见影
    
第二阶段：Vision 分析 + 垫图逻辑
    ↓ 两者配合，实现角色一致性生成
    
第三阶段：模板扩展（按需）
    ↓ 根据业务需求添加新模板
```

### 技术依赖

- **功能1+2**：需要支持 Vision 的 LLM（GPT-4V、Claude 3 等）
- **功能4**：依赖 FFmpeg-python 库
- **功能3**：纯前端模板，无依赖

---

## 附录：相关 Commit

| 功能 | Commit | 作者 | 日期 |
|------|--------|------|------|
| 生图提示词生成优化 | `faa3ac3` | puke | 2025-11-21 |
| 分镜垫图逻辑支持 | `f62d534` | puke | 2025-11-21 |
| 模板功能增强 | `94e6262` | li | 2025-11-21 |
| 视频音频智能时长调整 | `02ef878` | puke | 2025-11-21 |

---

*本文档由 AI 助手分析生成，供团队参考决策。*
