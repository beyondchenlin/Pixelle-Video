# AGENTS.md

## 1. 常驻原则

- 用户在当前任务中的明确指令优先于本文件中的默认流程；安全边界与仓库保护规则除外。
- 修改代码、架构、目录、契约或行为时，必须定位并修复根因，改动范围应与根因相称；禁止堆叠临时补丁，也禁止借窄任务扩散无关重构。
- 只修改当前任务明确涉及的仓库和文件；外部示例、模板及其他仓库默认只读。
- 必须保护用户已有工作，不得擅自删除、覆盖、回滚或混入归属不明的改动。

## 2. 项目地图

- 当前项目：`Pixelle-Video`，基于本地 ComfyUI 工作流的 AI 视频创作平台。
- 项目根目录：以 `git rev-parse --show-toplevel` 的输出为准，不在仓库文件中固化机器专属绝对路径。
- 默认基线分支：`main`；默认任务来源与集成分支：`dev`。
- 任务分支默认使用 `codex/<任务名>`；需要隔离时，linked worktree 放在已忽略的 `.worktrees/` 下。
- 代理工作流初始化入口：Windows 使用 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/agent-bootstrap.ps1`，其他系统使用 `sh scripts/agent-bootstrap.sh`。
- 代理基础验证入口：Windows 使用 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/agent-verify.ps1`，其他系统使用 `sh scripts/agent-verify.sh`。
- 应用开发环境使用 `scripts/install-runtime-dependencies.ps1` 或 `scripts/install-runtime-dependencies.sh` 完整初始化；通用测试使用 `uv run pytest`；Python 静态检查使用 `uv run ruff check <改动路径>`。
- 分支流程、任务写法和项目长参考分别见 `docs/agent/branch-workflow.md`、`docs/agent/prompting-workflow.md`、`docs/agent/project-reference.md`；贡献流程见 `docs/zh/development/contributing.md`。

## 3. 安全底线

- 不提交 `.env`、私钥、令牌、凭证、敏感日志、真实配置或机器专属路径。
- 不安装来源不明的依赖；确需新增依赖时，必须说明用途、维护状态和替代方案。
- 不把临时调试输出、测试账号、一次性脚本或本地运行产物混入生产代码。
- 不用不可信输入拼接 Shell 命令、SQL 或文件路径；数据库访问使用参数化查询或项目既有安全 API。
- 不直接修改由 IDL、schema、工作流转换器或构建流程生成的文件；必须修改事实源并重新生成。
- 不使用 `git reset --hard`、强制推送、无明确目标的批量删除或其他不可恢复操作，除非用户明确授权并确认范围。

## 4. 工作边界

- 开始修改前，必须收敛单一变更意图，并检查当前分支、工作区状态和已有 worktree。
- 遵守仓库现有分支保护、PR、CI 和评审策略，不把任务实现直接提交到受保护分支。
- 并行任务、子代理编辑、长周期改动或切换 checkout 会干扰用户工作时，必须使用独立分支和独立 linked worktree；轻量单任务可按风险决定，用户明确要求时必须使用。
- 创建 worktree 前运行 `git worktree list`，并从将接收合并的集成分支创建任务分支；进入后重新读取本文件，并优先运行仓库已有初始化脚本。
- 发现无关问题时只记录并说明，不顺手修改；必须触碰用户已有改动时，先理解其意图并在其基础上继续。
- 发送外部消息、发布内容、推送远程、合入共享分支、部署或修改他人依赖的信息属于共享副作用，必须取得用户在当前任务中的明确授权。

## 5. 执行方式与分层

- 从能够满足质量与风险控制的最轻流程开始；范围、风险或不确定性升高时，再升级为计划、专项评审或多技能流程。
- 只读任务直接分析并给出有证据的结论；故障排查先复现、定位根因并对比现有可用模式，再实施修复。
- 视觉锚点的内容、融合与前置审查阶段，每个分镜各只允许一次大模型调用；任一输出解析、合同校验或审查失败时立即终止，禁止自动重试、修复性重试或携带反馈再次调用。无歧义格式兼容只能在单次响应的解析边界完成。
- 并行或子代理只在用户明确要求，且子任务互不影响、边界清晰、可独立验证时使用；同一文件、共享契约、根配置、CI、锁文件或根因未明的调试任务默认串行。
- 搜索仓库内容优先使用 `rg` 或 `rg --files`。
- 只有每次会话都必须知道的高频硬规则才保留在本文件；路径专属约束放到相应目录的局部规则文件。
- 多步判断流程与专题清单沉淀为 skill 或 `docs/agent/` 文档；任务 Prompt 的按需写法以 `docs/agent/prompting-workflow.md` 为准，不强制用户套固定模板。
- 可机械判断的检查放到 `scripts/`，必须在生命周期节点执行的确定性检查由 Git hooks 调用脚本完成。
- hooks 必须保持只读、快速和确定性，不调用 LLM，不下载或同步依赖，不创建虚拟环境，不承载复杂判断；长链接、背景资料和长示例放到 `docs/`，不常驻本文件。

## 6. 模型与大体积制品

- 下载 AI 模型、ComfyUI 模型资源及其他大体积制品时，优先使用 `ModelScope`；只有该平台缺少目标文件或实际不可用时，才回退到 `Hugging Face` 等其他来源，并记录回退原因。
- 开始下载前必须确认工作流加载器类型、目标子目录和可用磁盘空间；多个独立文件在带宽与磁盘允许时优先并行下载。
- 下载完成后必须验证文件存在、大小合理且加载路径正确；验证通过后才能报告成功。
- 不得仅因找到同名文件就判定模型可用；还必须核对格式、精度、加载器兼容性和工作流引用名称。
- `.safetensors`、`.ckpt`、`.gguf`、`.pt`、`.pth`、`.onnx`、`.bin`、`.engine` 等本地模型制品不得提交到仓库。

## 7. 工作流依赖文档

- `workflows/down/` 按“一工作流一文档”维护依赖、下载、安装与验证说明，文件名使用 `<workflow_name>_依赖与下载说明.md`，文件名、标题、正文和命令注释默认使用中文。
- 每份说明至少包含：工作流路径、节点与依赖清单、依赖分类、目标目录、下载优先级、`ModelScope` 检索或主地址、备用地址、安装命令、验证命令和常见问题。
- 补充说明前必须先检查 `ModelScope`；若无对应资源或实际不可用，必须在文档中记录后再提供回退来源。
- 插件、Python 包和系统工具同样必须写明来源、用途与安装方式；文档中的每个下载项、安装项和备用方案都要实际验证后才能标记成功。
- 新增 `selfhost` 或其他本地工作流并引入新依赖时，必须同步更新对应中文说明文档。

## 8. Git 与发布边界

- 每次提交只包含一个独立、完整、可单独理解和回滚的变更意图；暂存时显式限定任务文件，不得夹带无关改动。
- 提交说明使用分类标签前缀，格式为 `<类型>: <标题>`，标题必须至少包含中文；允许类型包括 `feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`chore`、`build`、`ci`、`revert`、`merge`，复杂提交使用中文正文列出主要改动。
- 原子变更通过匹配验证后直接提交，不再询问是否提交；只有用户在当前任务中明确授权远程同步时才推送任务分支，推送失败必须说明真实阻塞原因。
- 合入 `dev`、`main` 或其他共享分支必须取得用户明确同意，并在合入前同步目标分支、复跑匹配验证；禁止强制推送。
- 发布、部署、`ship`、`land-and-deploy` 等动作必须由用户明确授权，不能仅凭代码完成自动执行。
- 仓库存在 Git hooks 时不得绕过；新增或修改钩子时，钩子只负责调用可独立运行的校验脚本。

## 9. 验证与完成标准

- 声称完成、提交、推送或同步前，必须运行与改动范围和风险匹配的验证，并如实报告命令与结果。
- 提交前先运行 `scripts/agent-verify.ps1` 或 `scripts/agent-verify.sh`；文档改动还要核对 UTF-8 编码、链接、命令和路径，代码改动还要运行相关测试与受影响路径的静态检查。
- 修改工作流、加载器或依赖说明时，还必须执行文档中列出的实际可用性验证，不得以静态检查代替运行验证。
- 无法执行某项验证时，必须说明原因、未覆盖范围和剩余风险，不能虚构测试、截图或命令输出。
