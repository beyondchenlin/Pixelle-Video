# 视频生产改进实施记录

日期：2026-09-05。对应[产品需求文档](../specs/2026-09-05-文案与视觉锚点视频生产改进产品需求文档.md)。

用户确认的八项工程改进已按顺序实现，各项独立本地提交。开发分支基线为 `939881be`，实现保存在独立任务分支 `codex/visual-production-improvements`（视觉生产改进分支），尚未合入共享分支、推送或部署。

## 一、八项执行结果

| 顺序 | 改进及实际行为 | 实现提交 |
|---|---|---|
| 1 | 世界设定、镜头意图及逐镜覆盖独立传入三阶段；内容阶段继续隔离系列身份外观。最终返回原文直接用于生图。 | `ed686c2f` |
| 2 | 文案调用承接已有用户创作要求；按题材组织事件、动作、证据和结果，移除固定七段式及无依据的成功保证。用户定稿保持原文。 | `a2db3e7f` |
| 3 | 前端新增“按信息设计镜头（推荐）”并设为默认，接口默认同步。利用已有字段表达信息推进与可见证据；该模式规划失败直接报错，不自动修复或退回按句拆分。 | `51e781d6` |
| 4 | 移除六类形态的序号轮换，由调用前提示要求按场景选形态。程序只依据明确场景编号连接连续场景，共享人物名称不再直接触发场景合并。 | `c2b580e2` |
| 5 | 身份本来就是原文主体时保留动作与主体位置；其他身份作为次级信息。取消统一中背景、小尺寸和遮挡规则，同时保护识别特征。 | `50259d65` |
| 6 | 已修复最终审查的职责边界：第三次调用依据原文修正内容草稿和融合草稿中的主体、数量、载体工艺、风格冲突与解释性语言。修复要求全部写在调用前，不能把错误草稿当作更高事实依据。 | `648774f5` |
| 7 | 增加“必须使用参考图”选项；所选工作流不具备对应条件时生成前报错，不静默退回文字模式。历史详情展示实际身份生成条件。 | `a5aa8ac5` |
| 8 | 历史详情增加成片人工验收，覆盖内容、身份、连续性、信息推进、节奏、字幕、声音和同步。锚点任务必须先完成全部逐图验收，才能标记成片通过；结论绑定视频、分镜和逐图记录摘要，产物变化后失效。单镜超过八秒提示人工查看。 | `ea24f8f1` |

第六项是第三阶段现有一次调用中的修复职责，不是生成结束后追加审查。整个视觉锚点链路仍为每镜三次文字模型调用，各阶段原始文本响应，最终原文直传；没有本地创作内容解析、自动重试、自动审图或重生成。

## 二、兼容性补充

提示词升级同时将生成请求升为第二十版，保留第十九版及更早版本读取能力，避免旧任务被新模板版本校验拒绝。补充提交为 `257231c3`。正式请求字段测试同步新增参考图要求，提交为 `e68813ff`。

读取既有任务 `d8530d6f-8d85-4174-9bb4-fa0e8aab1b78` 的十六镜记录，全部通过当前模型载入。该检查只验证历史数据兼容，不重新生成或改写历史产物。

## 三、已完成验证

- 相关回归测试共 596 项通过：主回归集合 589 项，加正式请求契约 7 项。覆盖文案、分镜、默认选项、三阶段投递、调用边界、参考条件、历史读取、逐图与成片验收。新增字段导致的一处旧契约预期已修正并复测。
- 全部受影响的源代码和测试文件通过静态检查；各次提交均执行仓库基础验证和提交钩子。
- 前端组件运行测试确认新选项默认选中，手动切换后保持选择。成片验收八项初始均未勾选，不会自动写入通过结论；模拟人工提交后记录及页面状态正确。
- 禁用测试缓存插件产生一项缓存配置警告，不影响测试通过。自动测试使用替身和测试文件，没有调用真实生成模型。

复核命令使用项目已初始化的运行环境：`python -m pytest`（运行测试）执行对应测试文件，`ruff check`（静态检查）覆盖变更的代码文件；提交前执行 `scripts/agent-verify.ps1`（仓库基础验证脚本）。

## 四、尚未验证的交付效果

最初工程验收没有生成真实原图或视频。后续第三次网页实测已生成完整视频，并通过文件解码和画面抽查；尚未完成完整播放听音及人工验收。因此不能据此报告首次可用率、身份一致性改善幅度、画面材质正确率或成片达到量产标准。实际执行结果见第五至七节。

需求文档中的四版本对照、四十八张样本基线、两名审核人评分属于后续真实质量验证，尚未执行。更细的受众资料输入控件、生成前镜头预计时长展示，以及完整的创作质量统计也不属于本次八项工程交付；现阶段承接已有创作要求，并在历史详情展示实际镜头时长及验收状态。

真实验收使用固定原稿、固定身份、固定画风与工作流，分别记录文字条件和参考图条件的首次产物。逐图检查后完整观看并听音，在历史详情人工填写成片结论；不通过时保留失败证据，修改输入后由用户主动新建任务。

## 五、首次网页生成实测：执行失败

用户要求从网页输入“马斯克的未来”，启用身份与视觉锚点，其余保持默认。已使用本任务分支启动网页，通过页面控件填写并点击生成；没有通过独立接口调用提交生成。

- 任务编号：`d5ca8c96-c152-4f49-a680-ae8a5f255a38`。
- 页面默认身份：戴黑色墨镜的斑点狗；默认按信息设计镜头；文案长度和镜头数量自动；横屏 1280×720。
- 实际完成：文案、六个分镜、六镜三阶段提示词、旁白生成与六段配音切分。原始旁白音频约 66.84 秒。
- 实际失败：默认量化生图工作流在第一镜加载模型期间发生 Windows（视窗系统）内存访问崩溃；图片数为零，最终视频不存在，任务状态为失败。
- 应用记录的连接拒绝是后端进程崩溃后的表现。后端堆栈定位到量化插件卸载模型时的张量内存迁移，与[插件上游第 445 号待合并修复](https://github.com/city96/ComfyUI-GGUF/pull/445)描述的位置一致。上游补丁未在本机应用或验证，不能据此宣称根因已修复。
- 本次生成的文案仍包含泛化评价和泛化励志收尾，不能认定文案质量要求已通过真实验收。输入投递和模板测试通过不等于模型创作结果通过。

完整失败证据位于该任务的输出目录及本任务工作区的生图后端日志中，保留原文和配音。遵守禁止失败后自动追加调用的规则，没有自动重跑、替换模型或改写生成内容。下一步需要修复并验证本地生图运行环境，再由用户明确发起新的网页生成任务。

## 六、用户授权修复后的第二次网页实测：一张出图，后续内存不足

用户明确同意修复默认生图环境后从网页新建任务。已增加定向修复脚本和三项回归测试，提交为 `5f5868f7`。该脚本依据上述上游修复的最新版本，只替换匹配的量化插件卸载函数，保留既有定制并备份原文件；不修改工作流、模型或前端默认值。

- 实际环境验证：默认量化图片模型在真实显卡完成加载与卸载，检查进程正常退出；修复脚本三项测试和静态检查通过。
- 新任务编号：`92ef3099-4070-402d-9d94-b31d062fe47b`。从网页输入“马斯克的未来”，启用默认戴黑色墨镜的斑点狗视觉签名；镜头方式仍为按信息设计镜头，长度、数量及其他设置保持默认。
- 实际完成：五镜规划、对应三阶段提示词、约 59.01 秒完整配音及五段切分、第一张原图。原图保存在任务目录 `frames/01_image.png`。
- 实际失败：第二镜完成八步采样后，图片解码输出复制到系统内存时分配约 10.55 兆字节失败。第一镜正常出图，后端日志没有再次出现原先量化模型卸载的内存访问崩溃；不能因此认定所有内存问题已经解决。
- 本机资源检查：物理内存约 31.82 吉字节，分页文件约 20.12 吉字节；运行中的容器虚拟机进程占用约 14.82 吉字节私有内存，涉及其他项目的 21 个容器服务。这是实际观察到的内存占用，尚未通过关闭服务后的对照运行确认其对失败的贡献。
- 最终视频尚未生成。本次没有自动追加创作调用、降低默认分辨率、切换模型或关闭其他项目服务。已向用户询问是否允许暂时停止这些服务并关闭容器虚拟机，然后从网页新建任务。

第二次失败没有覆盖首次失败记录。工程修复、真实出图和完整成片分别记录；当前不能报告视频交付完成或量产验收通过。

## 七、释放容器虚拟机内存后的第三次网页实测：完整视频生成成功

用户明确同意暂停其他项目的容器服务、关闭容器虚拟机并重新从网页生成。已保存原先运行的 21 个容器清单，逐个停止后关闭虚拟机；提交任务前系统可用提交内存约 26.27 吉字节。保留前述插件修复，不修改模型、分辨率、工作流或前端默认值。

- 任务编号：`4e377b7a-802a-443e-9fda-685cf09a2f23`。仍通过网页填写“马斯克的未来”，启用默认戴黑色墨镜的斑点狗视觉签名；按信息设计镜头，其他设置默认。
- 实际完成：六镜文案和三阶段提示词、六张原图、六段配音及字幕、六张合成图和最终视频。网页显示成功，生成耗时约 380.7 秒。
- 最终文件：任务输出目录下的 `final.mp4`（最终视频）；74.766667 秒，1280×720，每秒 30 帧，大小 3,139,027 字节。视频编码为 H.264（视频压缩编码），音频编码为 AAC（高级音频编码）。
- 完整性验证：使用 `ffprobe`（媒体信息检查工具）确认音视频轨道、时长和尺寸；使用 `ffmpeg`（音视频处理工具）将完整文件解码到空输出，退出码为零且没有错误。网页成功状态和成片预览已实际查看。
- 文件 SHA-256（安全散列摘要）：`bd6fdc56ffd64dc66eda603c9d24d26900ef4be0f281db7a27c11fb118567b83`。
- 质量边界：视频画面抽查可见主题人物、汽车、火箭和墨镜斑点狗，字幕已烧录。六镜时长分别为 8.24、10.88、8.48、11.20、10.56、25.39 秒；最后一镜停留过长，文案仍含泛化评价与励志收尾。此次交付的是默认设置生成的真实成片，没有自动重生成或手动改写成片来掩盖上述问题，也未写入人工验收通过结论。

本次成功证明释放系统资源后，当前默认流程可以走完真实出图和视频合成。它不等于长期并发稳定性验证，后续仍需独立改进镜头节奏、文案信息密度及完整人工听音验收。

恢复原有容器时，21 个中 20 个已运行；原有缓存容器因本地 6380 端口被另一项工作在本次生成期间新建的缓存容器占用，启动失败。已核对新容器的创建时间，未停止这项新工作；已向用户询问保留新容器还是恢复原服务。该冲突不影响已生成的视频文件。

## 八、用户成片反馈：斑点狗位置不合理，质量验收未通过

用户观看第三次成片后明确反馈效果不如以前、斑点狗位置非常不合理。端口问题由用户自行调整，本任务不再操作其他容器。

复核本次六镜最终提示词、三阶段原始记录、第五镜原图，并对照历史“乔布斯的一生”第三镜：历史样本的狗图形附着在发布台正面，当前第五镜却为发射现场增加了一块空教学板，狗完整坐在板外。历史样本另有重复人物问题，这里只比较身份与载体的关系，不宣称历史整片合格。

定位到两个本次实现引入的退步：

1. **默认预设泄漏。** 网页未启用高级分镜规划，世界预设为空；新增世界上下文投递却无条件查询预设库，库函数为空参数返回默认教室。实际第一镜内容阶段输入出现教学板、教室或实验室等场景要求；六镜最终文本均使用教学板。这是输入构造错误，不能交给图片模型承担修复。
2. **次级身份的位置约束被过度放宽。** 第五项改进为保护原文主体，将前景、中心和主角身旁不视为失败的表述推广到整个身份规则。六镜最终文本反复使用教学板角落和完整动物的描述，缺少图形与板面边界的有效关系；第五镜实图表现为独立实体，破坏场景。最终阶段已有平面语法规则未被模型落实，不能仅凭规则存在认定问题解决。

修正：未选择预设时不注入预设库默认世界，显式预设和用户世界提示继续投递；融合与最终审查按原文职责限制额外身份的并列站位和独立留白，禁止为身份把内容场景改成教学板；表面标记必须保留载体四周底色、跟随透视，避免贴住底边或地面形成落地轮廓，以保留识别特征的局部形状和相对尺度代替泛化的“角落有一只”。这些要求只在原有调用前提示词中表达，不增加创作字段或本地输出判定。

提示词升为融合第 51 版、最终审查第 32 版；请求第 21 版保留第 20 版读取。相关 120 项测试通过，受影响代码静态检查通过；实际六镜旧请求均通过当前模型读取。测试覆盖未选预设无教室输入、明确选教室仍生效、用户世界提示保留、三阶段直传和历史链路兼容；模板检查不能证明生成画面已经改善。

本次没有追加生图、修改旧成片或自动重生成；新版实际摆放效果尚未验证。用户已否定的旧成片保留为回归证据，不再作为质量通过样本。

## 九、按用户纠正收敛为通用融合机制

用户明确指出需要通用方案，而不是修复某一个斑点狗样本。第八节的故障证据继续保留，但其针对教学板、固定留边、局部轮廓及位置禁令的修正方向由本节取代。

产品方案已写入需求文档第十一节：内容先成立，身份职责与存在类型分开，按场景选择实体、表面或界面形态，从接触或承载关系推导位置，再兼顾可辨认程度、视觉主次和真实连续性。任何身份均不绑定方位、比例、载体名称或固定轮换；完整图形与局部图形按识别需要选择。无默认预设时不注入世界设定的代码修复继续保留。

融合与最终审查提示词删除前次案例特化的教学板禁令、统一保留底色、统一局部轮廓和按绝对方位否决的规则，统一使用场景用途、材质工艺、承载几何、身份职责和视觉竞争判断。平面完整人物或动物图形不再因拥有完整轮廓而被否决，判断其是否被错误描述成脱离载体的实体；浅浮雕与曲面不再强制按平板共面关系处理。

提示词升级为融合第 52 版、最终审查第 33 版；请求第 22 版保留第 21 版读取。没有新增创作输出字段、本地内容判定或模型调用，也没有修改模型、工作流、身份档案、成片和其他项目容器。

验证：相关 127 项测试通过，静态检查通过。新增六类身份输入检查覆盖人物、动物、植物、功能物品、标志图形和抽象符号，确认使用同一三阶段路径，身份输入隔离和特征投递正确，最终原文保持不变。历史第 19、20、21 版完整链路读取检查通过。上述为工程契约检查，不能证明真实生成质量；需求文档中的十八种场景样本尚未生成或人工验收。

## 十、跨历史样本复核与本轮真实网页生成

用户要求跳出单个身份和方位补丁，比较过去的提示词和三次交互，最终以实际结果判断。已查看四组历史画面拼图和对应三阶段原文，覆盖融合第 40、44、45、46 版。历史任务 `5c4d892c-8954-4248-b5fb-bc666ae57081` 的设计纸面等样本具有实际内容用途；其他旧样本也有重复人物、独立动物等问题，不能把旧版整体判为合格。

这轮先将融合升为第 53 版、最终审查升为第 34 版、请求升为第 23 版：完整实体由原文职责决定，额外身份附着在当前内容需要的物品上；去掉身份后，载体的位置、大小、姿态仍应成立；身份写在载体句中，最终阶段重新组织完整画面。相关 128 项检查通过。

### 真实生成结果：失败，未交付新成片

- 用户允许暂停另一项目的诊断进程；已暂停对应工作进程。该诊断程序期间又重新启动，再次按同一授权停止。本轮没有停止容器服务或修改端口。
- 重启本项目页面后，通过网页输入“马斯克的未来”，启用默认墨镜斑点狗视觉签名，其余保持默认。任务编号 `8c65f035-dd9f-4de8-bbb0-b9f3d4e585a9`。
- 完成六镜文案、18 次视觉阶段调用、约 70.88 秒整段旁白及六段切分；视觉调用日志记录重试数为零。
- 第一镜图片请求在量化模型卸载时申请 88,473,600 字节系统内存失败；19:18:05 生图后端记录内存错误。检查时系统可用提交内存仅约 77 兆字节。任务目录没有原图和最终视频。资源不足发生于正常流程，不能伪装为质量对照成图。
- 文本质量同样未通过：第二镜书籍封面、第四镜纸质备忘录、第五镜电脑屏幕、第六镜演讲屏幕均继续使用刻线工艺；融合与最终原文未有效纠正这些冲突。文案也偏向人物成功经历与励志收尾，没有充分展开未来主题。

### 继续定位并修正输入冲突

服务代码向身份档案额外注入“优先附着，除非当前内容允许原生实体”的默认创作指令；这与调用前提示词收紧为原文必须需要实体的规则不一致。独立场景还会接收上一镜完整最终提示词，提供了复制上一镜载体和工艺的文本来源。本轮六镜反复沿用刻线的现象与此输入问题并存，尚无隔离生成结果证明各自贡献，不能把两处修正直接等同于质量修复。

已删除代码注入的创作指令和独立镜头的历史全文投递，保留用户身份档案、当前原文、当前两阶段草稿和真实连续场景承载关系。历史全文继续存档，旧版本仍可读取。融合升为第 54 版、最终审查升为第 35 版、请求升为第 24 版；未新增解析、审图、反馈调用或自动重生成。同步修正网页仍声称二阶段和本地确定性创作校验的旧说明。

验证：相关 129 项检查通过，受影响代码静态检查通过。新增行为检查确认独立场景后续调用没有前镜原文，连续场景仍且只携带一次既有原文，代码不再注入默认创作指令；第 23 版完整链路兼容检查通过。测试缓存插件被禁用产生一项配置警告，不影响检查结果。最新规则没有再次调用真实模型；新版成片与通用质量验收仍未完成。

## 十一、继续优化测试：模型驻留对照与完整网页实测

本轮没有继续堆叠视觉提示词，先保持融合第 54 版、最终第 35 版，定位阻断真实出图的资源问题。

### 两模型加载实验

使用本机已安装的量化文字编码模型与图片模型，依次执行加载、重复使用、卸载，不生成创作内容。默认模式、显卡驻留模式、仅关闭动态显存管理三个独立进程均正常结束。每秒读取进程私有内存，采样峰值分别约 29.1、17.5、29.1 吉字节。这个实验验证了模型驻留策略对本机两模型流程的内存影响，没有证明所有完整工作流都能装入显存。

新增后端配置 `vram_mode`（显存模式），默认 `normal`（正常卸载）；经过容量实测的单个后端可设置 `high`（显卡驻留）。配置经管理服务传递至启动、检查、停止脚本，参与托管进程身份校验。参数不匹配或同时包含两种模式的进程不得被当作当前托管实例。批次结束仍以完整后端进程退出释放资源，无失败后自动切换。仅本任务忽略的本地配置启用图片后端显卡驻留，语音和所有前端生成设置保持原值。

另按[内核说明](https://www.kernel.org/doc/html/latest/admin-guide/sysctl/vm.html?highlight=vfs_cache_pressure)及[微软内存回收说明](https://devblogs.microsoft.com/commandline/memory-reclaim-in-the-windows-subsystem-for-linux-2/)释放已落盘文件缓存并整理空闲内存，容器虚拟机私有内存由约 15.9 降至约 10.3 吉字节。没有停止容器、修改端口、调整永久系统设置或删除文件。

### 网页实测仍失败

- 从网页填写“马斯克的未来”，启用默认视觉签名，其余前端选项默认。任务编号 `25d6aeba-9a98-42f4-9aee-6095f9fd20b5`。
- 本次自动规划实际为四镜，完成 12 次视觉调用、约 60.60 秒旁白与四段配音；视觉调用日志重试数为零。
- 后端真实启动参数已确认显卡驻留生效。第一镜加载时出现设备分配错误，随后清理阶段申请 777,912,320 字节系统内存失败；没有原图和最终视频。设备报错同时报告约 15.76 吉字节显存可用，不能把错误文字直接等同于显卡物理容量耗尽。
- 另一项目诊断在本轮期间多次重新启动，最后通过隔离父进程及子进程执行同一诊断工作，观察到子进程约 10.4 吉字节私有内存。已按用户授权暂停已确认的诊断父子进程；已请求用户暂停持续拉起这些进程的诊断任务，以建立可重复测试的资源条件。本轮资源竞争与运行失败同时存在，未完成无竞争完整成片对照。
- 提示词检查：四镜分别使用导航界面、旅行者手机、野营视频通话、宇航服织物；不再全片继承刻线。但第二镜仍把手机屏幕写成刺绣，最终审查未修正；第一与第三镜的界面细节能否在目标尺寸下自然可辨，也没有成图证据。因此本轮没有标记视觉质量通过。

工程验证：后端管理及脚本整组 97 项检查通过；随后扩展进程身份测试，正常模式和驻留模式各自拒绝错误或冲突参数，两项针对检查通过。受影响代码静态检查通过。测试缓存插件禁用带来一项配置警告。真实生成失败记录保留，未覆盖旧视频或自动再次生成。

## 十二、暂停诊断任务后的完整网页结果与复核记录

用户明确要求由本任务暂停持续拉起的诊断任务。已联系对应任务，其确认诊断父子进程均停止并停止自动重启；本次没有停止容器或修改端口。在此条件下从网页重新填写“马斯克的未来”，启用默认视觉签名，其余前端选项默认，保持融合第 54 版、最终第 35 版及图片后端显卡驻留配置。

- 任务编号：`6a73b59d-5229-4ec5-b499-3dd133194b65`。
- 网页显示生成成功，耗时 290.5 秒；完成四镜文案、四张原图、四张合成图、旁白及最终视频。每镜三阶段各调用一次，共 12 次视觉调用，重试数均为零。
- 最终视频位于该任务输出目录的 `final.mp4`（最终视频）：62.18 秒、1280×720、每秒 30 帧、2,575,981 字节；包含 H.264（视频压缩编码）画面和 AAC（高级音频编码）音轨。
- `ffprobe`（媒体信息检查工具）读取成功；`ffmpeg`（音视频处理工具）完整解码到空输出，退出码为零且没有错误。SHA-256（安全散列摘要）为 `e9b65ae08d71bc851bd21f29667fa7e003a4d170ef591fe94a128df2fe581770`。
- 后端按批次退出，确认生图和配音端口均无监听；已通知对应诊断任务本次模型资源释放。另一任务随后告知按其用户的后续授权恢复验收，本任务保持模型关闭。

代理查看全部四张原图及对应三阶段原文，作出下列严格复核记录。这里记录代理对承载、特征与提示词执行的判断，不代表用户的观感结论，也不是应用内自动创作判定或重生成逻辑。用户后续反馈见第十三节。

| 镜头 | 原始提示词与真实画面 | 验收 |
|---|---|---|
| 人物与未来布局 | 内容阶段已有站立人物、企业标志和概念图；融合阶段在全文末尾新增墙面印刷装饰，最终阶段保留。实图的狗头位于标志上方，呈现独立展示物的效果 | 不通过：载体为身份新增，视觉焦点竞争 |
| 卫星网络与远程服务 | 最终文本把平板背面写成刺绣；实图把身份画到孩子衣服背部 | 不通过：实际衣服承载相对自然，但没有实现指定物体，材质错误未被审查修正 |
| 脑机接口 | 最终文本把屏幕图标写成刻线，且没有保留墨镜特征；实图医生向外展示的平板由狗占据 | 不通过：物证被替换，固定特征丢失，展示姿态服务身份 |
| 实验室研究 | 最终文本要求数据屏幕一角的身份图标；实图电脑主要显示大幅狗图案 | 不通过：次级身份占据内容设备的信息区域 |

本次证明当前本机在诊断任务暂停、图片模型驻留显卡的条件下能完成一次默认网页流程；没有隔离两项条件各自贡献，也不代表并发稳定性通过。实际输入包含现有融合与审查规则，但融合继续采用全文加装饰、最终继续保留错误草稿；因此不能把“提示词有规则”或“代码检查通过”当作融合修复完成。

另核对真实渲染计划：四镜来源均为合成静态图片，元素动画关闭，字幕独立合成。此前把逐笔书写动画列为本项目交付缺口，用户已明确纠正该需求属于另一个项目；本项目不以该项验收。完整听音验收尚未执行。本次没有修改原图或成片，也没有追加质量反馈调用或自动重新生成。

## 十三、用户确认范围与当前效果，保留有效基线

用户明确：本项目只实现角色形象与视觉锚点融入画面产品，逐笔绘画和书写属于另一个项目。用户观看本轮视频后反馈“效果还可以，基本上没有什么太大的违和感”。这属于用户对本片整体自然程度的正向评价，不等同于十八种通用样本全部通过。第十二节保留的代理严格复核不得覆盖这项用户反馈，也不应把提示词与成图的每一处差异都当成观感失败。

当前四镜成片作为后续比较基线，保留三阶段原文直传和现有生成设置。改进优先级为：固定识别特征不丢失；承载物继续表达原有内容，身份不占据主要信息区域；提示词中的工艺与实际材质相容。第二镜服装上的标记是现有自然承载的参考，但不将服装、徽章、屏幕或任何方位设为通用固定答案。后续再用不同主题和身份验证稳定性。

本次只修正文档范围和评价记录，没有更改生成代码、提示词、配置或成片，没有启动新生成任务。

## 十四、按用户澄清修正最高层目标：以融合后的整幅画判断

用户进一步澄清，核心是让角色形象或视觉锚点找到合理位置，融入后视觉协调、不违和；“去掉它，主体表达仍完整”不是核心目标或验收硬条件。本节取代此前关于载体删除检验、原有位置大小姿态不变的约束，既有生成结果和用户对本片的认可继续保留。

已直接修改融合与最终审查调用前的提示词。两阶段共同以融合后的整幅画为判断对象，原文事实、世界规则和固定身份特征作为边界；允许共同设计景别、视角、物体布局与承载方式。合理装饰、环境归属和视觉呼应可以建立融合关系，不要求身份直接参与文案事件或不可替代。现有载体和新增设计细节采用同一标准：动作与安装接触可信、材质和透视相容、主体与身份视觉层级协调。

对应删除“同样的位置、大小和姿态”硬门槛、必须先耗尽现有载体才能调整构图的限制，以及仅因变化或装饰便判错的条款。仍保留原文主体与额外身份的职责边界、核心物证保护、单实例和真实场景连续性，不把装饰许可扩展为新增剧情角色。最终文本用具体成像关系表达融合效果，不只写“自然融入”等评价。

另根据已核实的第三镜输入与原文：身份档案包含黑色墨镜，融合和最终文本都遗漏。两阶段输出要求明确将档案中的可见核心特征写入身份所在的同一句描述，最终阶段在其唯一调用内恢复草稿遗漏；没有按某种动物写特例，也没有增加本地检查、补词或调用。

融合升为第 55 版、最终审查升为第 36 版、请求升为第 25 版。第 24 版请求与第 54／35 版完整链路保留读取。三阶段服务、人物故事回归和首次生成审计三组共 117 项检查通过，受影响代码静态检查通过；测试缓存插件禁用产生一项配置警告。新增历史读取覆盖及现有模板检查确认移除旧门槛，原文直传与每阶段一次调用检查继续通过。

本次没有生成新版图片或视频；上述验证证明代码契约和提示词投递正确，不能证明第 55／36 版真实视觉效果已改善。后续真实对照仍使用已认可的四镜成片作为基线。

## 十五、第 55／36 版网页验证：首轮资源失败，协调后生成五镜成片

用户要求再次生成视频验证。两次均通过网页填写“马斯克的未来”、开启默认墨镜斑点狗视觉签名，其余前端设置默认，保持原有图片模型、工作流和分辨率。没有修改提示词或把失败反馈交给创作调用。

首个任务 `9e4a8ce7-70a6-4bed-83fa-a798139d4440` 完成六镜提示词、约 69.45 秒配音和第一张原图，第二张图片解码时申请 11,059,200 字节系统内存失败；随后托管清理出现页面文件不足错误，后端最终退出。该任务没有最终视频，失败记录与现有资产均保留。检查发现另一项目分割服务在测试期间运行，约占 3.35 吉字节私有内存；相关任务随后确认重新启动过该服务并主动停止。运行失败与资源竞争同时存在，不能据此认定提示词有问题或完全归因于单个进程。

网页没有直接续跑该失败任务的入口。释放已落盘缓存、重启本项目网页，并收到诊断与分割任务暂停且不再拉起的确认后，进行一次独立网页测试：`6a4a66b3-1636-4421-97a1-04d214527ebe`。网页显示成功，耗时 266.9 秒，实际生成五镜文案、五张原图、五张合成图、旁白字幕和完整视频。每镜三个阶段各一次，共 15 次视觉调用，重试数为零；真实请求记录确认融合第 55 版、最终第 36 版、请求第 25 版生效。

最终视频位于成功任务目录的 `final.mp4`（最终视频），时长 63.133008 秒，1280×720，每秒 30 帧，2,738,361 字节，包含 H.264（视频压缩编码）和 AAC（高级音频编码）轨道。媒体探测成功，完整解码退出码为零且没有错误。已打开网页预览。生图与配音后端均退出，确认对应端口无监听，已同时通知另两项任务释放资源。

人工复核全部五张原图：五镜都保留墨镜和斑点狗识别特征。第一镜笔记本屏幕、第二镜桌边垂布、第三镜控制屏、第四镜椅背和第五镜书册封面均形成可见承载关系。第四镜椅背图案与人物研究图纸的场景结合最自然；第二镜提示词明确是桌边垂布，不能因白色线稿外观而误判为悬空纸张。第三镜控制屏图案的视觉分量仍偏强。第五镜的写实人物照片破坏统一线稿风格，属于另一个画面质量问题。

本轮不以载体新增、装饰用途或未参与文案事件直接判失败。新增的合理承载关系已得到实图验证，但不能据单片认定通用稳定性通过；自动文案、分镜和种子没有与旧片逐项固定，不能把所有观感变化都归因于提示词版本。完整听音验收尚未执行。没有修改成片、隐藏失败试次或根据成图追加自动审查与重生成。
