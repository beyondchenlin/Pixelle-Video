# 分层模板预览工作台剩余功能交接实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成标题、字幕、图片、背景、模板库、即时预览和最终生成链路的统一分层模板能力。

**Architecture:** `LayeredTemplateSpec` 是模板排版唯一事实源；Streamlit 只负责编辑状态和提交 normalized spec；即时预览、模板保存、真实预览帧、HTML screenshot、HyperFrames compiled、ffmpeg manifest 都从同一份 spec 派生。禁止把分层排版继续塞进 `template_params` 或散落在 session/UI/adapter 中。

**Tech Stack:** Python 3.11, dataclasses, Pydantic/FastAPI, pytest, Streamlit, Playwright HTML capture, HyperFrames, ffmpeg.

---

## 1. 当前基线

当前目标分支应为 `dev`。接手前先执行 `git fetch origin` 和 `git status --short --branch`，以最新 `origin/dev` 为准开展工作。

2026-05-03 核对结果：本地 `dev` 与 `origin/dev` 一致；修订前 `origin/dev` 最新提交为 `ebd30f3 docs: 增加分镜工作台客户端边界实施计划`。`dev` 是当前交接清单的默认开发主线，但不是“包含所有远端分支提交”的全集分支；`origin/main` 以及 `origin/feature/layered-template-workbench`、`origin/feature/layered-template-integration`、`origin/feature/layered-template-preset-registry`、`origin/feature/layered-template-preview-api` 仍有未合入 `origin/dev` 的提交。后续若要采用这些分支内容，必须先单独审查并按原子提交方式合入。

与本功能直接相关的早期源码基线来自 `efcc340 fix: 统一标题字幕样式源头契约`；之后 `fd4e1fb feat: 接入分层模板规格快照` 已把部分分层模板 spec 快照链路接入 `dev`。本清单按当前 `origin/dev` 修订，不应再把 `efcc340` 当作最新业务代码基线。

已经完成的内容：

- `pixelle_video/models/layered_template.py` 已有 `RectSpec`、`LayerSourceSpec`、`TemplateLayer`、`LayeredTemplateSpec` 和 fingerprint。
- `pixelle_video/models/template_preset.py` 已有 `TemplatePreset` 基础模型，且 `spec` 必须是真实 `LayeredTemplateSpec`，不能是 `None`。
- `web/components/layered_template_state.py` 已有 `LayeredTemplateEditorState`、多图层 append、source update、spec build 和 spec 回填能力。
- `pixelle_video/models/media_placement.py` 已支持 `scale_percent + offset_x + offset_y`，旧 `anchor` 只作为兼容输入保留。
- `web/components/style_config.py` 已把主媒体位置 UI 切到 `scale_percent + offset_x + offset_y`，并已接入基础分层模板编辑入口，可新增背景、图片和文字 layer；但还没有完整图层属性编辑 UI。
- `web/components/text_rendering_config.py` 已不再在中栏直接渲染旧即时预览；旧 `text_rendering_preview` 工具仍作为兼容模块保留。
- `style_config.render_style_config(...)` 已返回 normalized `layered_template_spec` 和 `selected_template_preset_id`。
- `web/components/output_preview.py` 的单条生成请求和批量 shared config 已携带 `layered_template_spec`、`selected_template_preset_id`。
- 标题和字幕样式源头契约已经修复，标题背景默认透明度应为 `0.0`，显式边距按画布边缘解释。

仍未完成或未端到端打通的内容：

- `web/components/output_preview.py` 仍只有“生成视频”和“最近视频”，尚未插入“即时预览工作台”。
- 当前 `layered_template_spec` 主要来自 `LayeredTemplateEditorState` 的基础 layer 列表；标题/字幕 role、主媒体 placement、图片/文件/背景资产物化、图层位置/尺寸/层级/透明度/旋转/锁定编辑尚未完整接入。
- 尚无 `TemplatePresetRepository`、`TemplateRegistry`、“我的模板”和“最近 5 个模板”持久化闭环。
- 尚无 `LayeredTemplateService`、分层模板 preview API、严格 `LayeredTemplateSpecRequest` schema。
- Web UI 生成请求已透传 `layered_template_spec`；但 `api/schemas/video.py`、`api/routers/video.py`、`StoryboardConfig`、`RenderManifest`、`TemplateRenderContext` 和标准 pipeline 仍未校验、保存和消费该 spec snapshot。
- 尚无 HTML screenshot、HyperFrames、ffmpeg manifest 的分层模板 adapter。

## 2. 不可妥协原则

- `LayeredTemplateSpec` 是唯一排版事实源；`session_state`、`template_params`、HTML 字符串都不能成为事实源。
- `template_params` 只保留 legacy HTML 模板参数，禁止新增任何分层排版字段。
- API 边界必须用严格 schema 校验完整 spec，不能长期接受裸 `dict`。
- 系统模板、我的模板、最近模板必须走同一套 registry/summary 数据结构。
- 用户上传图片、文件、背景在保存模板前必须物化为 repository asset key，不能保存 `UploadedFile`、临时路径或 preview-only URL。
- 保存模板必须先生成缩略图；缩略图失败则整个保存失败，不能产生半成品 preset。
- 旧 `anchor` 只能在输入 normalize 阶段兼容，新的 UI 和新 payload 只能输出 offset。
- adapter 禁止读取 Streamlit session，只能消费 normalized spec 和显式入参。
- 预览和最终生成必须使用同一份 spec snapshot，避免“预览正确、导出错误”。
- 不能用最小补丁绕过架构问题；每一步都要补测试，避免留下技术债。

## 3. 与 Stage1/Stage2 的边界

本功能不应修改 Stage1/Stage2 的主线设计目标，也不应重排分镜规划、投影选择、stale dependency、stage-gated delivery loop 的职责。

允许接入的边界：

- 在生成请求参数中增加 `layered_template_spec` 和 `selected_template_preset_id`。
- 在标准 pipeline 的配置、manifest 和 render context 中保存 spec snapshot。
- 在 UI 中消费现有标题、字幕、输出尺寸、媒体尺寸和主媒体位置状态来构建 spec。

禁止越界的行为：

- 不要把分层模板字段混入 Stage1/Stage2 的 planning snapshot 语义。
- 不要让分层模板保存逻辑依赖分镜工作台内部状态。
- 不要修改 Stage1/Stage2 文档或流程来适配本功能，除非发现真实 contract 冲突，并单独提交修复说明。

## 4. 剩余实施任务

### Task A: 解耦旧即时预览并接入中栏 spec 构建

**目标:** 中栏只负责编辑和生成 normalized `LayeredTemplateSpec`，旧 `text_rendering_config.py` 不再渲染即时预览。

**Files:**

- Modify: `web/components/text_rendering_config.py`
- Modify: `web/components/style_config.py`
- Modify: `web/components/layered_template_state.py`
- Modify: `tests/test_style_config_text_rendering_ui.py`
- Modify: `tests/test_style_config_template_gallery.py`
- Create/Modify: `tests/test_style_config_layered_template_ui.py`

**Steps:**

- [x] 从 `text_rendering_config.py` 中移除 `render_text_rendering_preview(...)`、`request_real_preview_frame(...)` 的直接 UI 渲染职责，只保留标题、字幕、文字策略 payload 构建。
- [x] 在 `style_config.py` 中接入 `LayeredTemplateEditorState`，从当前画布尺寸、媒体尺寸和基础图层状态构建 spec。
- [ ] 补齐标题/字幕 role、主媒体位置和图层详细属性到完整 spec 的映射。
- [x] `render_style_config(...)` 返回 `layered_template_spec` 和 `selected_template_preset_id`，供右栏和生成请求消费。
- [x] 增加图层基础编辑入口：添加文字、添加图片/文件、添加背景，每次点击新增一个 layer，而不是覆盖单一字段。
- [x] 保证当前已输出的 `layered_template_spec` 来自 `LayeredTemplateSpecBuilder`；后续新增输出仍必须遵守该约束。

**Acceptance:**

- `text_rendering_config.py` 不再出现旧即时预览渲染调用。
- 中栏可构建包含 canvas、media、safe_area、layers、metadata 的 spec；其中标题/字幕 role、主媒体 placement 和完整图层属性仍需在后续补齐。
- 多次添加文字/图片/背景会产生多个独立 layer id。
- `render_style_config(...)` 的返回值包含 normalized `layered_template_spec`。

### Task B: 模板 preset repository 和统一 registry

**目标:** 建立系统模板、我的模板、最近模板的统一读取、保存和回填协议。

**Files:**

- Create: `pixelle_video/repositories/template_presets.py`
- Create: `pixelle_video/services/template_registry.py`
- Modify: `pixelle_video/models/template_preset.py`
- Modify: `web/components/style_config.py`
- Create: `tests/test_template_presets_repository.py`
- Create: `tests/test_template_registry.py`
- Modify: `tests/test_style_config_template_gallery.py`

**Steps:**

- [ ] 在 `data/template_presets/` 下定义 `presets.json`、`thumbnails/`、`assets/` 存储结构。
- [ ] 实现 `TemplatePresetRepository`，支持原子保存、读取、更新 `last_used_at`、列出最近 5 个模板。
- [ ] 实现用户上传资产物化，把图片、文件、背景保存成稳定 asset key。
- [ ] 实现系统模板包装为 `TemplatePreset`；无法完整表达 legacy HTML 内部结构时，生成完整 spec，并在 metadata 中标记 `source_kind=legacy_html`、`legacy_template_path`，同时设置 `editable=False`。
- [ ] 实现 `TemplateRegistry`，统一返回系统模板、我的模板、最近模板的 summary，不允许 UI 对系统模板和用户模板走两套结构。
- [ ] 保存“我的模板”时先生成缩略图；缩略图失败则保存失败。

**Acceptance:**

- 模板库可以列出系统模板、我的模板、最近模板。
- 保存当前配置后，模板出现在“我的模板”中。
- 点击模板能回填完整 `LayeredTemplateSpec`。
- 最近 5 个模板来自持久化 `last_used_at`，不是 session 临时列表。
- repository 不持久化临时文件句柄、UploadedFile 或 preview-only URL。

### Task C: LayeredTemplateService 和真实预览 API

**目标:** 后端统一负责 spec 校验、HTML 预览生成、真实预览帧渲染和缓存。

**Files:**

- Create: `pixelle_video/services/layered_template_service.py`
- Create: `pixelle_video/services/layered_template_adapters/html_preview.py`
- Create: `api/schemas/layered_template_preview.py`
- Create: `api/routers/layered_template_preview.py`
- Modify: `api/routers/__init__.py`
- Modify: `api/app.py`
- Create: `tests/test_layered_template_preview_service.py`
- Create: `tests/test_layered_template_preview_api.py`

**Steps:**

- [ ] 定义严格 `LayeredTemplateSpecRequest`，完整校验当前模型字段 `version/template_id/template_name/template_type/canvas_width/canvas_height/media_width/media_height/safe_area/layers/metadata`；如后续要在 API schema 中暴露嵌套 `canvas`/`media`，必须在 `to_model()` 中显式转换为现有 `LayeredTemplateSpec` 扁平字段。
- [ ] 实现 schema `to_model()`，API 入站先转为 `LayeredTemplateSpec`，再传递 normalized `to_dict()` snapshot。
- [ ] 实现 `LayeredTemplateService.render_preview_html(...)`，按 `z_index` 和 layer 顺序稳定渲染。
- [ ] `role="title"` 使用当前 `title_style`，`role="caption"` 使用当前 `caption_style`，普通 text layer 使用自己的 style。
- [ ] 实现 fingerprint 缓存：相同视觉 spec 复用真实预览帧；spec 变化后标记旧预览过期。
- [ ] 注册 `POST /api/layered-templates/preview-frame`。

**Acceptance:**

- API 拒绝不完整 spec。
- HTML 预览不接受用户自定义脚本。
- 同一 spec 的即时预览、真实预览帧和保存缩略图使用同一渲染入口。
- `text_rendering_preview` 旧 API 可兼容保留，但新工作台不依赖它。

### Task D: 右栏即时预览工作台

**目标:** 把即时预览区放到右栏“生成视频”和“最近视频”之间，并支持最近 5 个模板快速切换。

**Files:**

- Create: `web/components/layout_preview_workbench.py`
- Modify: `web/components/output_preview.py`
- Modify: `web/pipelines/standard.py`
- Modify: `web/i18n/locales/zh_CN.json`
- Modify: `web/i18n/locales/en_US.json`
- Create: `tests/test_layout_preview_workbench.py`
- Modify: `tests/test_output_preview.py`

**Steps:**

- [ ] 新建 `LayoutPreviewWorkbench`，只消费 `LayeredTemplateSpec`、`text_rendering`、预览状态和模板 registry summary。
- [ ] 在 `output_preview.render_single_output(...)` 中固定顺序：生成视频、即时预览工作台、最近视频。
- [ ] 工作台顶部展示最近使用 5 个模板文字列表，点击后通过 registry 回填 spec 并更新 `last_used_at`。
- [ ] 工作台展示当前画布尺寸、素材尺寸、图层数、主媒体 scale/offset、标题/字幕状态摘要。
- [ ] 工作台用受控 HTML/CSS 缩放画布，不让 HTML 成为新状态源。
- [ ] 增加“刷新真实预览帧”和“保存为我的模板”动作入口。

**Acceptance:**

- 右栏顺序稳定为 `生成视频 -> 即时预览工作台 -> 最近视频`。
- 即时预览能反映输出尺寸、媒体位置、标题、字幕、图片、背景和多图层。
- 最近 5 个模板点击后能快速切换并回填当前编辑状态。
- 工作台不直接读取散落 session key。

### Task E: 生成请求和 pipeline 贯通

**目标:** 生成链路从 UI 到后端 render context 都携带同一份 spec snapshot。

**Files:**

- Modify: `web/components/output_preview.py`
- Modify: `api/schemas/video.py`
- Modify: `api/routers/video.py`
- Modify: `pixelle_video/models/storyboard.py`
- Modify: `pixelle_video/models/render_package.py`
- Modify: `pixelle_video/models/template_render_context.py`
- Modify: `pixelle_video/pipelines/standard.py`
- Create: `tests/test_standard_pipeline_layered_template.py`
- Modify: `tests/test_template_render_context.py`
- Modify: `tests/test_output_preview.py`

**Steps:**

- [x] `build_single_generation_request(...)` 和 batch shared config 携带 `layered_template_spec`、`selected_template_preset_id`。
- [ ] `api/schemas/video.py` 使用严格 schema 接收 spec，不允许裸 dict 长期穿透。
- [ ] `StoryboardConfig` 保存 normalized spec snapshot 和 selected preset id。
- [ ] `RenderManifest` 保存 spec snapshot，便于任务复现。
- [ ] `TemplateRenderContext` 保存 spec snapshot，供 HyperFrames compiled 路径消费。
- [ ] `standard.py` 构建配置、manifest、render context 时都走 `LayeredTemplateSpec.from_dict(...)` 校验。

**Acceptance:**

- 生成请求中包含完整 `layered_template_spec`。
- 历史任务保存 spec snapshot，模板后续被修改也不会影响旧任务复现。
- pipeline 内部可以序列化 dict snapshot，但不能绕过模型校验。
- `template_params` 没有新增图层布局字段。

### Task F: HTML screenshot 真实帧渲染

**目标:** 分层模板能通过 raw HTML document 渲染为 PNG，不依赖旧 legacy template path。

**Files:**

- Create: `pixelle_video/services/layered_template_adapters/html_frame.py`
- Modify: `pixelle_video/services/frame_html.py`
- Modify: `pixelle_video/services/template_visual_materializer.py`
- Modify: `tests/test_frame_html.py`
- Modify: `tests/test_template_visual_materializer.py`

**Steps:**

- [ ] 在 `frame_html.py` 抽出 `HTMLDocumentFrameRenderer`，支持显式 `base_path` 和 raw HTML document 截图。
- [ ] 实现 `LayeredTemplateHTMLFrameAdapter`，由 `LayeredTemplateService` 生成 HTML，再截图为 PNG。
- [ ] `TemplateVisualMaterializer.materialize_frame(...)` 在存在 `layered_template_spec` 时走新 adapter。
- [ ] legacy `HTMLFrameGenerator(template_path)` 只服务旧模板路径，不再作为 raw HTML 的通用入口。

**Acceptance:**

- 分层模板截图不依赖 `templates/1080x1920/image_default.html` 这类旧路径。
- 截图尺寸来自 spec 的 canvas。
- materializer diagnostics 中记录 layered template id。

### Task G: HyperFrames 和 ffmpeg manifest adapter

**目标:** HyperFrames compiled 和 ffmpeg manifest 路径也从同一 spec 派生，避免第三套排版逻辑。

**Files:**

- Create: `pixelle_video/services/layered_template_adapters/hyperframes.py`
- Create: `pixelle_video/services/layered_template_adapters/ffmpeg_manifest.py`
- Modify: `pixelle_video/services/hyperframes_compiler.py`
- Modify: `pixelle_video/services/ffmpeg_manifest_renderer.py`
- Modify: `pixelle_video/services/render_capability_resolver.py`
- Modify: `pixelle_video/pipelines/standard.py`
- Modify: `tests/test_hyperframes_compiler.py`
- Modify: `tests/test_ffmpeg_manifest_renderer.py`
- Modify: `tests/test_render_capability_resolver.py`

**Steps:**

- [ ] 实现 `LayeredTemplateHyperFramesAdapter`，从 spec 构建 HyperFrames 可消费的 composition/context。
- [ ] `HyperFramesCompiler` 在 context 存在 spec 时生成分层模板 composition。
- [ ] 实现 `LayeredTemplateFfmpegAdapter`，先把 spec 预渲染为视觉资产，再交给 ffmpeg manifest。
- [ ] `ffmpeg_manifest` 不直接解释复杂 layer list，只消费预物化结果。
- [ ] `RenderCapabilityResolver` 明确 layered template 的可用路径和降级原因。

**Acceptance:**

- HyperFrames、HTML screenshot、ffmpeg manifest 都消费同一 spec。
- unsupported layer type 必须明确失败，不能静默忽略。
- ffmpeg manifest 不产生独立布局解释器。

### Task H: 清理旧 UI 文案和兼容边界

**目标:** 移除产品层旧 9 宫格位置概念，保留必要的 legacy 输入兼容。

**Files:**

- Modify: `web/i18n/locales/zh_CN.json`
- Modify: `web/i18n/locales/en_US.json`
- Modify: `api/schemas/text_rendering_preview.py`
- Modify: `web/components/text_rendering_preview.py`
- Modify: `tests/test_text_rendering_preview.py`
- Modify: `tests/test_text_rendering_preview_api.py`

**Steps:**

- [ ] 清理产品 UI 中不再使用的 `media_placement.anchor.*` 文案，避免新 UI 再暴露 9 宫格；不要删除 `text_style.position.*` 或 `text_rendering_preview` 所需的历史位置文案。
- [ ] 区分 `text_rendering_preview` 旧 API 内部 `position`/`anchor` 兼容和主媒体 `MediaPlacement` 新 offset 契约，不要混用。
- [ ] 确认 fixture 中旧 `anchor` 只属于历史兼容或文本预览上下文，不进入新分层模板 payload。

**Acceptance:**

- 新 UI 不展示左上、右上、左下、右下等位置选择。
- 新生成 payload 不输出 `media_placement.anchor`。
- legacy fixture 和 API 兼容路径有测试覆盖。

## 5. 推荐并行拆分

可并行，但必须保持写入边界清晰：

- Worker 1: 负责 Task B，写入 `pixelle_video/repositories/template_presets.py`、`pixelle_video/services/template_registry.py`、对应 repository/registry tests。
- Worker 2: 负责 Task C 和 Task F，写入 `layered_template_service`、preview API、HTML preview/frame adapter、对应 service/API/materializer tests。
- Worker 3: 负责 Task A 和 Task D，写入 Streamlit 中栏 spec 构建、右栏 workbench、output_preview 接入和 UI tests。
- Worker 4: 负责 Task E 和 Task G，写入 API/video schema、storyboard/render context/pipeline、HyperFrames/ffmpeg adapters 和 pipeline tests。
- Worker 5: 负责 Task H 和 i18n 清理，只处理文案、兼容边界和 fixtures，不碰核心服务。

并行限制：

- 不允许多个 worker 同时重写 `web/components/style_config.py` 或 `web/components/output_preview.py` 的同一函数。
- API schema 和 pipeline 字段命名必须先约定为 `layered_template_spec`、`selected_template_preset_id`。
- 各 worker 不得 revert 其他人的改动；冲突时以 `LayeredTemplateSpec` 单事实源为准。

## 6. 验证命令

每个任务完成后先跑对应 focused tests，最终统一跑：

下列命令包含本计划将要新增的测试文件；在对应 Task 尚未落地前，不应直接把“文件不存在”视为功能回归。执行某个 Task 时，先运行该 Task 已创建或已修改的 focused tests，等 Task A-H 全部完成后再运行完整命令。

```powershell
python -m pytest tests/test_media_placement.py tests/test_layered_template_models.py tests/test_layered_template_spec_builder.py tests/test_style_config_template_gallery.py tests/test_style_config_layered_template_ui.py tests/test_layout_preview_workbench.py tests/test_output_preview.py tests/test_template_presets_repository.py tests/test_template_registry.py tests/test_layered_template_preview_service.py tests/test_layered_template_preview_api.py tests/test_template_visual_materializer.py tests/test_frame_html.py tests/test_hyperframes_compiler.py tests/test_ffmpeg_manifest_renderer.py tests/test_render_capability_resolver.py tests/test_standard_pipeline_layered_template.py tests/test_template_render_context.py -q
```

提交前补充：

```powershell
python -m py_compile pixelle_video\models\layered_template.py pixelle_video\models\template_preset.py web\components\layered_template_state.py
git diff --check
git status --short --branch
```

如果改动触及 API schema，还要跑：

```powershell
python -m pytest tests/test_text_rendering_preview_api.py tests/test_output_preview.py tests/test_standard_pipeline_layered_template.py -q
```

## 7. 最终验收标准

- 右栏顺序为 `生成视频 -> 即时预览工作台 -> 最近视频`。
- 即时预览显示最终画布比例、输出尺寸、媒体尺寸、主媒体 scale/offset、标题、字幕、图片、背景和多图层效果。
- 图片位置产品 UI 默认中心，只通过 `scale_percent`、`offset_x`、`offset_y` 调整。
- 用户可以添加多个文字层、多个图片/文件层、多个背景层。
- 每个 layer 都能调整位置、尺寸、层级、透明度、旋转和锁定状态。
- 当前配置可以保存为“我的模板”，并出现在模板库中。
- 最近使用 5 个模板显示在即时预览工作台顶部，点击后快速切换。
- 预览、保存模板和生成请求使用同一份 normalized `LayeredTemplateSpec`。
- HTML screenshot、HyperFrames compiled、ffmpeg manifest 不各自维护独立排版事实源。
- 所有新增 API 和 repository 行为有自动化测试覆盖。

## 8. 交接注意事项

- 接手前先用 `git status --short --branch` 识别并隔离与本任务无关的工作区改动；不要把无关文件混入本计划的提交。
- 旧计划 `docs/superpowers/plans/2026-05-02-layered-template-preview-workbench-implementation.md` 中 Task 1、Task 2 和部分 Task 5 已有实现，接手时不要重复实现，只做 review 或补丁。
- `origin/feature/layered-template-workbench`、`origin/feature/layered-template-integration`、`origin/feature/layered-template-preset-registry`、`origin/feature/layered-template-preview-api` 含有尚未进入 `origin/dev` 的分层模板相关实现；继续实施前应先 review/diff 这些分支，确认可复用内容，再按本仓库原子提交规则合入或重做。
- 如果发现旧计划代码片段和本交接清单冲突，以本清单的边界规则为准。
- 每个原子任务完成后按仓库规则使用中文提交信息，并立即 push 到 `origin/dev`。
