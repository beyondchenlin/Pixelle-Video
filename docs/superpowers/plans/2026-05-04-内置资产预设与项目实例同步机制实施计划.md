# 内置资产预设与项目实例同步机制 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 AssetBible/IP 建立随代码发布的内置预设库，并通过显式导入机制生成项目可编辑实例，彻底切断 `output/` 作为团队共享资产源的错误路径。

**Architecture:** 新增只读 `resources/presets/asset_bibles/` 预设源和 `AssetBiblePresetRegistry` 加载服务；API 增加内置预设列表、详情和导入到项目实例的入口；项目生成链路仍只消费项目级 AssetBible。第一阶段不重构模板、风格前缀、分镜预设，只让 AssetBible/IP 补齐同等生命周期能力。

**Tech Stack:** Python dataclasses, JSON file presets, FastAPI routers, existing `AssetBible` dataclass validation, filesystem dev repositories, Streamlit UI helpers, pytest.

---

## Scope Guard

本计划实现 `docs/superpowers/specs/2026-05-04-内置资产预设与项目实例同步机制设计.md` 的阶段一，并为阶段二留下清晰接口。

包含：

- 内置 AssetBible 预设文件格式
- 内置预设加载、校验、列表和详情服务
- 正定向导兔内置 AssetBible 预设
- API：列出预设、读取预设、导入到项目 AssetBible
- Web client helper：调用预设列表和导入接口
- IP 设计工作台基础入口：浏览内置预设并导入
- 测试：加载器、API、Web helper、UI 基础行为

不包含：

- 自动三方合并上游版本
- 模板/风格前缀/分镜预设外置化
- `output/_platform/assets` 到 `data/platform/assets` 的正式迁移实现
- 命令行迁移工具
- 生成链路大改造

---

## File Structure

- Create `pixelle_video/models/asset_bible_preset.py`
  - 定义 `AssetBiblePreset`，负责预设元数据和内置 `AssetBible` payload 的模型校验。
- Create `pixelle_video/services/asset_bible_preset_registry.py`
  - 从 `resources/presets/asset_bibles/*.json` 读取内置预设。
  - 提供 `list_presets()`、`get_preset()`、`build_project_asset_bible()`。
- Create `resources/presets/asset_bibles/zhengding_guide_bible.json`
  - 保存正定向导兔内置预设。
- Create `api/routers/asset_bible_presets.py`
  - 暴露预设列表、详情、导入接口。
- Modify `api/app.py`
  - 注册新增 router。
- Modify `api/platform_dependencies.py`
  - 挂载 `asset_bible_preset_registry`。
- Modify `web/utils/asset_bible_api.py`
  - 新增预设列表、详情、导入 helper。
- Modify `web/components/ip_design_workbench.py`
  - 在 AssetBible 区块显示内置预设入口并支持导入。
- Modify `web/i18n/locales/zh_CN.json`
  - 新增中文 UI 文案。
- Modify `web/i18n/locales/en_US.json`
  - 新增英文 UI 文案。
- Test `tests/test_asset_bible_preset_registry.py`
  - 覆盖内置预设加载与导入 payload。
- Test `tests/test_asset_bible_preset_api.py`
  - 覆盖 API 列表、详情、导入和冲突失败。
- Modify `tests/test_app_platform_dependencies.py`
  - 覆盖 app state 挂载 registry。
- Modify `tests/test_asset_prompt_plan_projection_ui.py`
  - 覆盖 Web helper endpoint 和导入 payload。
- Modify `tests/test_ip_design_workbench_ui.py`
  - 覆盖 UI 调用导入 helper。

---

## Task 1: AssetBible Preset Model

**Files:**

- Create: `pixelle_video/models/asset_bible_preset.py`
- Test: `tests/test_asset_bible_preset_registry.py`

- [ ] **Step 1: Write failing model test**

Create `tests/test_asset_bible_preset_registry.py` with this first test:

```python
from pixelle_video.models.asset_bible_preset import AssetBiblePreset


def _preset_payload() -> dict:
    return {
        "preset_id": "builtin_asset_bible_demo",
        "revision": "2026-05-04.1",
        "source": "builtin",
        "display_name": "Demo IP",
        "description": "Demo preset.",
        "tags": ["demo"],
        "preview_asset_path": "resources/presets/asset_bibles/previews/demo.png",
        "asset_bible": {
            "asset_bible_id": "demo_bible",
            "workspace_id": "__builtin__",
            "project_id": "__builtin__",
            "ip_profiles": [
                {
                    "ip_profile_id": "ip_main",
                    "workspace_id": "__builtin__",
                    "project_id": "__builtin__",
                    "name": "Demo IP",
                    "identity_lock": ["white cartoon rabbit"],
                    "identity_anchors": ["blue bow tie"],
                }
            ],
            "character_profiles": [],
            "scene_assets": [],
            "prop_assets": [],
            "style_profiles": [],
            "metadata": {},
        },
    }


def test_asset_bible_preset_validates_asset_bible_payload():
    preset = AssetBiblePreset.from_dict(_preset_payload())

    assert preset.preset_id == "builtin_asset_bible_demo"
    assert preset.revision == "2026-05-04.1"
    assert preset.asset_bible.asset_bible_id == "demo_bible"
    assert preset.to_summary_dict()["display_name"] == "Demo IP"
```

- [ ] **Step 2: Run test and confirm it fails**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py::test_asset_bible_preset_validates_asset_bible_payload
```

Expected: FAIL with `ModuleNotFoundError: No module named 'pixelle_video.models.asset_bible_preset'`.

- [ ] **Step 3: Implement model**

Create `pixelle_video/models/asset_bible_preset.py`:

```python
from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass
from typing import Any

from pixelle_video.models.asset_bible import AssetBible


@dataclass(frozen=True)
class AssetBiblePreset:
    preset_id: str
    revision: str
    source: str
    display_name: str
    description: str
    tags: tuple[str, ...]
    preview_asset_path: str | None
    asset_bible: AssetBible

    def __post_init__(self) -> None:
        object.__setattr__(self, "preset_id", _require_text("preset_id", self.preset_id))
        object.__setattr__(self, "revision", _require_text("revision", self.revision))
        object.__setattr__(self, "source", _require_text("source", self.source))
        if self.source != "builtin":
            raise ValueError("asset bible preset source must be builtin")
        object.__setattr__(self, "display_name", _require_text("display_name", self.display_name))
        object.__setattr__(self, "description", _optional_text(self.description) or "")
        object.__setattr__(self, "tags", _normalize_tags(self.tags))
        object.__setattr__(self, "preview_asset_path", _optional_text(self.preview_asset_path))
        if not isinstance(self.asset_bible, AssetBible):
            raise ValueError("asset_bible must be an AssetBible")

    @classmethod
    def from_dict(cls, payload: Mapping[str, Any]) -> "AssetBiblePreset":
        if not isinstance(payload, Mapping):
            raise ValueError("asset bible preset payload must be a mapping")
        return cls(
            preset_id=payload.get("preset_id", ""),
            revision=payload.get("revision", ""),
            source=payload.get("source", ""),
            display_name=payload.get("display_name", ""),
            description=payload.get("description", ""),
            tags=tuple(payload.get("tags") or ()),
            preview_asset_path=payload.get("preview_asset_path"),
            asset_bible=AssetBible.from_dict(payload.get("asset_bible") or {}),
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            **self.to_summary_dict(),
            "asset_bible": self.asset_bible.to_dict(),
        }

    def to_summary_dict(self) -> dict[str, Any]:
        return {
            "preset_id": self.preset_id,
            "revision": self.revision,
            "source": self.source,
            "display_name": self.display_name,
            "description": self.description,
            "tags": list(self.tags),
            "preview_asset_path": self.preview_asset_path,
        }


def _require_text(field_name: str, value: Any) -> str:
    if not isinstance(value, str) or not value.strip():
        raise ValueError(f"{field_name} must not be empty")
    return value.strip()


def _optional_text(value: Any) -> str | None:
    if value is None:
        return None
    if not isinstance(value, str):
        raise ValueError("optional text values must be strings")
    return value.strip() or None


def _normalize_tags(value: tuple[str, ...]) -> tuple[str, ...]:
    tags = tuple(_require_text("tags", item) for item in value)
    if len(set(tags)) != len(tags):
        raise ValueError("tags must not contain duplicates")
    return tags


__all__ = ["AssetBiblePreset"]
```

- [ ] **Step 4: Run model test**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py::test_asset_bible_preset_validates_asset_bible_payload
```

Expected: PASS.

---

## Task 2: Preset Registry

**Files:**

- Create: `pixelle_video/services/asset_bible_preset_registry.py`
- Modify: `tests/test_asset_bible_preset_registry.py`

- [ ] **Step 1: Add failing registry tests**

Append:

```python
import json
from pathlib import Path

import pytest

from pixelle_video.services.asset_bible_preset_registry import (
    AssetBiblePresetRegistry,
)


def test_registry_loads_json_presets_from_root(tmp_path: Path):
    root = tmp_path / "asset_bibles"
    root.mkdir()
    (root / "demo.json").write_text(json.dumps(_preset_payload()), encoding="utf-8")

    registry = AssetBiblePresetRegistry(root=root)

    assert [item.preset_id for item in registry.list_presets()] == [
        "builtin_asset_bible_demo"
    ]
    assert registry.get_preset("builtin_asset_bible_demo").display_name == "Demo IP"


def test_registry_builds_project_asset_bible_with_origin_metadata(tmp_path: Path):
    root = tmp_path / "asset_bibles"
    root.mkdir()
    (root / "demo.json").write_text(json.dumps(_preset_payload()), encoding="utf-8")
    registry = AssetBiblePresetRegistry(root=root)

    asset_bible = registry.build_project_asset_bible(
        preset_id="builtin_asset_bible_demo",
        workspace_id="workspace_1",
        project_id="project_1",
        asset_bible_id="demo_project_bible",
        imported_at="2026-05-04T00:00:00Z",
    )

    assert asset_bible.workspace_id == "workspace_1"
    assert asset_bible.project_id == "project_1"
    assert asset_bible.asset_bible_id == "demo_project_bible"
    assert asset_bible.ip_profiles[0].workspace_id == "workspace_1"
    assert asset_bible.ip_profiles[0].project_id == "project_1"
    assert asset_bible.metadata["source_kind"] == "imported"
    assert asset_bible.metadata["origin_preset_id"] == "builtin_asset_bible_demo"
    assert asset_bible.metadata["origin_revision"] == "2026-05-04.1"


def test_registry_rejects_unknown_preset(tmp_path: Path):
    registry = AssetBiblePresetRegistry(root=tmp_path)

    with pytest.raises(KeyError, match="unknown asset bible preset"):
        registry.get_preset("missing")
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py
```

Expected: FAIL with missing registry module.

- [ ] **Step 3: Implement registry**

Create `pixelle_video/services/asset_bible_preset_registry.py`:

```python
from __future__ import annotations

import json
from copy import deepcopy
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

from pixelle_video.models.asset_bible import AssetBible
from pixelle_video.models.asset_bible_preset import AssetBiblePreset

DEFAULT_ASSET_BIBLE_PRESET_ROOT = Path("resources/presets/asset_bibles")


class AssetBiblePresetRegistry:
    def __init__(self, root: str | Path = DEFAULT_ASSET_BIBLE_PRESET_ROOT) -> None:
        self.root = Path(root)

    def list_presets(self) -> list[AssetBiblePreset]:
        return sorted(
            self._load_all(),
            key=lambda preset: preset.display_name,
        )

    def list_summaries(self) -> list[dict[str, Any]]:
        return [preset.to_summary_dict() for preset in self.list_presets()]

    def get_preset(self, preset_id: str) -> AssetBiblePreset:
        target_id = _require_text("preset_id", preset_id)
        for preset in self._load_all():
            if preset.preset_id == target_id:
                return preset
        raise KeyError(f"unknown asset bible preset: {target_id}")

    def build_project_asset_bible(
        self,
        *,
        preset_id: str,
        workspace_id: str,
        project_id: str,
        asset_bible_id: str | None = None,
        imported_at: str | None = None,
    ) -> AssetBible:
        preset = self.get_preset(preset_id)
        payload = deepcopy(preset.asset_bible.to_dict())
        target_asset_bible_id = _require_text(
            "asset_bible_id",
            asset_bible_id or payload["asset_bible_id"],
        )
        target_workspace_id = _require_text("workspace_id", workspace_id)
        target_project_id = _require_text("project_id", project_id)
        payload["asset_bible_id"] = target_asset_bible_id
        payload["workspace_id"] = target_workspace_id
        payload["project_id"] = target_project_id
        for collection_name in (
            "ip_profiles",
            "character_profiles",
            "scene_assets",
            "prop_assets",
            "style_profiles",
        ):
            for item in payload.get(collection_name) or []:
                if isinstance(item, dict):
                    item["workspace_id"] = target_workspace_id
                    item["project_id"] = target_project_id
        metadata = dict(payload.get("metadata") or {})
        metadata.update(
            {
                "source_kind": "imported",
                "origin_preset_id": preset.preset_id,
                "origin_revision": preset.revision,
                "imported_at": imported_at or datetime.now(timezone.utc).isoformat(),
                "customized": False,
            }
        )
        payload["metadata"] = metadata
        return AssetBible.from_dict(payload)

    def _load_all(self) -> list[AssetBiblePreset]:
        if not self.root.exists():
            return []
        presets: list[AssetBiblePreset] = []
        for path in sorted(self.root.glob("*.json")):
            try:
                payload = json.loads(path.read_text(encoding="utf-8"))
            except json.JSONDecodeError as exc:
                raise ValueError(f"invalid asset bible preset JSON: {path}") from exc
            presets.append(AssetBiblePreset.from_dict(payload))
        return presets


def _require_text(field_name: str, value: object) -> str:
    if not isinstance(value, str) or not value.strip():
        raise ValueError(f"{field_name} must not be empty")
    return value.strip()


__all__ = ["AssetBiblePresetRegistry", "DEFAULT_ASSET_BIBLE_PRESET_ROOT"]
```

- [ ] **Step 4: Run registry tests**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py
```

Expected: PASS.

---

## Task 3: Built-In Zhengding Preset

**Files:**

- Create: `resources/presets/asset_bibles/zhengding_guide_bible.json`
- Modify: `tests/test_asset_bible_preset_registry.py`

- [ ] **Step 1: Add failing test for packaged preset**

Append:

```python
from collections import Counter


def test_packaged_zhengding_guide_preset_is_valid():
    registry = AssetBiblePresetRegistry()

    preset = registry.get_preset("builtin_asset_bible_zhengding_guide")
    profile = preset.asset_bible.ip_profiles[0]
    combined = list(profile.identity_lock) + list(profile.identity_anchors)
    duplicates = [item for item, count in Counter(combined).items() if count > 1]

    assert preset.display_name == "正定向导兔"
    assert profile.name == "正定向导兔"
    assert profile.style_hint == "清爽文旅插画风格，干净线条，柔和自然光，温暖色彩，适合短视频导览画面。"
    assert duplicates == []
```

- [ ] **Step 2: Run test and confirm failure**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py::test_packaged_zhengding_guide_preset_is_valid
```

Expected: FAIL with `unknown asset bible preset`.

- [ ] **Step 3: Add preset JSON**

Create `resources/presets/asset_bibles/zhengding_guide_bible.json`:

```json
{
  "asset_bible": {
    "asset_bible_id": "zhengding_guide_bible",
    "character_profiles": [],
    "ip_profiles": [
      {
        "color_palette": {},
        "forbidden_elements": [],
        "identity_anchors": [
          "蓝色领结",
          "长耳朵",
          "浅粉色耳朵内侧",
          "圆润脸型",
          "清爽亲和的卡通造型"
        ],
        "identity_lock": [
          "白色卡通兔子",
          "古城文旅向导",
          "亲和好奇的表情",
          "温和陪伴式讲解者"
        ],
        "identity_suppression_rules": [
          "不能变成人类",
          "不能变成其他动物",
          "不能丢失长耳朵",
          "不能丢失蓝色领结",
          "不能变成写实恐怖动物",
          "不能替代历史建筑或宗教主体"
        ],
        "image_text_palette": {},
        "ip_profile_id": "ip_main",
        "logline": "一只白色兔子古城向导，温和、好奇，擅长带游客理解正定历史。",
        "metadata": {},
        "name": "正定向导兔",
        "negative_constraints": [
          "不要替代古城主体",
          "不要遮挡佛像寺庙城墙等核心内容",
          "不要出现错误文字",
          "不要出现乱码",
          "不要变成蓝色兔子",
          "不要变成真人玩偶"
        ],
        "project_id": "__builtin__",
        "semantic_boundary": [
          "它是陪伴式古城导游和文旅吉祥物",
          "不是历史人物",
          "不是宗教人物",
          "不是佛像",
          "不能替代正定古城城墙寺庙碑刻等真实主体"
        ],
        "style_hint": "清爽文旅插画风格，干净线条，柔和自然光，温暖色彩，适合短视频导览画面。",
        "variable_slots": [
          "表情",
          "动作",
          "站位",
          "手持地图",
          "手持导览旗",
          "讲解姿势",
          "与游客或古城场景的距离",
          "出场强度"
        ],
        "visible_text_whitelist": [
          "正定",
          "古城",
          "导览"
        ],
        "workspace_id": "__builtin__",
        "world_hint": "正定古城、城墙、古寺、青砖、历史文化旅游。"
      }
    ],
    "metadata": {},
    "project_id": "__builtin__",
    "prop_assets": [],
    "scene_assets": [],
    "style_profiles": [],
    "workspace_id": "__builtin__"
  },
  "description": "面向正定古城文旅短视频的陪伴式导游 IP。",
  "display_name": "正定向导兔",
  "preset_id": "builtin_asset_bible_zhengding_guide",
  "preview_asset_path": null,
  "revision": "2026-05-04.1",
  "source": "builtin",
  "tags": [
    "文旅",
    "正定",
    "导游IP",
    "卡通角色"
  ]
}
```

- [ ] **Step 4: Run packaged preset test**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py::test_packaged_zhengding_guide_preset_is_valid
```

Expected: PASS.

- [ ] **Step 5: Commit Task 1-3**

Run:

```powershell
git add pixelle_video/models/asset_bible_preset.py pixelle_video/services/asset_bible_preset_registry.py resources/presets/asset_bibles/zhengding_guide_bible.json tests/test_asset_bible_preset_registry.py
git commit -m "feat: 新增内置 AssetBible 预设注册表"
git push
```

---

## Task 4: API Router For Presets

**Files:**

- Create: `api/routers/asset_bible_presets.py`
- Modify: `api/app.py`
- Modify: `api/platform_dependencies.py`
- Test: `tests/test_asset_bible_preset_api.py`
- Modify: `tests/test_app_platform_dependencies.py`

- [ ] **Step 1: Write failing API tests**

Create `tests/test_asset_bible_preset_api.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from fastapi import FastAPI
from fastapi.testclient import TestClient

from api.routers.asset_bible import router as asset_bible_router
from api.routers.asset_bible_presets import router as preset_router
from pixelle_video.services.asset_bible_preset_registry import AssetBiblePresetRegistry
from tests.test_asset_bible_api import FakeAssetBibleRepository
from tests.test_asset_bible_preset_registry import _preset_payload


def _client(tmp_path, repository: FakeAssetBibleRepository | None = None) -> TestClient:
    root = tmp_path / "asset_bibles"
    root.mkdir()
    (root / "demo.json").write_text(
        __import__("json").dumps(_preset_payload()),
        encoding="utf-8",
    )
    app = FastAPI()
    app.state.asset_bible_repository = repository or FakeAssetBibleRepository()
    app.state.asset_bible_preset_registry = AssetBiblePresetRegistry(root=root)
    app.include_router(asset_bible_router)
    app.include_router(preset_router)
    return TestClient(app)


def test_list_asset_bible_presets(tmp_path):
    response = _client(tmp_path).get("/api/presets/asset-bibles")

    assert response.status_code == 200
    assert response.json()["items"][0]["preset_id"] == "builtin_asset_bible_demo"


def test_load_asset_bible_preset_detail(tmp_path):
    response = _client(tmp_path).get(
        "/api/presets/asset-bibles/builtin_asset_bible_demo"
    )

    assert response.status_code == 200
    payload = response.json()
    assert payload["preset"]["asset_bible"]["asset_bible_id"] == "demo_bible"


def test_import_asset_bible_preset_to_project(tmp_path):
    repository = FakeAssetBibleRepository()
    client = _client(tmp_path, repository)

    response = client.post(
        "/api/projects/project_1/asset-bible/import-from-preset",
        json={
            "workspace_id": "workspace_1",
            "preset_id": "builtin_asset_bible_demo",
            "asset_bible_id": "demo_project_bible",
            "conflict_policy": "fail",
        },
    )

    assert response.status_code == 201
    asset_bible = response.json()["asset_bible"]
    assert asset_bible["asset_bible_id"] == "demo_project_bible"
    assert asset_bible["workspace_id"] == "workspace_1"
    assert asset_bible["project_id"] == "project_1"
    assert asset_bible["metadata"]["origin_preset_id"] == "builtin_asset_bible_demo"
    assert repository.saved[0][0] == "workspace_1"


def test_import_asset_bible_preset_rejects_existing_target_by_default(tmp_path):
    repository = FakeAssetBibleRepository()
    repository.asset_bibles[("workspace_1", "demo_project_bible")] = {
        **_preset_payload()["asset_bible"],
        "workspace_id": "workspace_1",
        "project_id": "project_1",
        "asset_bible_id": "demo_project_bible",
    }
    client = _client(tmp_path, repository)

    response = client.post(
        "/api/projects/project_1/asset-bible/import-from-preset",
        json={
            "workspace_id": "workspace_1",
            "preset_id": "builtin_asset_bible_demo",
            "asset_bible_id": "demo_project_bible",
        },
    )

    assert response.status_code == 409
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_api.py
```

Expected: FAIL with missing `api.routers.asset_bible_presets`.

- [ ] **Step 3: Implement router**

Create `api/routers/asset_bible_presets.py`:

```python
from __future__ import annotations

from typing import Literal

from fastapi import APIRouter, HTTPException, Request, status
from pydantic import BaseModel, Field

from api.routers.asset_bible import _asset_bible_response, _build_asset_bible_response
from api.schemas.storyboard_workbench import validate_public_reference_id

router = APIRouter(prefix="", tags=["Asset Bible Presets"])


class AssetBiblePresetListResponse(BaseModel):
    items: list[dict]


class AssetBiblePresetResponse(BaseModel):
    preset: dict


class AssetBiblePresetImportRequest(BaseModel):
    workspace_id: str
    preset_id: str
    asset_bible_id: str | None = Field(default=None)
    conflict_policy: Literal["fail"] = "fail"


@router.get("/presets/asset-bibles", response_model=AssetBiblePresetListResponse)
async def list_asset_bible_presets(request: Request) -> AssetBiblePresetListResponse:
    registry = _get_asset_bible_preset_registry(request)
    return AssetBiblePresetListResponse(items=registry.list_summaries())


@router.get(
    "/presets/asset-bibles/{preset_id}",
    response_model=AssetBiblePresetResponse,
)
async def load_asset_bible_preset(
    preset_id: str,
    request: Request,
) -> AssetBiblePresetResponse:
    registry = _get_asset_bible_preset_registry(request)
    try:
        preset = registry.get_preset(preset_id)
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    return AssetBiblePresetResponse(preset=preset.to_dict())


@router.post(
    "/projects/{project_id}/asset-bible/import-from-preset",
    status_code=status.HTTP_201_CREATED,
)
async def import_asset_bible_preset(
    project_id: str,
    payload: AssetBiblePresetImportRequest,
    request: Request,
):
    project_id = validate_public_reference_id("project_id", project_id)
    workspace_id = validate_public_reference_id("workspace_id", payload.workspace_id)
    if payload.asset_bible_id is not None:
        asset_bible_id = validate_public_reference_id("asset_bible_id", payload.asset_bible_id)
    else:
        asset_bible_id = None
    repository = _get_asset_bible_repository(request)
    registry = _get_asset_bible_preset_registry(request)
    asset_bible = registry.build_project_asset_bible(
        preset_id=payload.preset_id,
        workspace_id=workspace_id,
        project_id=project_id,
        asset_bible_id=asset_bible_id,
    )
    existing = await repository.load_asset_bible(workspace_id, asset_bible.asset_bible_id)
    if existing is not None:
        raise HTTPException(
            status_code=409,
            detail="asset bible already exists; choose a different asset_bible_id",
        )
    saved = await repository.save_asset_bible(workspace_id, asset_bible.to_dict())
    return _build_asset_bible_response(
        asset_bible=_asset_bible_response(
            saved,
            project_id=project_id,
            asset_bible_id=asset_bible.asset_bible_id,
        )
    )


def _get_asset_bible_repository(request: Request):
    repository = getattr(request.app.state, "asset_bible_repository", None)
    if repository is None:
        raise HTTPException(status_code=503, detail="asset bible repository is not configured")
    return repository


def _get_asset_bible_preset_registry(request: Request):
    registry = getattr(request.app.state, "asset_bible_preset_registry", None)
    if registry is None:
        raise HTTPException(status_code=503, detail="asset bible preset registry is not configured")
    return registry
```

- [ ] **Step 4: Register dependencies and app router**

Modify `api/platform_dependencies.py`:

```python
from pixelle_video.services.asset_bible_preset_registry import AssetBiblePresetRegistry
```

Add field to `PlatformDependencies`:

```python
asset_bible_preset_registry: AssetBiblePresetRegistry
```

In `build_platform_dependencies(...)`, create:

```python
asset_bible_preset_registry = AssetBiblePresetRegistry()
```

Pass it into `PlatformDependencies(...)`.

Modify `api/app.py` import list to include:

```python
from api.routers import asset_bible_presets as asset_bible_presets_router
```

Register before or near existing AssetBible router:

```python
app.include_router(asset_bible_presets_router.router, prefix=api_config.api_prefix)
```

- [ ] **Step 5: Extend app dependency test**

In `tests/test_app_platform_dependencies.py`, add assertion where other dependencies are asserted:

```python
assert app.state.asset_bible_preset_registry is dependencies.asset_bible_preset_registry
```

- [ ] **Step 6: Run API tests**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_api.py tests/test_app_platform_dependencies.py
```

Expected: PASS.

- [ ] **Step 7: Commit Task 4**

Run:

```powershell
git add api/routers/asset_bible_presets.py api/app.py api/platform_dependencies.py tests/test_asset_bible_preset_api.py tests/test_app_platform_dependencies.py
git commit -m "feat: 添加 AssetBible 内置预设导入 API"
git push
```

---

## Task 5: Web Client Helpers

**Files:**

- Modify: `web/utils/asset_bible_api.py`
- Modify: `tests/test_asset_prompt_plan_projection_ui.py`

- [ ] **Step 1: Add failing helper tests**

Append to `tests/test_asset_prompt_plan_projection_ui.py`:

```python
def test_list_asset_bible_presets_gets_presets_endpoint(monkeypatch):
    from web.utils import asset_bible_api

    captured = {}

    class FakeResponse:
        def raise_for_status(self):
            return None

        def json(self):
            return {"items": [{"preset_id": "builtin_asset_bible_demo"}]}

    def fake_get(url, timeout):
        captured["url"] = url
        captured["timeout"] = timeout
        return FakeResponse()

    monkeypatch.setattr(asset_bible_api.httpx, "get", fake_get)

    result = asset_bible_api.list_asset_bible_presets(
        api_base_url="http://test/api",
    )

    assert captured["url"] == "http://test/api/presets/asset-bibles"
    assert result == [{"preset_id": "builtin_asset_bible_demo"}]


def test_import_asset_bible_preset_posts_project_endpoint(monkeypatch):
    from web.utils import asset_bible_api

    captured = {}

    class FakeResponse:
        def raise_for_status(self):
            return None

        def json(self):
            return {"asset_bible": {"asset_bible_id": "demo_project_bible"}}

    def fake_post(url, json, timeout):
        captured["url"] = url
        captured["json"] = json
        captured["timeout"] = timeout
        return FakeResponse()

    monkeypatch.setattr(asset_bible_api.httpx, "post", fake_post)

    result = asset_bible_api.import_asset_bible_preset(
        api_base_url="http://test/api",
        project_id="project_1",
        workspace_id="workspace_1",
        preset_id="builtin_asset_bible_demo",
        asset_bible_id="demo_project_bible",
    )

    assert captured["url"] == "http://test/api/projects/project_1/asset-bible/import-from-preset"
    assert captured["json"] == {
        "workspace_id": "workspace_1",
        "preset_id": "builtin_asset_bible_demo",
        "asset_bible_id": "demo_project_bible",
        "conflict_policy": "fail",
    }
    assert result["asset_bible"]["asset_bible_id"] == "demo_project_bible"
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```powershell
python -m pytest -q tests/test_asset_prompt_plan_projection_ui.py::test_list_asset_bible_presets_gets_presets_endpoint tests/test_asset_prompt_plan_projection_ui.py::test_import_asset_bible_preset_posts_project_endpoint
```

Expected: FAIL with missing helper functions.

- [ ] **Step 3: Implement helpers**

Add to `web/utils/asset_bible_api.py`:

```python
def build_asset_bible_preset_list_endpoint(*, api_base_url: str) -> str:
    return f"{api_base_url.rstrip('/')}/presets/asset-bibles"


def build_asset_bible_preset_detail_endpoint(
    *,
    api_base_url: str,
    preset_id: str,
) -> str:
    preset_id = _validate_public_reference_id("preset_id", preset_id)
    return f"{api_base_url.rstrip('/')}/presets/asset-bibles/{preset_id}"


def build_asset_bible_preset_import_endpoint(
    *,
    api_base_url: str,
    project_id: str,
) -> str:
    project_id = _validate_public_reference_id("project_id", project_id)
    return f"{api_base_url.rstrip('/')}/projects/{project_id}/asset-bible/import-from-preset"


def list_asset_bible_presets(
    *,
    api_base_url: str,
    timeout: float = 30.0,
) -> list[dict[str, Any]]:
    response = httpx.get(
        build_asset_bible_preset_list_endpoint(api_base_url=api_base_url),
        timeout=timeout,
    )
    response.raise_for_status()
    data = response.json()
    if not isinstance(data, dict):
        raise ValueError("asset bible preset list response must be a JSON object")
    items = data.get("items")
    if not isinstance(items, list):
        raise ValueError("asset bible preset list response must include items")
    return _require_dict_items("items", items)


def import_asset_bible_preset(
    *,
    api_base_url: str,
    project_id: str,
    workspace_id: str,
    preset_id: str,
    asset_bible_id: str | None = None,
    timeout: float = 30.0,
) -> dict[str, Any]:
    payload = {
        "workspace_id": _validate_public_reference_id("workspace_id", workspace_id),
        "preset_id": _validate_public_reference_id("preset_id", preset_id),
        "conflict_policy": "fail",
    }
    if asset_bible_id is not None and asset_bible_id.strip():
        payload["asset_bible_id"] = _validate_public_reference_id(
            "asset_bible_id",
            asset_bible_id,
        )
    response = httpx.post(
        build_asset_bible_preset_import_endpoint(
            api_base_url=api_base_url,
            project_id=project_id,
        ),
        json=payload,
        timeout=timeout,
    )
    response.raise_for_status()
    data = response.json()
    if not isinstance(data, dict):
        raise ValueError("asset bible preset import response must be a JSON object")
    if not isinstance(data.get("asset_bible"), dict):
        raise ValueError("asset bible preset import response must include asset_bible")
    return data
```

- [ ] **Step 4: Run helper tests**

Run:

```powershell
python -m pytest -q tests/test_asset_prompt_plan_projection_ui.py::test_list_asset_bible_presets_gets_presets_endpoint tests/test_asset_prompt_plan_projection_ui.py::test_import_asset_bible_preset_posts_project_endpoint
```

Expected: PASS.

---

## Task 6: IP Workbench Import UI

**Files:**

- Modify: `web/components/ip_design_workbench.py`
- Modify: `web/i18n/locales/zh_CN.json`
- Modify: `web/i18n/locales/en_US.json`
- Modify: `tests/test_ip_design_workbench_ui.py`

- [ ] **Step 1: Add failing UI test**

Append to `tests/test_ip_design_workbench_ui.py`:

```python
def test_ip_design_workbench_imports_builtin_asset_bible_preset():
    from web.components.ip_design_workbench import render_ip_design_workbench

    class FakeClient:
        def __init__(self):
            self.import_calls = []

        def list_asset_bibles(self, **kwargs):
            return {"asset_bibles": []}

        def list_asset_bible_presets(self):
            return [
                {
                    "preset_id": "builtin_asset_bible_demo",
                    "display_name": "Demo IP",
                    "revision": "2026-05-04.1",
                }
            ]

        def import_asset_bible_preset(self, **kwargs):
            self.import_calls.append(kwargs)
            return {"asset_bible": {"asset_bible_id": "demo_project_bible"}}

    class FakeUI:
        def __init__(self):
            self.session_state = {
                "workspace_id": "workspace_1",
                "project_id": "project_1",
                "ip_design_builtin_preset_select": "builtin_asset_bible_demo",
                "ip_design_import_asset_bible_id": "demo_project_bible",
            }
            self.successes = []

        def markdown(self, *args, **kwargs): pass
        def caption(self, *args, **kwargs): pass
        def info(self, *args, **kwargs): pass
        def error(self, *args, **kwargs): pass
        def success(self, value): self.successes.append(value)
        def container(self, **kwargs): return self
        def __enter__(self): return self
        def __exit__(self, *args): return False
        def selectbox(self, label, options, **kwargs): return self.session_state[kwargs["key"]]
        def text_input(self, label, **kwargs): return self.session_state.get(kwargs["key"], kwargs.get("value", ""))
        def text_area(self, label, **kwargs): return kwargs.get("value", "")
        def button(self, label, **kwargs): return kwargs["key"] == "ip_design_import_builtin_asset_bible"

    client = FakeClient()
    ui = FakeUI()

    render_ip_design_workbench(
        ip_design_client=client,
        ui=ui,
        translate=lambda key, **kwargs: key,
    )

    assert client.import_calls == [
        {
            "workspace_id": "workspace_1",
            "project_id": "project_1",
            "preset_id": "builtin_asset_bible_demo",
            "asset_bible_id": "demo_project_bible",
        }
    ]
    assert ui.successes == ["ip_design.asset_bible.imported"]
```

- [ ] **Step 2: Run test and confirm failure**

Run:

```powershell
python -m pytest -q tests/test_ip_design_workbench_ui.py::test_ip_design_workbench_imports_builtin_asset_bible_preset
```

Expected: FAIL because UI does not call preset helpers.

- [ ] **Step 3: Add client methods**

Where the in-process or HTTP IP design client is defined, add methods that call `web.utils.asset_bible_api.list_asset_bible_presets(...)` and `import_asset_bible_preset(...)`. Keep method names:

```python
def list_asset_bible_presets(self) -> list[dict[str, Any]]:
    return asset_bible_api.list_asset_bible_presets(api_base_url=self.api_base_url)


def import_asset_bible_preset(
    self,
    *,
    workspace_id: str,
    project_id: str,
    preset_id: str,
    asset_bible_id: str | None = None,
) -> dict[str, Any]:
    return asset_bible_api.import_asset_bible_preset(
        api_base_url=self.api_base_url,
        workspace_id=workspace_id,
        project_id=project_id,
        preset_id=preset_id,
        asset_bible_id=asset_bible_id,
    )
```

- [ ] **Step 4: Render import section**

In `_render_asset_bible_section(...)`, before manual AssetBible fields, add:

```python
        if hasattr(ip_design_client, "list_asset_bible_presets"):
            try:
                builtin_presets = ip_design_client.list_asset_bible_presets()
            except Exception:
                builtin_presets = []
                ui.caption(translate("ip_design.asset_bible.presets_load_failed"))
            if builtin_presets:
                preset_options = [item["preset_id"] for item in builtin_presets]
                preset_labels = {
                    item["preset_id"]: item.get("display_name") or item["preset_id"]
                    for item in builtin_presets
                }
                selected_preset_id = ui.selectbox(
                    translate("ip_design.asset_bible.builtin_presets"),
                    preset_options,
                    key="ip_design_builtin_preset_select",
                    format_func=lambda value: preset_labels.get(value, value),
                )
                import_asset_bible_id = _text_input(
                    ui,
                    translate("ip_design.asset_bible.import_id"),
                    key="ip_design_import_asset_bible_id",
                    value=selected_preset_id.replace("builtin_asset_bible_", "") + "_bible",
                )
                if ui.button(
                    translate("ip_design.asset_bible.import"),
                    key="ip_design_import_builtin_asset_bible",
                ):
                    try:
                        ip_design_client.import_asset_bible_preset(
                            workspace_id=workspace_id,
                            project_id=project_id,
                            preset_id=selected_preset_id,
                            asset_bible_id=import_asset_bible_id,
                        )
                    except Exception:
                        ui.error(translate("ip_design.asset_bible.import_failed"))
                    else:
                        ui.success(translate("ip_design.asset_bible.imported"))
                        return {"asset_bible_id": "", "saved": True}
```

- [ ] **Step 5: Add i18n keys**

Add to `web/i18n/locales/zh_CN.json`:

```json
"ip_design.asset_bible.builtin_presets": "内置资产库",
"ip_design.asset_bible.import_id": "导入后的 AssetBible ID",
"ip_design.asset_bible.import": "导入到当前项目",
"ip_design.asset_bible.imported": "内置 AssetBible 已导入当前项目。",
"ip_design.asset_bible.import_failed": "内置 AssetBible 导入失败。",
"ip_design.asset_bible.presets_load_failed": "内置资产库加载失败。"
```

Add to `web/i18n/locales/en_US.json`:

```json
"ip_design.asset_bible.builtin_presets": "Built-in Asset Library",
"ip_design.asset_bible.import_id": "Imported AssetBible ID",
"ip_design.asset_bible.import": "Import to Current Project",
"ip_design.asset_bible.imported": "Built-in AssetBible imported into the current project.",
"ip_design.asset_bible.import_failed": "Failed to import built-in AssetBible.",
"ip_design.asset_bible.presets_load_failed": "Failed to load built-in asset library."
```

- [ ] **Step 6: Run UI test**

Run:

```powershell
python -m pytest -q tests/test_ip_design_workbench_ui.py::test_ip_design_workbench_imports_builtin_asset_bible_preset
```

Expected: PASS.

- [ ] **Step 7: Commit Task 5-6**

Run:

```powershell
git add web/utils/asset_bible_api.py web/components/ip_design_workbench.py web/i18n/locales/zh_CN.json web/i18n/locales/en_US.json tests/test_asset_prompt_plan_projection_ui.py tests/test_ip_design_workbench_ui.py
git commit -m "feat: 支持在 IP 工作台导入内置 AssetBible"
git push
```

---

## Task 7: Final Verification

**Files:**

- No code changes unless verification reveals a defect.

- [ ] **Step 1: Run focused test suite**

Run:

```powershell
python -m pytest -q tests/test_asset_bible_preset_registry.py tests/test_asset_bible_preset_api.py tests/test_app_platform_dependencies.py tests/test_asset_prompt_plan_projection_ui.py tests/test_ip_design_workbench_ui.py tests/test_asset_bible_api.py tests/test_standard_pipeline_storyboard_generation.py tests/test_style_config_ip_controls.py
```

Expected: PASS.

- [ ] **Step 2: Verify no output asset files are tracked**

Run:

```powershell
git ls-files -- output data
```

Expected: no `output/_platform/assets/assets.json`. If `data/` remains gitignored, no runtime project instance files should appear.

- [ ] **Step 3: Verify packaged preset is tracked**

Run:

```powershell
git ls-files -- resources/presets/asset_bibles/zhengding_guide_bible.json
```

Expected:

```text
resources/presets/asset_bibles/zhengding_guide_bible.json
```

- [ ] **Step 4: Run lint on touched Python files**

Run:

```powershell
python -m ruff check pixelle_video/models/asset_bible_preset.py pixelle_video/services/asset_bible_preset_registry.py api/routers/asset_bible_presets.py api/app.py api/platform_dependencies.py web/utils/asset_bible_api.py web/components/ip_design_workbench.py tests/test_asset_bible_preset_registry.py tests/test_asset_bible_preset_api.py tests/test_asset_prompt_plan_projection_ui.py tests/test_ip_design_workbench_ui.py
```

Expected: PASS.

- [ ] **Step 5: Commit verification fixes if needed**

If any verification fix is required:

```powershell
git add <fixed-files>
git commit -m "fix: 修正内置 AssetBible 预设接入问题"
git push
```

---

## Follow-Up Plan

阶段二应单独写计划，不混进本实施计划：

1. 将 AssetBible 主持久化根从 `output/_platform/assets` 迁移到 `data/platform/assets`。
2. 提供兼容读取旧 `output/_platform/assets/assets.json` 的 adapter。
3. 提供一次性迁移命令或 UI 操作。
4. 为导入实例增加上游 revision 状态识别。

这些属于持久化迁移和版本同步提示，应该在阶段一稳定后再做。

---

## Self-Review Checklist

- Spec coverage: 阶段一覆盖内置预设、导入 API、UI 基础入口、正定向导兔预设和 `output/` 不入 Git 的要求。
- Placeholder scan: 已检查计划正文，没有未落地的占位项或延后项。
- Type consistency: `AssetBiblePreset`、`AssetBiblePresetRegistry`、`list_asset_bible_presets`、`import_asset_bible_preset` 名称在任务之间一致。
