# 生成页世界观提示辅助回填 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为生成页现有 `世界观提示` 输入框补上“根据文案生成”和“使用 IP 默认”两个辅助回填入口，同时保持正式生成合同和现有世界观主链路不变。

**Architecture:** 新增一个轻量 `content/world-hint-draft` 接口复用 `ContentWorldPlanner` 生成结构化世界画像，再用确定性 helper 渲染成用户可编辑草稿文本。前端通过纯 helper 解析当前已选 IP 的默认世界观，在高级分镜控件中完成两个按钮的回填与提示，不把 `ip_profile_world_hint` 发送到 API。

**Tech Stack:** FastAPI, Pydantic, Streamlit, httpx, existing `ContentWorldPlanner`, pytest.

---

## Branch Strategy

- 建议实现分支：`feat/world-hint-draft-assist`
- 每个任务完成后先推送到 `origin/feat/world-hint-draft-assist`
- 全部验证通过后，再合并回 `dev` 并推送 `origin/dev`

## File Structure

- Create `pixelle_video/services/content_world_hint_draft_builder.py`
  - 把 `ContentWorldProfile` 渲染成用户可编辑的 `world_hint_draft`
- Modify `api/schemas/content.py`
  - 新增世界观草稿请求/响应 schema
- Modify `api/routers/content.py`
  - 新增 `POST /content/world-hint-draft`
- Create `web/utils/content_api.py`
  - 前端调用世界观草稿接口的 HTTP helper
- Modify `web/components/ip_prompt_chain_controls.py`
  - 新增不依赖 UI 渲染顺序的 selected profile summary helper
- Modify `web/components/storyboard_planning_controls.py`
  - 在高级分镜控件中增加两个辅助按钮及回填逻辑
- Modify `web/components/style_config.py`
  - 统一加载 asset bibles，并把 `content_context` / `ip_profile_world_hint` 接给高级分镜控件
- Modify `web/i18n/locales/zh_CN.json`
  - 新增按钮、帮助、错误提示文案
- Modify `web/i18n/locales/en_US.json`
  - 新增英文 fallback 文案
- Create `tests/test_content_world_hint_draft_builder.py`
  - 纯 helper 测试
- Modify `tests/test_content_image_prompt_api.py`
  - 内容 API 新接口测试
- Create `tests/test_web_content_api.py`
  - 前端 content API helper 测试
- Modify `tests/test_style_config_ip_controls.py`
  - selected profile summary helper 测试
- Modify `tests/test_style_config_storyboard_planning_ui.py`
  - 两个按钮的 UI 回填与 warning 测试

## Task 1: World Hint Draft Builder

**Files:**
- Create: `pixelle_video/services/content_world_hint_draft_builder.py`
- Test: `tests/test_content_world_hint_draft_builder.py`

- [ ] **Step 1: Write the failing builder tests**

Create `tests/test_content_world_hint_draft_builder.py`:

```python
from pixelle_video.models.content_world import ContentWorldProfile
from pixelle_video.services.content_world_hint_draft_builder import build_world_hint_draft


def test_build_world_hint_draft_renders_compact_chinese_text():
    profile = ContentWorldProfile(
        summary="正定古城清晨漫游",
        time_space="当代正定古城，清晨，长乐门到城楼一线",
        visual_environment="青砖城墙、晨雾、水汽和古城街巷",
        story_constraints="不能替代真实古建筑与历史主体",
        ip_integration_guidance="IP 作为陪伴式向导，低侵入融入",
    )

    draft = build_world_hint_draft(profile, prompt_language="zh_CN")

    assert "正定古城清晨漫游" in draft
    assert "不能替代真实古建筑与历史主体" in draft
    assert "IP 作为陪伴式向导" in draft
    assert "summary" not in draft
    assert "generation_world_profile" not in draft


def test_build_world_hint_draft_skips_empty_fields():
    profile = ContentWorldProfile(
        summary="古城漫游",
        atmosphere=None,
        story_constraints=None,
    )

    draft = build_world_hint_draft(profile, prompt_language="zh_CN")

    assert "古城漫游" in draft
    assert "None" not in draft


def test_build_world_hint_draft_supports_english():
    profile = ContentWorldProfile(
        summary="Ancient city dawn walk",
        ip_integration_guidance="The IP appears as a low-intrusion guide.",
    )

    draft = build_world_hint_draft(profile, prompt_language="en_US")

    assert "Ancient city dawn walk" in draft
    assert "low-intrusion guide" in draft
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_content_world_hint_draft_builder.py
```

Expected: fail because the builder module does not exist.

- [ ] **Step 3: Write minimal implementation**

Create `pixelle_video/services/content_world_hint_draft_builder.py`:

```python
from __future__ import annotations

from pixelle_video.models.content_world import ContentWorldProfile


def build_world_hint_draft(
    profile: ContentWorldProfile,
    *,
    prompt_language: str = "zh_CN",
) -> str:
    clauses = _english_clauses(profile) if prompt_language == "en_US" else _chinese_clauses(profile)
    return " ".join(clause for clause in clauses if clause).strip()


def _chinese_clauses(profile: ContentWorldProfile) -> list[str]:
    return [
        profile.summary or "",
        profile.time_space or "",
        profile.visual_environment or "",
        f"整体氛围为{profile.atmosphere}。" if profile.atmosphere else "",
        f"叙事边界：{profile.story_constraints}。" if profile.story_constraints else "",
        f"IP 融入方式：{profile.ip_integration_guidance}。" if profile.ip_integration_guidance else "",
    ]


def _english_clauses(profile: ContentWorldProfile) -> list[str]:
    return [
        profile.summary or "",
        profile.time_space or "",
        profile.visual_environment or "",
        f"Overall atmosphere: {profile.atmosphere}." if profile.atmosphere else "",
        f"Story constraints: {profile.story_constraints}." if profile.story_constraints else "",
        f"IP integration: {profile.ip_integration_guidance}." if profile.ip_integration_guidance else "",
    ]
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_content_world_hint_draft_builder.py
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- pixelle_video/services/content_world_hint_draft_builder.py tests/test_content_world_hint_draft_builder.py
git commit -m "feat: 增加世界观提示草稿文本构建器"
git push origin feat/world-hint-draft-assist
```

## Task 2: Content Draft API

**Files:**
- Modify: `api/schemas/content.py`
- Modify: `api/routers/content.py`
- Test: `tests/test_content_image_prompt_api.py`

- [ ] **Step 1: Write failing API tests**

Add to `tests/test_content_image_prompt_api.py`:

```python
from api.routers.content import generate_world_hint_draft
from api.schemas.content import WorldHintDraftGenerateRequest


def test_world_hint_draft_request_rejects_blank_source_text():
    with pytest.raises(ValidationError):
        WorldHintDraftGenerateRequest(source_text="   ")


@pytest.mark.asyncio
async def test_generate_world_hint_draft_endpoint_uses_content_world_planner(monkeypatch):
    captured = {}

    class FakePlanner:
        async def plan(self, **kwargs):
            captured.update(kwargs)
            from pixelle_video.models.content_world import ContentWorldHintSource, ContentWorldProfile
            return ContentWorldProfile(
                summary="正定古城清晨漫游",
                story_constraints="不能替代真实古建筑",
                ip_integration_guidance="IP 作为陪伴式向导",
                hint_source=ContentWorldHintSource.GENERATED_FROM_SCRIPT,
            )

    monkeypatch.setattr("api.routers.content.ContentWorldPlanner", lambda: FakePlanner())

    response = await generate_world_hint_draft(
        WorldHintDraftGenerateRequest(
            source_text="从长乐门出发，这是正定的南大门。",
            title="正定漫游",
            world_preset_id="neutral_knowledge_storyboard",
            ip_default_world_hint="适合亲切文旅讲解世界。",
            storyboard_prompt_language="zh_CN",
        ),
        _FakePixelleVideo(),
    )

    assert captured["ip_world_hint"] == "适合亲切文旅讲解世界。"
    assert "正定古城清晨漫游" in response.world_hint_draft
    assert response.generation_world_profile["summary"] == "正定古城清晨漫游"
    assert response.hint_source == "generated_from_script"
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_content_image_prompt_api.py -k "world_hint_draft"
```

Expected: fail because request/endpoint do not exist.

- [ ] **Step 3: Implement request and endpoint**

In `api/schemas/content.py`, add:

```python
class WorldHintDraftGenerateRequest(BaseModel):
    source_text: str = Field(..., description="Source text used to derive a request-scoped world hint draft")
    title: Optional[str] = Field(None, description="Optional title used as extra world context")
    world_preset_id: Optional[str] = Field(None, description="Storyboard world preset id")
    storyboard_prompt_language: StoryboardPromptLanguage = Field(
        "zh_CN",
        description="Language used for the returned world hint draft",
    )
    ip_default_world_hint: Optional[str] = Field(
        None,
        description="Frontend-only selected IP default world hint used as planner guidance",
    )

    @model_validator(mode="after")
    def validate_source_text(self) -> "WorldHintDraftGenerateRequest":
        self.source_text = self.source_text.strip()
        if not self.source_text:
            raise ValueError("source_text must not be blank")
        if self.title is not None:
            self.title = self.title.strip() or None
        if self.ip_default_world_hint is not None:
            self.ip_default_world_hint = self.ip_default_world_hint.strip() or None
        return self


class WorldHintDraftGenerateResponse(BaseModel):
    success: bool = True
    message: str = "Success"
    world_hint_draft: str
    generation_world_profile: dict
    hint_source: str
```

In `api/routers/content.py`, add:

```python
from pixelle_video.config.storyboard_preset_library import lookup_world_preset
from pixelle_video.services.content_world_hint_draft_builder import build_world_hint_draft
from pixelle_video.services.content_world_planner import ContentWorldPlanner
```

and endpoint:

```python
@router.post("/world-hint-draft", response_model=WorldHintDraftGenerateResponse)
async def generate_world_hint_draft(
    request: WorldHintDraftGenerateRequest,
    pixelle_video: PixelleVideoDep,
):
    world_library = pixelle_video.config.get("storyboard_world_preset_library", {})
    world_preset = lookup_world_preset(world_library, request.world_preset_id)
    source_text = (
        f"标题：{request.title}\n正文：{request.source_text}"
        if request.title
        else request.source_text
    )
    profile = await ContentWorldPlanner().plan(
        llm_service=pixelle_video.llm,
        source_text=source_text,
        generation_world_hint=None,
        ip_world_hint=request.ip_default_world_hint,
        world_preset=world_preset,
    )
    draft = build_world_hint_draft(
        profile,
        prompt_language=request.storyboard_prompt_language,
    )
    return WorldHintDraftGenerateResponse(
        world_hint_draft=draft,
        generation_world_profile=profile.to_dict(),
        hint_source=profile.hint_source.value,
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_content_image_prompt_api.py -k "world_hint_draft"
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- api/schemas/content.py api/routers/content.py tests/test_content_image_prompt_api.py
git commit -m "feat: 增加世界观提示草稿接口"
git push origin feat/world-hint-draft-assist
```

## Task 3: Frontend Content API Helper

**Files:**
- Create: `web/utils/content_api.py`
- Test: `tests/test_web_content_api.py`

- [ ] **Step 1: Write failing frontend helper tests**

Create `tests/test_web_content_api.py`:

```python
import httpx

from web.utils import content_api


def test_generate_world_hint_draft_posts_expected_payload(monkeypatch):
    captured = {}

    def fake_post(url, json, timeout):
        captured["url"] = url
        captured["json"] = json
        captured["timeout"] = timeout
        return httpx.Response(
            200,
            request=httpx.Request("POST", url),
            json={
                "world_hint_draft": "古城漫游草稿",
                "generation_world_profile": {"summary": "古城漫游"},
                "hint_source": "generated_from_script",
            },
        )

    monkeypatch.setattr(content_api.httpx, "post", fake_post)

    payload = content_api.generate_world_hint_draft(
        source_text="demo",
        title="title",
        world_preset_id="neutral_knowledge_storyboard",
        storyboard_prompt_language="zh_CN",
        ip_default_world_hint="friendly guide world",
    )

    assert captured["json"]["ip_default_world_hint"] == "friendly guide world"
    assert payload["world_hint_draft"] == "古城漫游草稿"
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_web_content_api.py
```

Expected: fail because helper module does not exist.

- [ ] **Step 3: Implement helper**

Create `web/utils/content_api.py`:

```python
from __future__ import annotations

import httpx

DEFAULT_TIMEOUT = 60.0
DEFAULT_ENDPOINT = "http://127.0.0.1:8000/api/content/world-hint-draft"


def generate_world_hint_draft(
    *,
    source_text: str,
    title: str | None = None,
    world_preset_id: str | None = None,
    storyboard_prompt_language: str = "zh_CN",
    ip_default_world_hint: str | None = None,
    endpoint: str = DEFAULT_ENDPOINT,
    timeout: float = DEFAULT_TIMEOUT,
) -> dict:
    response = httpx.post(
        endpoint,
        json={
            "source_text": source_text,
            "title": title,
            "world_preset_id": world_preset_id,
            "storyboard_prompt_language": storyboard_prompt_language,
            "ip_default_world_hint": ip_default_world_hint,
        },
        timeout=timeout,
    )
    response.raise_for_status()
    return response.json()
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_web_content_api.py
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- web/utils/content_api.py tests/test_web_content_api.py
git commit -m "feat: 增加世界观提示草稿前端接口 helper"
git push origin feat/world-hint-draft-assist
```

## Task 4: Selected IP Summary Helper

**Files:**
- Modify: `web/components/ip_prompt_chain_controls.py`
- Modify: `tests/test_style_config_ip_controls.py`

- [ ] **Step 1: Write failing helper tests**

Add to `tests/test_style_config_ip_controls.py`:

```python
def test_resolve_selected_ip_prompt_chain_profile_summary_returns_world_hint():
    summary = ip_prompt_chain_controls.resolve_selected_ip_prompt_chain_profile_summary(
        session_state={
            "style_ip_enabled": True,
            "style_ip_asset_bible_id": "bible_demo",
            "style_ip_profile_id": "ip_main",
        },
        asset_bibles=[
            {
                "asset_bible_id": "bible_demo",
                "ip_profiles": [
                    {
                        "ip_profile_id": "ip_main",
                        "name": "白兔向导",
                        "world_hint": "适合亲切文旅讲解世界。",
                    }
                ],
            }
        ],
    )

    assert summary["ip_profile_world_hint"] == "适合亲切文旅讲解世界。"
    assert summary["ip_profile_name"] == "白兔向导"
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_style_config_ip_controls.py -k "resolve_selected_ip_prompt_chain_profile_summary"
```

Expected: fail because helper does not exist.

- [ ] **Step 3: Implement helper**

In `web/components/ip_prompt_chain_controls.py`, add:

```python
def resolve_selected_ip_prompt_chain_profile_summary(
    *,
    session_state,
    asset_bibles,
) -> dict[str, Any]:
    if not bool(session_state.get("style_ip_enabled", False)):
        return {}
    asset_bible = _find_mapping_item(
        [dict(item) for item in asset_bibles],
        "asset_bible_id",
        session_state.get("style_ip_asset_bible_id"),
    ) or {}
    profile = _find_mapping_item(
        _list_of_dicts(asset_bible.get("ip_profiles")),
        "ip_profile_id",
        session_state.get("style_ip_profile_id"),
    ) or {}
    summary = {
        "ip_asset_bible_id": _first_text(asset_bible.get("asset_bible_id")),
        "ip_profile_id": _first_text(profile.get("ip_profile_id")),
        "ip_profile_name": _first_text(profile.get("name")),
    }
    world_hint = _first_text(profile.get("world_hint"))
    if world_hint:
        summary["ip_profile_world_hint"] = world_hint
    return {key: value for key, value in summary.items() if value}
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_style_config_ip_controls.py -k "resolve_selected_ip_prompt_chain_profile_summary"
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- web/components/ip_prompt_chain_controls.py tests/test_style_config_ip_controls.py
git commit -m "feat: 增加已选IP摘要解析 helper"
git push origin feat/world-hint-draft-assist
```

## Task 5: Storyboard Controls Assistant Actions

**Files:**
- Modify: `web/components/storyboard_planning_controls.py`
- Modify: `web/i18n/locales/zh_CN.json`
- Modify: `web/i18n/locales/en_US.json`
- Modify: `tests/test_style_config_storyboard_planning_ui.py`

- [ ] **Step 1: Write failing UI tests**

Add to `tests/test_style_config_storyboard_planning_ui.py`:

```python
def test_render_storyboard_advanced_controls_can_fill_world_hint_from_ip_default(monkeypatch):
    fake_st = _FakeStreamlit()
    fake_st.session_state["storyboard_planning_enabled"] = True
    fake_st.session_state["_button_returns"] = {"storyboard_world_hint_use_ip_default": True}
    monkeypatch.setattr(storyboard_planning_controls, "safe_rerun", lambda: None)

    payload = storyboard_planning_controls.render_storyboard_advanced_controls(
        ui=fake_st,
        translate=lambda key, **kwargs: key,
        session_state=fake_st.session_state,
        storyboard_default_enabled=True,
        world_library_loader=lambda: {"default_world_preset_id": None, "items": []},
        shot_library_loader=lambda: {"items": []},
        ip_profile_world_hint="适合亲切文旅讲解世界。",
        content_context={"title": "demo", "text": "demo"},
    )

    assert fake_st.session_state["storyboard_generation_world_hint"] == "适合亲切文旅讲解世界。"


def test_render_storyboard_advanced_controls_can_fill_world_hint_from_generated_draft(monkeypatch):
    fake_st = _FakeStreamlit()
    fake_st.session_state["storyboard_planning_enabled"] = True
    fake_st.session_state["_button_returns"] = {"storyboard_world_hint_generate_from_content": True}
    monkeypatch.setattr(storyboard_planning_controls, "safe_rerun", lambda: None)

    payload = storyboard_planning_controls.render_storyboard_advanced_controls(
        ui=fake_st,
        translate=lambda key, **kwargs: key,
        session_state=fake_st.session_state,
        storyboard_default_enabled=True,
        world_library_loader=lambda: {"default_world_preset_id": "neutral_knowledge_storyboard", "items": []},
        shot_library_loader=lambda: {"items": []},
        ip_profile_world_hint="适合亲切文旅讲解世界。",
        content_context={"title": "正定漫游", "text": "从长乐门出发。"},
        world_hint_draft_generator=lambda payload: {"world_hint_draft": "古城清晨漫游，IP 是陪伴式向导。"},
    )

    assert fake_st.session_state["storyboard_generation_world_hint"] == "古城清晨漫游，IP 是陪伴式向导。"
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py -k "world_hint_from"
```

Expected: fail because buttons and helper callback do not exist.

- [ ] **Step 3: Implement assistant actions**

In `web/components/storyboard_planning_controls.py`, import:

```python
from web.utils.content_api import generate_world_hint_draft
from web.utils.streamlit_helpers import safe_rerun
```

Extend signature:

```python
def render_storyboard_advanced_controls(
    *,
    ui=st,
    translate=tr,
    session_state=None,
    storyboard_default_enabled=False,
    preview_snapshot=None,
    world_library_loader=None,
    shot_library_loader=None,
    content_context: Mapping[str, Any] | None = None,
    ip_profile_world_hint: str | None = None,
    world_hint_draft_generator=None,
):
```

Inside the left column, immediately after `text_area(...)`, add:

```python
        draft_generator = world_hint_draft_generator or generate_world_hint_draft
        action_col1, action_col2 = ui.columns(2)
        with action_col1:
            if ui.button(
                translate("storyboard.generation_world_hint_generate"),
                key="storyboard_world_hint_generate_from_content",
                width="stretch",
            ):
                source_text = str((content_context or {}).get("text") or "").strip()
                if not source_text:
                    ui.warning(translate("storyboard.generation_world_hint_missing_content"))
                else:
                    response = draft_generator(
                        source_text=source_text,
                        title=str((content_context or {}).get("title") or "").strip() or None,
                        world_preset_id=storyboard_world_preset_id,
                        storyboard_prompt_language="zh_CN",
                        ip_default_world_hint=ip_profile_world_hint,
                    )
                    ui.session_state["storyboard_generation_world_hint"] = str(
                        response.get("world_hint_draft") or ""
                    ).strip()
                    safe_rerun()
        with action_col2:
            if ui.button(
                translate("storyboard.generation_world_hint_use_ip_default"),
                key="storyboard_world_hint_use_ip_default",
                width="stretch",
            ):
                if not str(ip_profile_world_hint or "").strip():
                    ui.warning(translate("storyboard.generation_world_hint_missing_ip_default"))
                else:
                    ui.session_state["storyboard_generation_world_hint"] = ip_profile_world_hint.strip()
                    safe_rerun()
```

Add i18n keys:

```json
"storyboard.generation_world_hint_generate": "根据文案生成",
"storyboard.generation_world_hint_use_ip_default": "使用 IP 默认",
"storyboard.generation_world_hint_missing_content": "请先填写文案，再生成世界观提示草稿。",
"storyboard.generation_world_hint_missing_ip_default": "当前 IP 没有可用的默认世界观提示。"
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py -k "world_hint_from"
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- web/components/storyboard_planning_controls.py web/i18n/locales/zh_CN.json web/i18n/locales/en_US.json tests/test_style_config_storyboard_planning_ui.py
git commit -m "feat: 增加世界观提示辅助回填按钮"
git push origin feat/world-hint-draft-assist
```

## Task 6: Style Config Integration

**Files:**
- Modify: `web/components/style_config.py`
- Modify: `tests/test_style_config_storyboard_planning_ui.py`

- [ ] **Step 1: Write failing integration test**

Add to `tests/test_style_config_storyboard_planning_ui.py`:

```python
def test_render_style_config_passes_selected_ip_world_hint_to_storyboard_controls(monkeypatch):
    fake_st = _FakeStreamlit()
    fake_st.session_state["template_type_selector"] = "image"
    fake_st.session_state["style_ip_enabled"] = True
    fake_st.session_state["style_ip_asset_bible_id"] = "bible_demo"
    fake_st.session_state["style_ip_profile_id"] = "ip_main"
    captured = {}

    monkeypatch.setattr(style_config, "st", fake_st)
    monkeypatch.setattr(style_config, "tr", lambda key, **kwargs: key)
    monkeypatch.setattr(style_config, "get_language", lambda: "zh_CN")
    monkeypatch.setattr(style_config, "render_render_backend_selector", lambda: "render_backend")
    monkeypatch.setattr(style_config, "render_tts_audio_strategy_selector", lambda: "auto")
    monkeypatch.setattr(style_config, "render_storyboard_planning_guide", lambda: None)
    monkeypatch.setattr(style_config, "render_storyboard_preview", lambda _snapshot: [])
    monkeypatch.setattr(style_config, "_render_image_prompt_prefix_library", lambda **_kwargs: "")
    monkeypatch.setattr(
        style_config,
        "load_ip_prompt_chain_asset_bibles",
        lambda **_kwargs: [
            {
                "asset_bible_id": "bible_demo",
                "ip_profiles": [
                    {
                        "ip_profile_id": "ip_main",
                        "name": "白兔向导",
                        "world_hint": "适合亲切文旅讲解世界。",
                    }
                ],
            }
        ],
    )

    def fake_render_storyboard_advanced_controls(**kwargs):
        captured.update(kwargs)
        return {}

    monkeypatch.setattr(
        style_config.storyboard_planning_controls,
        "render_storyboard_advanced_controls",
        fake_render_storyboard_advanced_controls,
    )

    style_config.render_style_config(
        _FakeVideo(),
        storyboard_default_enabled=True,
        content_context={"title": "标题", "text": "正文"},
    )

    assert captured["ip_profile_world_hint"] == "适合亲切文旅讲解世界。"
    assert captured["content_context"]["text"] == "正文"
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py -k "passes_selected_ip_world_hint"
```

Expected: fail because `style_config` does not pass the new inputs.

- [ ] **Step 3: Integrate shared loading and pass-through**

In `web/components/style_config.py`, import:

```python
from web.components.ip_prompt_chain_controls import (
    load_ip_prompt_chain_asset_bibles,
    render_ip_prompt_chain_controls,
    resolve_selected_ip_prompt_chain_profile_summary,
)
```

Before rendering storyboard controls, add:

```python
    ip_asset_bibles: list[dict[str, Any]] = []
    selected_ip_prompt_chain_profile_summary: dict[str, Any] = {}

    def _load_ip_asset_bibles_for_style_config() -> list[dict[str, Any]]:
        try:
            return load_ip_prompt_chain_asset_bibles(
                pixelle_video=pixelle_video,
                session_state=st.session_state,
            )
        except Exception:
            logger.exception("failed to load ip prompt chain asset bibles")
            st.warning(tr("style.ip_prompt_chain.load_failed"))
            return []

    if template_media_type == "image":
        ip_asset_bibles = _load_ip_asset_bibles_for_style_config()
        selected_ip_prompt_chain_profile_summary = resolve_selected_ip_prompt_chain_profile_summary(
            session_state=st.session_state,
            asset_bibles=ip_asset_bibles,
        )
```

When calling `storyboard_planning_controls.render_storyboard_advanced_controls(...)`, pass:

```python
        content_context=content_context,
        ip_profile_world_hint=selected_ip_prompt_chain_profile_summary.get("ip_profile_world_hint"),
```

When calling `render_ip_prompt_chain_controls(...)`, pass:

```python
        asset_bibles=ip_asset_bibles,
        asset_bible_loader=None,
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py -k "passes_selected_ip_world_hint"
```

Expected: pass.

- [ ] **Step 5: Commit**

```powershell
git add -- web/components/style_config.py tests/test_style_config_storyboard_planning_ui.py
git commit -m "refactor: 打通世界观提示辅助回填所需上下文"
git push origin feat/world-hint-draft-assist
```

## Task 7: Final Verification

**Files:**
- No planned source changes unless verification reveals defects.

- [ ] **Step 1: Run focused backend suite**

Run:

```powershell
python -m pytest -q tests/test_content_world_hint_draft_builder.py tests/test_content_image_prompt_api.py tests/test_content_world_planner.py
```

Expected: all pass.

- [ ] **Step 2: Run focused frontend suite**

Run:

```powershell
python -m pytest -q tests/test_web_content_api.py tests/test_style_config_ip_controls.py tests/test_style_config_storyboard_planning_ui.py tests/test_output_preview.py
```

Expected: all pass.

- [ ] **Step 3: Review pass 1**

Run:

```powershell
rg -n "ip_profile_world_hint|generation_world_hint|world-hint-draft|根据文案生成|使用 IP 默认" web api pixelle_video tests -S
```

Check:

- `ip_profile_world_hint` 只存在前端 helper / UI 测试 / draft 调用参数，不进入正式生成请求
- `generation_world_hint` 仍是正式生成唯一请求级世界观字段
- 新接口只存在于 `content` router，不混入 `video` router

- [ ] **Step 4: Review pass 2**

Run:

```powershell
python -m pytest -q tests/test_output_preview.py -k "generation_world_hint or ip_profile_world_hint"
```

Expected: pass, confirming正式请求仍只发送 `generation_world_hint`。

- [ ] **Step 5: Final commit if verification fixes were required**

Only if Steps 1-4 required fixes:

```powershell
git add -- <changed-files>
git commit -m "fix: 修复世界观提示辅助回填验证问题"
git push origin feat/world-hint-draft-assist
```

If no files changed, do not create an empty commit.

## Acceptance Criteria

Implementation is complete only when all of the following are true:

- 生成页 `世界观提示` 文本框旁存在 `根据文案生成` 与 `使用 IP 默认`
- `使用 IP 默认` 能回填当前选中 IP 的 `world_hint`
- `根据文案生成` 能调用世界观草稿接口并回填结果
- 世界观草稿接口复用 `ContentWorldPlanner`
- `ContentWorldProfile` 被确定性渲染成自然语言草稿，不泄露 JSON key
- `ip_profile_world_hint` 不进入正式生成请求
- `generation_world_hint` 仍是正式生成唯一请求级世界观字段
- focused backend suite 与 focused frontend suite 通过

## Self-Review

- Spec coverage:
  - 两个按钮交互：Task 5 / Task 6 覆盖
  - 复用 `ContentWorldPlanner`：Task 2 覆盖
  - `ContentWorldProfile -> 草稿文本`：Task 1 覆盖
  - 正式请求边界不变：Task 7 覆盖
- Placeholder scan:
  - 无未完成占位符
  - 所有任务均给出文件路径、测试、命令和最小代码骨架
- Type consistency:
  - 统一使用 `world_hint_draft`
  - 统一使用 `ip_profile_world_hint`
  - 统一使用 `storyboard_generation_world_hint`
