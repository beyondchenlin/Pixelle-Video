# 生成页世界观提示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **当前状态：已执行归档。** 本计划已完成实施、合并到 `dev` 并推送到 `origin/dev`。下方 checkbox 记录的是原始执行计划和审计轨迹，不再作为当前待执行任务列表使用。

**Goal:** 在标准生成链路中新增生成页的“世界观提示”，并把它结构化后注入高级分镜规划、IP 使用规划和最终图片提示词生成。

**Architecture:** 前端显示名称统一保持“世界观提示”，内部字段使用 `generation_world_hint` 与现有 `IPProfile.world_hint` 消歧。新增 `ContentWorldProfile` 与 `ContentWorldPlanner`，在 `generate_styled_image_prompt_batch(...)` 中、分镜规划之前生成或归一化本次文案世界画像，并通过 `PromptContextEnvelope.plan_context` 和逐帧上下文传给 storyboard planner、IP usage planner、image prompt prompt。旧链路在未填写该字段时继续可用。

**Tech Stack:** Python dataclasses/Pydantic-style validation helpers, existing PromptContext contract, FastAPI/Pydantic request schemas, Streamlit UI, pytest, ruff.

---

## Execution Status

- 状态：已执行、已合并、已推送
- 当前本地分支：`dev`
- 当前远程分支：`origin/dev`
- 当前核对基线：`dev == origin/dev`
- 世界观方案实现分支：`feat/generation-world-hint`
- 世界观方案关键提交：
  - `92fd918 feat: 接入本次世界观到分镜规划链路`
  - `5a4051b feat: 让IP规划读取本次世界观约束`
  - `f5ff103 test: 补强生成页世界观链路验证`
  - `f8aa0c4 style: 修复合并结果的 ruff 排序问题`
- 相关源头修复提交：
  - `10380df fix: 修复IP工作台保存覆盖其他形象`

后续维护规则：

- 不要从本文件 Task 1 重新执行。
- 如果继续扩展生成页世界观、IP 融入或图片提示词链路，必须新建后续 spec / plan。
- 本文件保留为实施审计记录，用于说明当时为什么拆分 `IPProfile.world_hint`、`generation_world_hint`、`generation_world_profile` 和 `ip_adaptation`。

## Acceptance Record

当前 `dev` 已满足本计划的核心验收目标：

- 生成页高级分镜规划区存在“世界观提示”输入，内部字段为 `generation_world_hint`
- API 接收并转发 `generation_world_hint`
- 单条和批量生成请求都会透传 `generation_world_hint`
- 后端通过 `ContentWorldPlanner` 生成 `generation_world_profile`
- storyboard planner 能消费 `generation_world_profile`
- IP usage planner 能消费 `generation_world_profile`，并基于保护主体和世界观约束调整 IP 出场强度
- 图片提示词生成阶段能同时消费：
  - `plan_context.generation_world_profile`
  - `frame_contexts[*].ip_adaptation`
- `ip_profile_world_hint` 只作为前端辅助回填信息，不发送到 API
- 未填写 `generation_world_hint` 时，旧生成链路仍可运行
- 启用 IP 但缺少 `identity_lock` / `identity_anchors` 时，生成链路会拒绝继续使用该 IP，避免无身份锚点的错误融入

最近一次针对本链路的聚焦验证：

```powershell
python -m pytest -q tests/test_image_prompt_generation_prompt.py tests/test_ip_usage_planner.py tests/test_output_preview.py tests/test_styled_image_prompt_batch.py::test_z_image_final_prompt_contains_structured_ip_identity_anchors tests/test_styled_image_prompt_batch.py::test_generate_styled_image_prompt_batch_builds_generation_world_profile tests/test_styled_image_prompt_batch.py::test_generate_styled_image_prompt_batch_passes_generation_world_profile_to_real_prompt_builder tests/test_styled_image_prompt_batch.py::test_generate_styled_image_prompt_batch_rejects_enabled_ip_without_identity_anchors
```

结果：

```text
111 passed in 1.82s
```

## Source-of-Truth Boundary

本计划落地后的长期维护边界如下：

- `IPProfile.identity_lock` / `IPProfile.identity_anchors` 是 IP 身份事实源
- `IPProfile.world_hint` 是资产级长期默认世界观
- `generation_world_hint` 是本次生成请求级世界观输入
- `generation_world_profile` 是请求级结构化世界画像
- `ip_adaptation` 是逐帧 IP 融入裁决

禁止事项：

- 禁止用 `generation_world_hint` 替代 `identity_lock` / `identity_anchors`
- 禁止把 `generation_world_hint` 或 `generation_world_profile` 写回 AssetBible
- 禁止绕过 `ContentWorldPlanner` / `IPUsagePlanner` 直接把请求级文本拼进最终图片 prompt
- 禁止把 `ip_profile_world_hint` 当作 API 字段发送
- 禁止在编辑单个 IP profile 时覆盖 sibling `ip_profiles`

## Scope Guard

本计划只实现“生成页世界观提示”进入标准生成文本规划链路。

## Branch Execution Rule

本节是历史执行规则，保留用于审计。该规则已经执行完毕，不再作为当前操作指令。

原始规则：本计划必须在独立 feature worktree/branch 中执行，建议分支名为 `feat/generation-world-hint`。逐任务原子提交后推送该 feature 分支；不要在逐任务步骤中直接推送 `dev`。全部任务验证完成后，再通过干净 worktree 合并回 `dev` 并推送。

包含：

- 生成页新增 `世界观提示` 输入与辅助回填入口
- API/contract 新增 `generation_world_hint`
- 后端新增 `ContentWorldProfile` / `ContentWorldPlanner`
- storyboard planning prompt 消费本次世界画像
- IP usage planner 消费本次世界画像
- image prompt prompt 消费本次世界画像
- planning snapshot 记录世界画像来源

明确不做：

- 不删除或重命名 IP 设计页现有 `世界观提示`
- 不把 `IPProfile.world_hint` 改成其它产品名
- 不新增独立 AI Chat 式 IP 创作页
- 不做图生图、参考图、LoRA 或 ComfyUI 工作流改造
- 不把结构化 JSON 暴露成用户必须编辑的表单
- 不修改模型下载、工作流依赖或 ModelScope 相关逻辑

## File Structure

- Create `pixelle_video/models/content_world.py`
  - 定义 `ContentWorldHintSource`、`ContentWorldProfile`
  - 负责结构化世界画像的规范化、序列化、反序列化
- Create `pixelle_video/prompts/content_world.py`
  - 构建“根据文案生成世界观提示 / 世界画像”的 LLM prompt
  - 解析 LLM JSON 响应
- Create `pixelle_video/services/content_world_planner.py`
  - 负责从用户输入、完整文案、world preset、IP 默认世界观生成 `ContentWorldProfile`
  - 失败时可回退到确定性 profile
- Modify `pixelle_video/models/video_generation_contract.py`
  - `StoryboardControlsContract` 增加 `generation_world_hint`
  - `STORYBOARD_PLANNING_OPTION_KEYS` 增加该字段
- Modify `api/schemas/video.py`
  - `VideoGenerateRequest` 增加 `generation_world_hint`
  - `build_video_generation_params(...)` 透传该字段
- Modify `api/routers/video.py`
  - `video_params` 增加 `generation_world_hint`
- Modify `web/components/storyboard_planning_controls.py`
  - `build_storyboard_control_payload(...)` 接收并规范化 `generation_world_hint`
  - `render_storyboard_advanced_controls(...)` 渲染“世界观提示”输入区
- Modify `web/components/style_config.py`
  - wrapper 透传 `generation_world_hint`
  - 调用高级分镜控件时传入 `content_context` 与当前 IP 控制上下文
- Modify `web/components/ip_prompt_chain_controls.py`
  - 返回选中 IP 的最小摘要，供“使用 IP 默认”回填使用
- Modify `web/components/output_preview.py`
  - 单条和批量请求透传 `generation_world_hint`
- Modify `pixelle_video/services/image_prompt_composer.py`
  - compose 入参透传 `generation_world_hint`
  - `_build_prompt_contexts(...)` 把世界画像写入 plan/frame context
- Modify `pixelle_video/utils/content_generators.py`
  - 在 storyboard planning 前调用 `ContentWorldPlanner`
  - 把 `generation_world_profile` 放入 planner prompt contexts、IP planner、image prompt contexts 和 planning snapshot
- Modify `pixelle_video/services/storyboard_planner.py`
  - 接收 `generation_world_profile`
  - 传给 `build_storyboard_planning_prompt(...)`
  - snapshot 写入相关字段
- Modify `pixelle_video/prompts/storyboard_planning.py`
  - prompt 中明确本次世界画像的使用规则
- Modify `pixelle_video/services/ip_usage_planner.py`
  - `plan_batch(...)` / `plan_frame(...)` 接收 `generation_world_profile`
  - presence 裁决读取 `story_constraints` 与 `ip_integration_guidance`
- Modify `pixelle_video/prompts/image_generation.py`
  - prompt 中加入 `generation_world_profile` 使用规则
  - 防止最终 prompt 泄露字段名、JSON key、hex 色号
- Modify `web/i18n/locales/zh_CN.json`
  - 新增生成页“世界观提示”相关文案
- Modify `web/i18n/locales/en_US.json`
  - 新增英文 fallback 文案
- Tests:
  - Create `tests/test_content_world_models.py`
  - Create `tests/test_content_world_planner.py`
  - Modify `tests/test_video_api.py`
  - Modify `tests/test_output_preview.py`
  - Modify `tests/test_style_config_storyboard_planning_ui.py`
  - Modify `tests/test_storyboard_planner.py`
  - Modify `tests/test_styled_image_prompt_batch.py`
  - Modify `tests/test_ip_usage_planner.py`
  - Create `tests/test_image_prompt_generation_prompt.py`
  - Modify `tests/test_standard_pipeline_prompt_prefix.py`

## Task 1: Contract Field

**Files:**
- Modify: `pixelle_video/models/video_generation_contract.py`
- Modify: `api/schemas/video.py`
- Modify: `api/routers/video.py`
- Test: `tests/test_video_api.py`

- [ ] **Step 1: Write failing contract and API tests**

Add to `tests/test_video_api.py`:

```python
def test_video_generate_request_accepts_generation_world_hint():
    request = VideoGenerateRequest(
        text="demo",
        generation_world_hint="古城漫游，清晨阳光，IP 作为陪伴式向导出现。",
    )

    assert request.generation_world_hint == "古城漫游，清晨阳光，IP 作为陪伴式向导出现。"


def test_build_video_generation_params_copies_generation_world_hint():
    params = build_video_generation_params(
        VideoGenerateRequest(
            text="demo",
            generation_world_hint="古城漫游，清晨阳光，IP 作为陪伴式向导出现。",
        ),
        request_id="req_world_hint",
    )

    assert params["generation_world_hint"] == "古城漫游，清晨阳光，IP 作为陪伴式向导出现。"


def test_standard_video_generation_contract_normalizes_generation_world_hint():
    params = normalize_standard_video_generation_params(
        {"generation_world_hint": "  古城漫游  "}
    )

    assert params["generation_world_hint"] == "古城漫游"


def test_standard_video_generation_contract_omits_blank_generation_world_hint():
    params = normalize_standard_video_generation_params(
        {"generation_world_hint": "   "}
    )

    assert "generation_world_hint" not in params
```

Extend the existing sync API router test that asserts forwarded params, adding:

```python
generation_world_hint="古城漫游，IP 是陪伴式向导。",
```

and assert:

```python
assert captured_params["generation_world_hint"] == "古城漫游，IP 是陪伴式向导。"
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_video_api.py
```

Expected: fail because `generation_world_hint` is not accepted or forwarded.

- [ ] **Step 3: Implement contract field**

In `pixelle_video/models/video_generation_contract.py`:

- Add `"generation_world_hint"` to `STORYBOARD_PLANNING_OPTION_KEYS`.
- Add field to `StoryboardControlsContract`:

```python
generation_world_hint: str | None = None
```

- In `from_mapping(...)`, set:

```python
generation_world_hint=_normalize_optional_contract_string(
    mapping.get("generation_world_hint")
),
```

- In `to_planning_dict(...)`, include it in the existing loop immediately after `world_preset_id`:

```python
"generation_world_hint",
```

In `api/schemas/video.py`, add to `VideoGenerateRequest` near storyboard planning fields:

```python
generation_world_hint: Optional[str] = Field(
    None,
    max_length=4000,
    description="Per-generation world hint for the current source text. Display name remains 世界观提示.",
)
```

Add a field validator:

```python
@field_validator("generation_world_hint")
@classmethod
def normalize_generation_world_hint(cls, value: str | None) -> str | None:
    if value is None:
        return None
    normalized = value.strip()
    return normalized or None
```

In `api/routers/video.py`, add:

```python
"generation_world_hint": request_body.generation_world_hint,
```

next to `world_preset_id`.

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_video_api.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/models/video_generation_contract.py api/schemas/video.py api/routers/video.py tests/test_video_api.py
git commit -m "feat: 增加生成页世界观提示合同"
git push origin feat/generation-world-hint
```

## Task 2: Content World Model

**Files:**
- Create: `pixelle_video/models/content_world.py`
- Test: `tests/test_content_world_models.py`

- [ ] **Step 1: Write failing model tests**

Create `tests/test_content_world_models.py`:

```python
import pytest

from pixelle_video.models.content_world import (
    ContentWorldHintSource,
    ContentWorldProfile,
)


def test_content_world_profile_serializes_structured_fields():
    profile = ContentWorldProfile(
        summary="正定古城清晨漫游",
        time_space="当代正定古城，清晨，长乐门到城楼",
        visual_environment="青砖城墙、滹沱河水汽、古城街巷",
        atmosphere="温柔、历史感、慢节奏",
        cultural_context="中国古城文旅叙事",
        story_constraints="不能让 IP 替代长乐门、历史建筑或真实历史主体",
        ip_integration_guidance="IP 作为陪伴式向导，低侵入融入画面",
        hint_source=ContentWorldHintSource.MANUAL,
    )

    payload = profile.to_dict()
    restored = ContentWorldProfile.from_dict(payload)

    assert payload["summary"] == "正定古城清晨漫游"
    assert payload["hint_source"] == "manual"
    assert restored.ip_integration_guidance == "IP 作为陪伴式向导，低侵入融入画面"


def test_content_world_profile_compacts_blank_fields_to_none():
    profile = ContentWorldProfile(
        summary="  正定古城  ",
        time_space=" ",
        visual_environment=None,
        atmosphere="历史感",
    )

    assert profile.summary == "正定古城"
    assert profile.time_space is None
    assert profile.visual_environment is None
    assert profile.atmosphere == "历史感"


def test_content_world_profile_rejects_hex_color_leakage():
    with pytest.raises(ValueError, match="hex color"):
        ContentWorldProfile(summary="主色 #FFFFFF 的古城")
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_content_world_models.py
```

Expected: fail because the model file does not exist.

- [ ] **Step 3: Implement model**

Create `pixelle_video/models/content_world.py`:

```python
from __future__ import annotations

import re
from dataclasses import dataclass
from enum import Enum
from typing import Any, Mapping

_HEX_COLOR_RE = re.compile(r"(?<![0-9a-fA-F])#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})(?![0-9a-fA-F])")


class ContentWorldHintSource(str, Enum):
    MANUAL = "manual"
    GENERATED_FROM_SCRIPT = "generated_from_script"
    IP_DEFAULT = "ip_default"
    EMPTY = "empty"
    FALLBACK = "fallback"


@dataclass(frozen=True)
class ContentWorldProfile:
    summary: str | None = None
    time_space: str | None = None
    visual_environment: str | None = None
    atmosphere: str | None = None
    cultural_context: str | None = None
    story_constraints: str | None = None
    ip_integration_guidance: str | None = None
    hint_source: ContentWorldHintSource = ContentWorldHintSource.EMPTY
    generation_failed: bool = False

    def __post_init__(self) -> None:
        for field_name in (
            "summary",
            "time_space",
            "visual_environment",
            "atmosphere",
            "cultural_context",
            "story_constraints",
            "ip_integration_guidance",
        ):
            value = _optional_text(getattr(self, field_name))
            _reject_hex_colors(field_name, value)
            object.__setattr__(self, field_name, value)
        object.__setattr__(self, "hint_source", _coerce_source(self.hint_source))
        object.__setattr__(self, "generation_failed", bool(self.generation_failed))

    def to_dict(self) -> dict[str, Any]:
        return {
            "summary": self.summary,
            "time_space": self.time_space,
            "visual_environment": self.visual_environment,
            "atmosphere": self.atmosphere,
            "cultural_context": self.cultural_context,
            "story_constraints": self.story_constraints,
            "ip_integration_guidance": self.ip_integration_guidance,
            "hint_source": self.hint_source.value,
            "generation_failed": self.generation_failed,
        }

    @classmethod
    def from_dict(cls, payload: Mapping[str, Any] | None) -> "ContentWorldProfile":
        mapping = dict(payload or {})
        return cls(
            summary=mapping.get("summary"),
            time_space=mapping.get("time_space"),
            visual_environment=mapping.get("visual_environment"),
            atmosphere=mapping.get("atmosphere"),
            cultural_context=mapping.get("cultural_context"),
            story_constraints=mapping.get("story_constraints"),
            ip_integration_guidance=mapping.get("ip_integration_guidance"),
            hint_source=mapping.get("hint_source") or ContentWorldHintSource.EMPTY,
            generation_failed=bool(mapping.get("generation_failed", False)),
        )

    def has_content(self) -> bool:
        return any(
            (
                self.summary,
                self.time_space,
                self.visual_environment,
                self.atmosphere,
                self.cultural_context,
                self.story_constraints,
                self.ip_integration_guidance,
            )
        )


def _optional_text(value: Any) -> str | None:
    if value is None:
        return None
    text = str(value).strip()
    return text or None


def _coerce_source(value: Any) -> ContentWorldHintSource:
    if isinstance(value, ContentWorldHintSource):
        return value
    try:
        return ContentWorldHintSource(str(value))
    except ValueError:
        return ContentWorldHintSource.EMPTY


def _reject_hex_colors(field_name: str, value: str | None) -> None:
    if value and _HEX_COLOR_RE.search(value):
        raise ValueError(f"{field_name} must not contain hex color literals")


__all__ = ["ContentWorldHintSource", "ContentWorldProfile"]
```

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_content_world_models.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/models/content_world.py tests/test_content_world_models.py
git commit -m "feat: 增加本次文案世界画像模型"
git push origin feat/generation-world-hint
```

## Task 3: Content World Planner

**Files:**
- Create: `pixelle_video/prompts/content_world.py`
- Create: `pixelle_video/services/content_world_planner.py`
- Test: `tests/test_content_world_planner.py`

- [ ] **Step 1: Write failing planner tests**

Create `tests/test_content_world_planner.py`:

```python
import pytest

from pixelle_video.models.content_world import ContentWorldHintSource
from pixelle_video.services.content_world_planner import ContentWorldPlanner


@pytest.mark.asyncio
async def test_content_world_planner_uses_manual_hint_as_authority():
    calls = []

    async def fake_llm(**kwargs):
        calls.append(kwargs)
        return {
            "summary": "正定古城文旅",
            "time_space": "当代正定古城清晨",
            "visual_environment": "青砖城墙和水汽",
            "atmosphere": "温柔历史感",
            "cultural_context": "中国古城漫游",
            "story_constraints": "不能替代真实古建筑",
            "ip_integration_guidance": "IP 作为陪伴式向导",
        }

    profile = await ContentWorldPlanner().plan(
        llm_service=fake_llm,
        source_text="从长乐门出发，这是正定的南大门。",
        generation_world_hint="正定古城清晨漫游，IP 作为陪伴式向导。",
        ip_world_hint="IP 默认适合亲切讲解。",
        world_preset={"display_name": "Neutral"},
    )

    assert calls
    assert profile.hint_source == ContentWorldHintSource.MANUAL
    assert profile.summary == "正定古城文旅"
    assert profile.ip_integration_guidance == "IP 作为陪伴式向导"


@pytest.mark.asyncio
async def test_content_world_planner_uses_ip_default_without_manual_hint():
    async def fake_llm(**kwargs):
        return {
            "summary": "亲切科普世界",
            "ip_integration_guidance": "IP 作为知识讲解者",
        }

    profile = await ContentWorldPlanner().plan(
        llm_service=fake_llm,
        source_text="讲一个知识点。",
        generation_world_hint=None,
        ip_world_hint="适合亲切科普世界。",
        world_preset={},
    )

    assert profile.hint_source == ContentWorldHintSource.IP_DEFAULT
    assert profile.summary == "亲切科普世界"


@pytest.mark.asyncio
async def test_content_world_planner_falls_back_when_llm_fails():
    async def failing_llm(**kwargs):
        raise RuntimeError("llm down")

    profile = await ContentWorldPlanner().plan(
        llm_service=failing_llm,
        source_text="从长乐门出发，这是正定的南大门。",
        generation_world_hint=None,
        ip_world_hint=None,
        world_preset={"display_name": "Neutral"},
    )

    assert profile.hint_source == ContentWorldHintSource.FALLBACK
    assert profile.generation_failed is True
    assert "从长乐门出发" in profile.summary
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_content_world_planner.py
```

Expected: fail because planner files do not exist.

- [ ] **Step 3: Implement prompt builder and parser**

Create `pixelle_video/prompts/content_world.py`:

```python
from __future__ import annotations

import json
from typing import Any, Mapping

from pixelle_video.models.content_world import ContentWorldProfile
from pixelle_video.utils.json_parsing import parse_llm_json_response


def build_content_world_prompt(
    *,
    source_text: str,
    generation_world_hint: str | None = None,
    ip_world_hint: str | None = None,
    world_preset: Mapping[str, Any] | None = None,
) -> str:
    payload = {
        "task": "extract_current_generation_world_profile",
        "source_text": source_text,
        "generation_world_hint": generation_world_hint,
        "ip_default_world_hint": ip_world_hint,
        "world_preset": dict(world_preset or {}),
        "required_output": {
            "summary": "string",
            "time_space": "string",
            "visual_environment": "string",
            "atmosphere": "string",
            "cultural_context": "string",
            "story_constraints": "string",
            "ip_integration_guidance": "string",
        },
        "instructions": [
            "Return JSON only.",
            "Treat generation_world_hint as the highest priority when present.",
            "Use ip_default_world_hint only as compatibility guidance, not as the current story world.",
            "Do not output markdown fences.",
            "Do not output hex color codes.",
            "Do not copy field names into natural language values.",
        ],
    }
    return json.dumps(payload, ensure_ascii=False, indent=2)


def parse_content_world_profile(raw_response: Any, *, hint_source) -> ContentWorldProfile:
    if isinstance(raw_response, ContentWorldProfile):
        return raw_response
    if isinstance(raw_response, Mapping):
        payload = dict(raw_response)
    else:
        payload = parse_llm_json_response(
            str(raw_response).strip(),
            allow_code_fence=True,
            allow_embedded_json=False,
        )
    if not isinstance(payload, Mapping):
        raise ValueError("content world response must be a JSON object")
    payload = dict(payload)
    payload["hint_source"] = hint_source
    return ContentWorldProfile.from_dict(payload)


__all__ = ["build_content_world_prompt", "parse_content_world_profile"]
```

- [ ] **Step 4: Implement planner service**

Create `pixelle_video/services/content_world_planner.py`:

```python
from __future__ import annotations

from collections.abc import Mapping
from typing import Any

from pixelle_video.models.content_world import (
    ContentWorldHintSource,
    ContentWorldProfile,
)
from pixelle_video.prompts.content_world import (
    build_content_world_prompt,
    parse_content_world_profile,
)


class ContentWorldPlanner:
    async def plan(
        self,
        *,
        llm_service,
        source_text: str,
        generation_world_hint: str | None = None,
        ip_world_hint: str | None = None,
        world_preset: Mapping[str, Any] | None = None,
    ) -> ContentWorldProfile:
        hint_source = _resolve_hint_source(generation_world_hint, ip_world_hint)
        prompt = build_content_world_prompt(
            source_text=source_text,
            generation_world_hint=_optional_text(generation_world_hint),
            ip_world_hint=_optional_text(ip_world_hint),
            world_preset=world_preset,
        )
        try:
            response = await llm_service(
                prompt=prompt,
                response_type=dict,
                temperature=0.2,
                max_tokens=900,
            )
            profile = parse_content_world_profile(response, hint_source=hint_source)
            return profile if profile.has_content() else _fallback_profile(source_text)
        except Exception:
            return _fallback_profile(source_text)


def _resolve_hint_source(
    generation_world_hint: str | None,
    ip_world_hint: str | None,
) -> ContentWorldHintSource:
    if _optional_text(generation_world_hint):
        return ContentWorldHintSource.MANUAL
    if _optional_text(ip_world_hint):
        return ContentWorldHintSource.IP_DEFAULT
    return ContentWorldHintSource.GENERATED_FROM_SCRIPT


def _optional_text(value: Any) -> str | None:
    if value is None:
        return None
    text = str(value).strip()
    return text or None


def _fallback_profile(source_text: str) -> ContentWorldProfile:
    source_excerpt = _optional_text(source_text) or ""
    return ContentWorldProfile(
        summary=source_excerpt[:80],
        story_constraints="尊重原文主体，不替代真实人物、历史建筑、宗教主体或明确叙事对象。",
        ip_integration_guidance="根据每一帧语境决定是否出现 IP，优先低侵入、陪伴式或象征性融入。",
        hint_source=ContentWorldHintSource.FALLBACK,
        generation_failed=True,
    )


__all__ = ["ContentWorldPlanner"]
```

- [ ] **Step 5: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_content_world_planner.py
```

Expected: pass.

- [ ] **Step 6: Commit and push**

```powershell
git add -- pixelle_video/prompts/content_world.py pixelle_video/services/content_world_planner.py tests/test_content_world_planner.py
git commit -m "feat: 增加本次文案世界观规划服务"
git push origin feat/generation-world-hint
```

## Task 4: Frontend Storyboard Controls

**Files:**
- Modify: `web/components/storyboard_planning_controls.py`
- Modify: `web/components/style_config.py`
- Modify: `web/components/ip_prompt_chain_controls.py`
- Modify: `web/i18n/locales/zh_CN.json`
- Modify: `web/i18n/locales/en_US.json`
- Test: `tests/test_style_config_storyboard_planning_ui.py`
- Test: `tests/test_style_config_ip_controls.py`

- [ ] **Step 1: Write failing UI payload tests**

In `tests/test_style_config_storyboard_planning_ui.py`, extend payload helper tests:

```python
def test_build_storyboard_control_payload_includes_generation_world_hint():
    payload = build_storyboard_control_payload(
        world_preset_id="neutral_knowledge_storyboard",
        generation_world_hint="  古城清晨漫游，IP 是陪伴式向导。  ",
    )

    assert payload == {
        "world_preset_id": "neutral_knowledge_storyboard",
        "generation_world_hint": "古城清晨漫游，IP 是陪伴式向导。",
    }


def test_build_storyboard_control_payload_omits_blank_generation_world_hint():
    payload = build_storyboard_control_payload(
        world_preset_id="neutral_knowledge_storyboard",
        generation_world_hint=" ",
    )

    assert payload == {"world_preset_id": "neutral_knowledge_storyboard"}
```

Add a render test using the existing `_FakeStreamlit` pattern:

```python
def test_render_storyboard_advanced_controls_renders_generation_world_hint(monkeypatch):
    fake_st = _FakeStreamlit()
    fake_st.session_state["storyboard_planning_enabled"] = True
    fake_st.session_state["storyboard_generation_world_hint"] = "古城清晨漫游"
    monkeypatch.setattr(storyboard_planning_controls, "render_storyboard_planning_guide", lambda **_kwargs: None)

    payload = storyboard_planning_controls.render_storyboard_advanced_controls(
        ui=fake_st,
        translate=lambda key, **kwargs: key,
        session_state=fake_st.session_state,
        storyboard_default_enabled=True,
        world_library_loader=lambda: {
            "default_world_preset_id": "neutral_knowledge_storyboard",
            "items": [{"preset_id": "neutral_knowledge_storyboard", "display_name": "Neutral"}],
        },
        shot_library_loader=lambda: {"items": []},
    )

    assert payload["generation_world_hint"] == "古城清晨漫游"
    text_area_calls = []

    def _text_area(label, value="", **kwargs):
        text_area_calls.append({"label": label, "value": value, **kwargs})
        key = kwargs.get("key")
        if key in fake_st.session_state:
            return fake_st.session_state[key]
        return value

    fake_st.text_area = _text_area
    storyboard_planning_controls.render_storyboard_advanced_controls(
        ui=fake_st,
        translate=lambda key, **kwargs: key,
        session_state=fake_st.session_state,
        storyboard_default_enabled=True,
        world_library_loader=lambda: {
            "default_world_preset_id": "neutral_knowledge_storyboard",
            "items": [{"preset_id": "neutral_knowledge_storyboard", "display_name": "Neutral"}],
        },
        shot_library_loader=lambda: {"items": []},
    )
    assert any(call["label"] == "storyboard.generation_world_hint" for call in text_area_calls)
```

In `tests/test_style_config_ip_controls.py`, add:

```python
def test_render_ip_prompt_chain_controls_returns_selected_profile_world_hint(monkeypatch):
    fake_ui = _FakeStyleConfigUI()
    fake_ui.session_state["style_ip_enabled"] = True
    payload = ip_prompt_chain_controls.render_ip_prompt_chain_controls(
        ui=fake_ui,
        asset_bibles=[
            {
                "asset_bible_id": "bible_demo",
                "ip_profiles": [
                    {
                        "ip_profile_id": "ip_main",
                        "name": "白兔向导",
                        "world_hint": "适合亲切文旅讲解世界。",
                    }
                ],
            }
        ],
        translate=lambda key, **kwargs: key,
    )

    assert payload["ip_profile_world_hint"] == "适合亲切文旅讲解世界。"
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py tests/test_style_config_ip_controls.py
```

Expected: fail because `generation_world_hint` controls are not implemented.

- [ ] **Step 3: Implement payload helper and UI input**

In `web/components/storyboard_planning_controls.py`:

- Add `generation_world_hint` parameter to `build_storyboard_control_payload(...)`.
- Add it to `StoryboardControlsContract.from_mapping(...)` input.
- In `render_storyboard_advanced_controls(...)`, after world preset selection render:

```python
generation_world_hint = ui.text_area(
    translate("storyboard.generation_world_hint"),
    key="storyboard_generation_world_hint",
    height=96,
    help=translate("storyboard.generation_world_hint_help"),
)
```

- Include `generation_world_hint=generation_world_hint` in returned payload.

In `web/components/ip_prompt_chain_controls.py`, include selected IP world hint in return payload:

```python
"ip_profile_world_hint": _first_text(selected_profile.get("world_hint")),
```

Keep `ip_profile_world_hint` frontend-only unless later explicitly copied to backend; output request builders should not forward this field.

In `web/components/style_config.py`, update the compatibility wrapper:

```python
generation_world_hint: str | None = None,
```

and pass it through to `storyboard_planning_controls.build_storyboard_control_payload(...)`.

- [ ] **Step 4: Add i18n keys**

In `web/i18n/locales/zh_CN.json`, add:

```json
"storyboard.generation_world_hint": "世界观提示",
"storyboard.generation_world_hint_help": "描述本次文案发生的世界、叙事边界和 IP 融入方式；不会覆盖 IP 设计页的长期世界观提示。"
```

In `web/i18n/locales/en_US.json`, add:

```json
"storyboard.generation_world_hint": "World Hint",
"storyboard.generation_world_hint_help": "Describe the current script world, narrative boundaries, and how the IP should fit this generation. This does not overwrite the IP design world hint."
```

- [ ] **Step 5: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py tests/test_style_config_ip_controls.py
```

Expected: pass.

- [ ] **Step 6: Commit and push**

```powershell
git add -- web/components/storyboard_planning_controls.py web/components/style_config.py web/components/ip_prompt_chain_controls.py web/i18n/locales/zh_CN.json web/i18n/locales/en_US.json tests/test_style_config_storyboard_planning_ui.py tests/test_style_config_ip_controls.py
git commit -m "feat: 增加生成页世界观提示控件"
git push origin feat/generation-world-hint
```

## Task 5: Request Builders

**Files:**
- Modify: `web/components/output_preview.py`
- Test: `tests/test_output_preview.py`

- [ ] **Step 1: Write failing request propagation tests**

In `tests/test_output_preview.py`, add:

```python
def test_build_single_generation_request_includes_generation_world_hint():
    def _progress(_event):
        return None

    request = output_preview.build_single_generation_request(
        {
            "text": "demo",
            "mode": "generate",
            "generation_world_hint": "古城清晨漫游，IP 是陪伴式向导。",
        },
        progress_callback=_progress,
        session_state={},
    )

    assert request["generation_world_hint"] == "古城清晨漫游，IP 是陪伴式向导。"


def test_build_batch_shared_config_includes_generation_world_hint():
    shared_config = output_preview.build_batch_shared_config(
        {
            "generation_world_hint": "古城清晨漫游，IP 是陪伴式向导。",
        }
    )

    assert shared_config["generation_world_hint"] == "古城清晨漫游，IP 是陪伴式向导。"
```

Add a regression test:

```python
def test_build_single_generation_request_does_not_forward_ip_profile_world_hint():
    def _progress(_event):
        return None

    request = output_preview.build_single_generation_request(
        {
            "text": "demo",
            "mode": "generate",
            "ip_profile_world_hint": "frontend helper only",
        },
        progress_callback=_progress,
        session_state={},
    )

    assert "ip_profile_world_hint" not in request
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_output_preview.py
```

Expected: fail because request builders do not copy `generation_world_hint`.

- [ ] **Step 3: Implement request propagation**

In `web/components/output_preview.py`, add a helper:

```python
def copy_generation_world_hint(source, target):
    value = source.get("generation_world_hint")
    if value is None:
        return
    normalized = str(value).strip()
    if normalized:
        target["generation_world_hint"] = normalized
```

Call it in:

- `build_single_generation_request(...)`
- `build_batch_shared_config(...)`
- any existing batch per-item config merge path that copies storyboard controls

Do not copy `ip_profile_world_hint`.

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_output_preview.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- web/components/output_preview.py tests/test_output_preview.py
git commit -m "feat: 透传生成页世界观提示请求参数"
git push origin feat/generation-world-hint
```

## Task 6: Planner Chain Integration

**Files:**
- Modify: `pixelle_video/utils/content_generators.py`
- Modify: `pixelle_video/services/storyboard_planner.py`
- Modify: `pixelle_video/prompts/storyboard_planning.py`
- Test: `tests/test_styled_image_prompt_batch.py`
- Test: `tests/test_storyboard_planner.py`

- [ ] **Step 1: Write failing integration tests**

In `tests/test_styled_image_prompt_batch.py`, add:

```python
@pytest.mark.asyncio
async def test_generate_styled_image_prompt_batch_builds_generation_world_profile(monkeypatch):
    captured = {}

    class FakePlanner:
        async def plan(self, **kwargs):
            captured["world_planner_kwargs"] = kwargs
            return ContentWorldProfile(
                summary="正定古城清晨漫游",
                story_constraints="不能替代长乐门",
                ip_integration_guidance="IP 作为陪伴式向导",
                hint_source=ContentWorldHintSource.MANUAL,
            )

    async def fake_plan_storyboard_batch(**kwargs):
        captured["storyboard_kwargs"] = kwargs
        return type(
            "PlanResult",
            (),
            {
                "frames": (
                    FramePlan(
                        scene_id="scene-1",
                        shot_type="medium_shot",
                        shot_purpose="context",
                        world_elements=("青砖城墙",),
                        prompt_intent="建立古城漫游开篇",
                    ),
                ),
                "planning_snapshot": {
                    "world_preset_id": "neutral_knowledge_storyboard",
                    "world_preset": {
                        "display_name": "Neutral Knowledge Storyboard",
                    },
                },
            },
        )()

    async def fake_generate_image_prompts(*args, **kwargs):
        contexts = kwargs["prompt_contexts"]
        captured["image_contexts"] = contexts
        return ["base prompt"]

    monkeypatch.setattr("pixelle_video.utils.content_generators.ContentWorldPlanner", lambda: FakePlanner())
    monkeypatch.setattr("pixelle_video.utils.content_generators.plan_storyboard_batch", fake_plan_storyboard_batch)
    monkeypatch.setattr("pixelle_video.utils.content_generators.generate_image_prompts", fake_generate_image_prompts)

    result = await generate_styled_image_prompt_batch(
        llm_service=object(),
        narrations=["从长乐门出发，这是正定的南大门。"],
        image_config={},
        world_preset_id="neutral_knowledge_storyboard",
        generation_world_hint="古城清晨漫游，IP 是陪伴式向导。",
    )

    assert captured["world_planner_kwargs"]["generation_world_hint"] == "古城清晨漫游，IP 是陪伴式向导。"
    assert captured["storyboard_kwargs"]["generation_world_profile"].summary == "正定古城清晨漫游"
    assert result.planning_snapshot["generation_world_profile"]["summary"] == "正定古城清晨漫游"
```

In `tests/test_storyboard_planner.py`, add a prompt-capture test:

```python
@pytest.mark.asyncio
async def test_plan_storyboard_batch_passes_generation_world_profile_to_prompt(monkeypatch):
    captured = {}

    async def fake_llm_service(**kwargs):
        captured["prompt"] = kwargs["prompt"]
        return StoryboardPlanningResponse(
            frames=[
                StoryboardPlanningFrameResponse(
                    scene_id="1",
                    shot_type="medium_shot",
                    shot_purpose="context",
                    primary_subject="长乐门",
                    prompt_intent="建立古城漫游开篇",
                )
            ]
        )

    await plan_storyboard_batch(
        llm_service=fake_llm_service,
        narrations=["从长乐门出发。"],
        generation_world_profile=ContentWorldProfile(
            summary="正定古城清晨漫游",
            story_constraints="不能替代长乐门",
            ip_integration_guidance="IP 作为陪伴式向导",
        ),
        world_preset_id="neutral_knowledge_storyboard",
    )

    assert "generation_world_profile" in captured["prompt"]
    assert "正定古城清晨漫游" in captured["prompt"]
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_styled_image_prompt_batch.py tests/test_storyboard_planner.py
```

Expected: fail because chain does not accept or pass `generation_world_hint/profile`.

- [ ] **Step 3: Integrate content world planner**

In `pixelle_video/utils/content_generators.py`:

- Import:

```python
from pixelle_video.services.content_world_planner import ContentWorldPlanner
from pixelle_video.models.content_world import ContentWorldProfile
```

- Add parameter to `generate_styled_image_prompt_batch(...)`:

```python
generation_world_hint: Optional[str] = None,
```

- Resolve world preset before storyboard planning as already done for normalized style.
- Before `plan_storyboard_batch(...)`, call:

```python
generation_world_profile = await ContentWorldPlanner().plan(
    llm_service=llm_service,
    source_text="\n".join(narrations),
    generation_world_hint=generation_world_hint,
    ip_world_hint=getattr(ip_profile, "world_hint", None),
    world_preset=storyboard_world_preset if storyboard_enabled else None,
)
```

- Pass `generation_world_profile=generation_world_profile` to `plan_storyboard_batch(...)`.
- Add it to `planning_snapshot`:

```python
planning_snapshot["generation_world_hint"] = generation_world_hint.strip() if generation_world_hint else None
planning_snapshot["generation_world_profile"] = generation_world_profile.to_dict()
planning_snapshot["generation_world_hint_source"] = generation_world_profile.hint_source.value
```

- Ensure if `storyboard_enabled` is false but image prompts still run, the profile is available to image prompt contexts.

In `pixelle_video/services/storyboard_planner.py`:

- Add parameter:

```python
generation_world_profile: ContentWorldProfile | Mapping[str, Any] | None = None,
```

- Normalize:

```python
world_profile = (
    generation_world_profile
    if isinstance(generation_world_profile, ContentWorldProfile)
    else ContentWorldProfile.from_dict(generation_world_profile)
    if generation_world_profile
    else None
)
```

- Pass to `build_storyboard_planning_prompt(...)`.
- Add `generation_world_profile` to snapshot when present.

In `pixelle_video/prompts/storyboard_planning.py`:

- Add parameter:

```python
generation_world_profile: Mapping[str, Any] | None = None,
```

- Add to payload if present:

```python
payload["generation_world_profile"] = dict(generation_world_profile)
payload["instructions"].append(
    "Use generation_world_profile as the current script world. It refines this request and does not replace protected source subjects."
)
```

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_styled_image_prompt_batch.py tests/test_storyboard_planner.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/utils/content_generators.py pixelle_video/services/storyboard_planner.py pixelle_video/prompts/storyboard_planning.py tests/test_styled_image_prompt_batch.py tests/test_storyboard_planner.py
git commit -m "feat: 接入本次世界观到分镜规划链路"
git push origin feat/generation-world-hint
```

## Task 7: ImagePromptComposer and Pipeline

**Files:**
- Modify: `pixelle_video/services/image_prompt_composer.py`
- Modify: `pixelle_video/pipelines/standard.py`
- Test: `tests/test_standard_pipeline_prompt_prefix.py`
- Test: `tests/test_image_prompt_composer.py`

- [ ] **Step 1: Write failing composer and pipeline tests**

In `tests/test_image_prompt_composer.py`, add:

```python
@pytest.mark.asyncio
async def test_image_prompt_composer_passes_generation_world_hint(monkeypatch):
    captured = {}

    async def fake_generate_styled_image_prompt_batch(**kwargs):
        captured.update(kwargs)
        return StyledImagePromptBatch(prompts=["prompt"], planning_snapshot={})

    monkeypatch.setattr(
        "pixelle_video.services.image_prompt_composer.generate_styled_image_prompt_batch",
        fake_generate_styled_image_prompt_batch,
    )

    await ImagePromptComposer().compose(
        llm_service=object(),
        storyboard_plan=_plan(),
        image_config={},
        generation_world_hint="古城清晨漫游",
    )

    assert captured["generation_world_hint"] == "古城清晨漫游"
```

In `tests/test_standard_pipeline_prompt_prefix.py`, extend the existing visual-planning capture test:

```python
assert captured["generation_world_hint"] == "古城清晨漫游"
```

and set `ctx.params["generation_world_hint"] = "古城清晨漫游"` in the arrange section.

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_image_prompt_composer.py tests/test_standard_pipeline_prompt_prefix.py
```

Expected: fail because composer/pipeline do not pass the new field.

- [ ] **Step 3: Implement pass-through**

In `pixelle_video/services/image_prompt_composer.py`:

- Add `generation_world_hint: Optional[str] = None` to `compose(...)`.
- Pass it into `generate_styled_image_prompt_batch(...)`.
- In `_build_prompt_contexts(...)`, accept `generation_world_profile=None`.
- Add to `plan_context`:

```python
if generation_world_profile is not None:
    plan_context["generation_world_profile"] = generation_world_profile.to_dict()
```

In `pixelle_video/pipelines/standard.py`, when calling `ImagePromptComposer().compose(...)`, pass:

```python
generation_world_hint=storyboard_contract.generation_world_hint,
```

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_image_prompt_composer.py tests/test_standard_pipeline_prompt_prefix.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/services/image_prompt_composer.py pixelle_video/pipelines/standard.py tests/test_image_prompt_composer.py tests/test_standard_pipeline_prompt_prefix.py
git commit -m "feat: 透传世界观提示到图片提示词编排器"
git push origin feat/generation-world-hint
```

## Task 8: IP Usage Planner Integration

**Files:**
- Modify: `pixelle_video/services/ip_usage_planner.py`
- Modify: `pixelle_video/utils/content_generators.py`
- Test: `tests/test_ip_usage_planner.py`
- Test: `tests/test_styled_image_prompt_batch.py`

- [ ] **Step 1: Write failing IP planner tests**

In `tests/test_ip_usage_planner.py`, add:

```python
def test_ip_usage_planner_uses_world_constraints_for_protected_subjects():
    planner = IPUsagePlanner()
    frame = StoryboardPlanFrame(
        frame_id="frame_0001",
        index=1,
        source_text="佛祖的故事在古寺壁画中展开。",
        visual_goal="表现古寺壁画和宗教人物",
        prompt_intent="尊重宗教主体",
        shot_type="wide_shot",
        shot_purpose="context",
        primary_subject="佛祖与古寺壁画",
        secondary_subjects=(),
        continuity_anchors=(),
        world_elements=("古寺", "壁画"),
    )
    package = planner.plan_frame(
        frame=frame,
        ip_profile=_profile(),
        generation_world_profile=ContentWorldProfile(
            story_constraints="不能让 IP 替代佛祖、宗教人物或历史建筑。",
            ip_integration_guidance="只允许低侵入或不出现。",
        ),
    )

    assert package.ip_presence_type in {
        IPPresenceType.LOW_INTRUSION,
        IPPresenceType.SYMBOLIC_ONLY,
        IPPresenceType.ABSENT,
    }
    assert "佛祖" in package.must_not_replace
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_ip_usage_planner.py
```

Expected: fail because planner does not accept `generation_world_profile`.

- [ ] **Step 3: Implement IP planner consumption**

In `pixelle_video/services/ip_usage_planner.py`:

- Add `generation_world_profile=None` to `plan_batch(...)` and `plan_frame(...)`.
- Include it in `_presence_type_for_frame(...)`.
- Build a helper:

```python
def _world_profile_text(generation_world_profile) -> str:
    if generation_world_profile is None:
        return ""
    if hasattr(generation_world_profile, "to_dict"):
        mapping = generation_world_profile.to_dict()
    elif isinstance(generation_world_profile, Mapping):
        mapping = generation_world_profile
    else:
        return str(generation_world_profile)
    return " ".join(str(value) for value in mapping.values() if value)
```

- If world profile text contains protected-subject language such as `佛祖`, `宗教人物`, `历史建筑`, `真实人物`, force `LOW_INTRUSION` unless scene_cast explicitly sets presence.
- Extend `_landmark_terms(...)` candidates to include protected terms from profile text.

In `pixelle_video/utils/content_generators.py`, when invoking `IPUsagePlanner.plan_batch(...)`, pass:

```python
generation_world_profile=generation_world_profile,
```

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_ip_usage_planner.py tests/test_styled_image_prompt_batch.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/services/ip_usage_planner.py pixelle_video/utils/content_generators.py tests/test_ip_usage_planner.py tests/test_styled_image_prompt_batch.py
git commit -m "feat: 让IP规划读取本次世界观约束"
git push origin feat/generation-world-hint
```

## Task 9: Image Prompt Prompt Integration

**Files:**
- Modify: `pixelle_video/prompts/image_generation.py`
- Test: `tests/test_image_prompt_generation_prompt.py`

- [ ] **Step 1: Write failing prompt tests**

Create `tests/test_image_prompt_generation_prompt.py`:

```python
from pixelle_video.models.content_world import ContentWorldProfile
from pixelle_video.models.prompt_context import PromptContextEnvelope
from pixelle_video.prompts.image_generation import build_image_prompt_prompt


def test_image_prompt_prompt_includes_generation_world_profile_without_leaking_field_names():
    prompt = build_image_prompt_prompt(
        ["从长乐门出发。"],
        min_words=30,
        max_words=60,
        prompt_contexts=PromptContextEnvelope(
            plan_context={
                "generation_world_profile": ContentWorldProfile(
                    summary="正定古城清晨漫游",
                    story_constraints="不能替代长乐门",
                    ip_integration_guidance="IP 作为陪伴式向导",
                ).to_dict()
            },
            frame_contexts=[{"frame_source_text": "从长乐门出发。"}],
        ),
        prompt_language="zh_CN",
    )

    assert "generation_world_profile" in prompt
    assert "正定古城清晨漫游" in prompt
    assert "Use generation_world_profile as the current script world" in prompt
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```powershell
python -m pytest -q tests/test_image_prompt_generation_prompt.py
```

Expected: fail because explicit world-profile instructions are missing.

- [ ] **Step 3: Update image prompt instructions**

In `pixelle_video/prompts/image_generation.py`, add to `# Frame-Aware Context Contract`:

```text
- When `generation_world_profile` is present in plan_context, use it as the current script world for this generation. It refines world_preset and does not overwrite protected source subjects.
- Use generation_world_profile.story_constraints to protect original subjects, landmarks, historical buildings, religious subjects, real people, and explicit narrative objects from being replaced by IP elements.
- Use generation_world_profile.ip_integration_guidance to decide how the IP blends into the scene together with ip_adaptation.
```

Add to output requirements:

```text
- Do not copy internal keys such as generation_world_profile, story_constraints, ip_integration_guidance, ip_adaptation, or JSON labels into final image prompt strings.
```

- [ ] **Step 4: Run tests to verify GREEN**

Run:

```powershell
python -m pytest -q tests/test_image_prompt_generation_prompt.py
```

Expected: pass.

- [ ] **Step 5: Commit and push**

```powershell
git add -- pixelle_video/prompts/image_generation.py tests/test_image_prompt_generation_prompt.py
git commit -m "feat: 在图片提示词中使用本次世界观"
git push origin feat/generation-world-hint
```

## Task 10: Full Verification and Review

**Files:**
- No planned source changes unless tests reveal issues.

- [ ] **Step 1: Run focused backend suite**

Run:

```powershell
python -m pytest -q tests/test_content_world_models.py tests/test_content_world_planner.py tests/test_video_api.py tests/test_storyboard_planner.py tests/test_styled_image_prompt_batch.py tests/test_ip_usage_planner.py tests/test_image_prompt_generation_prompt.py tests/test_image_prompt_composer.py tests/test_standard_pipeline_prompt_prefix.py
```

Expected: all pass.

- [ ] **Step 2: Run focused frontend suite**

Run:

```powershell
python -m pytest -q tests/test_style_config_storyboard_planning_ui.py tests/test_style_config_ip_controls.py tests/test_output_preview.py
```

Expected: all pass.

- [ ] **Step 3: Run lint**

Run:

```powershell
ruff check .
```

Expected: pass.

- [ ] **Step 4: Review pass 1**

Manually inspect:

```powershell
git diff --stat origin/dev...HEAD
git diff origin/dev...HEAD -- pixelle_video/models/video_generation_contract.py api/schemas/video.py web/components/storyboard_planning_controls.py pixelle_video/utils/content_generators.py pixelle_video/services/ip_usage_planner.py pixelle_video/prompts/image_generation.py
```

Check:

- UI display text remains “世界观提示”
- `IPProfile.world_hint` was not renamed
- `generation_world_hint` is request-scoped only
- `ip_profile_world_hint` is not sent to API
- final prompt rules do not require negative prompt fields unsupported by Z-Image

- [ ] **Step 5: Review pass 2**

Run:

```powershell
rg -n "长期适用边界|ip_profile_world_hint|generation_world_hint|IPProfile.world_hint|世界观提示" web pixelle_video api tests docs -S
```

Check:

- no user-facing label uses “长期适用边界”
- `ip_profile_world_hint` appears only in frontend helper/UI tests
- `generation_world_hint` appears through contract, API, output request, planner chain and snapshots
- both `zh_CN.json` and `en_US.json` contain the new UI labels

- [ ] **Step 6: Final commit if verification fixes were required**

Only if Steps 1-5 required small fixes:

```powershell
git add -- <changed-files>
git commit -m "fix: 修复生成页世界观提示验证问题"
git push origin feat/generation-world-hint
```

If no files changed, do not create an empty commit.

## Acceptance Criteria

Implementation is complete only when all of the following are true:

- 生成页高级分镜规划区域显示一个名为“世界观提示”的输入框
- IP 设计页现有“世界观提示”仍然存在，字段仍为 `IPProfile.world_hint`
- API 接收并转发 `generation_world_hint`
- 单条和批量生成请求都能传递该字段
- 后端生成 `generation_world_profile`
- storyboard planner 能消费本次世界画像
- IP usage planner 能基于本次世界画像降低或禁止不合理 IP 出场
- image prompt prompt 能读取本次世界画像并禁止泄露内部字段名
- 未填写 `generation_world_hint` 时旧生成链路仍可用
- focused backend suite、focused frontend suite 和 `ruff check .` 均通过

## Implementation Notes

- 用户界面统一叫“世界观提示”，不要在 UI 上引入“长期适用边界”。
- `generation_world_hint` 是请求级字段，不应写回 AssetBible。
- `IPProfile.world_hint` 是资产级字段，不应被本次生成覆盖。
- 世界画像可以写入 `planning_snapshot` 方便调试，但不要让最终图片 prompt 出现 JSON key、字段名或 hex 色号。
- 如果 LLM 自动生成世界画像失败，必须降级，不应中断标准生成。
