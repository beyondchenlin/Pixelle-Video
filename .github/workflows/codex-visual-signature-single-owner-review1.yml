name: Codex visual signature single owner review1

on:
  push:
    branches:
      - codex/visual-signature-runtime-single-owner

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/visual-signature-runtime-single-owner
          fetch-depth: 0
      - name: Apply first-review hardening
        run: python tools/codex_patch_visual_signature_single_owner_review1.py
      - name: Syntax check
        run: |
          python -m py_compile pixelle_video/utils/content_generators.py
          python -m py_compile tests/architecture/test_visual_signature_runtime_single_owner.py
      - name: Commit first-review hardening
        shell: bash
        run: |
          rm .github/workflows/codex-visual-signature-single-owner-review1.yml
          rm tools/codex_patch_visual_signature_single_owner_review1.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test: harden V4.5 single-owner routing invariants"
          git push origin HEAD:codex/visual-signature-runtime-single-owner
